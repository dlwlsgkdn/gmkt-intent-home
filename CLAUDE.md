# DDAK Scenario Studio — 프로젝트 가이드

> 전체 기능 명세는 [FEATURES.md](FEATURES.md) 참고.

지마켓 뷰티 AI 쇼핑 컨셉의 **시나리오 목업 제작·발행 도구**. 사용자가 "설문→계획" 플로우 시나리오를 노코드로 편집·발행하면 홈 검색창 밑에 칩으로 노출되고, 칩 클릭으로 실제처럼 체험할 수 있다.

## 저장소 구조 (npm workspaces 모노레포)

```
apps/studio/       ← 스튜디오 소스 (React 18 + Vite 5). 빌드 산출물은 apps/studio/dist (커밋 안 함)
apps/core/         ← NestJS backend core — 쓰레드 저장·조회 (Drizzle+Neon). 실행·배포는 apps/core/README.md
apps/bff/          ← NestJS BFF — LLM(설문·계획 생성, 계획은 웹 검색 병행)·core 오케스트레이션. DESIGN-LLM-SERVICE.md §4, API.md §1 참고. **홈 검색 라우터**(`src/search/` — 검색어를 DDAK(설문→계획)/검색 결과 페이지 두 갈래로 가르고 AI 검색어 추천을 만든다: LLM effort low 1회, 쓰레드·core 기록 없음, LLM 이 막히면 같은 규칙의 휴리스틱이 `source: fallback` 으로 대신 답한다. 프롬프트는 @ddak/pipeline `SEARCH_ROUTE_SYSTEM`/`SEARCH_SUGGEST_SYSTEM` — PROMPT_DEFS 밖이라 운영 콘솔 재정의 대상이 아니다. API.md §1-2). 같은 컨트롤러의 **홈 첫 화면 재료**(2026-09) — `POST /api/search/home`(개인화 인사말+보라 칩: `WeatherService` 가 Open-Meteo 날씨를 붙여 `HOME_PERSONALIZE_SYSTEM` 1회, 휴리스틱은 @ddak/pipeline `home.ts`)·`GET /api/search/popular`(파랑 칩: `PopularSearchesService` — core KV `search-popular` 후보 표 인기순, 없으면 `POPULAR_SEARCH_SEED` 시딩, `/route` 검색어가 표와 일치하면 count+1). **상품·콘텐츠 추천(2026-09 개선)**: 계획 = 5a 뼈대 ∥ 5b 상품(`PLAN_PRODUCTS_SYSTEM` v22 — 상품 전용, 웹 검색 4회, 섹션당 5~8개·저·중·고 가격대·이름은 프로모션 문구 없이·가격 미확인은 0. **v25(2026-09-14): 지마켓 50 : 외부몰 50** — 섹션마다 지마켓 절반(웹 검색 `item.gmarket.co.kr/Item?goodscode=…` 상세 주소를 mall "지마켓"으로, 모자라면 카탈로그 productIds 최대 3개) + 외부몰 절반(올리브영 우선), 적합성이 비율보다 우선·모자라면 비운다) ∥ **5c 참고 콘텐츠**(`PLAN_CONTENTS_SYSTEM` — 상품과 분리된 검색 예산 3회, 영상·게시글 각 1회 이상, 출처 우선순위·최근 3년, 항목마다 고른 이유 `why`. 분리 이유: 한 호출에 몰아 두면 검색 상한 4회를 상품이 다 써 콘텐츠가 33% 누락됐다) → 6 검증(그라운딩 + 확장 게이트) → 7 기록(`llmMeta.quality` = `planQualityOf` 품질 요약 · `phases.contentsMs`). **뼈대 호출 재시도(2026-09-13)**: 5a 는 실패하면 계획 전체가 죽는 유일한 LLM 호출이라(운영에서 「일시적인 문제로 계획 뼈대 생성에 실패했어요」 — 재시도로 성공) `llm/retry.ts` `retryLlmStage` 가 SDK 자동 재시도(`new Anthropic({ maxRetries: 3 })` — 429·5xx·연결 오류 지수 백오프)를 다 쓴 `llm_failed` 뒤 2초 쉬고 한 번 더 부른다(그래프·legacy·dry-run 공통, 재시도 중 SSE `status` 「일시적인 오류가 있어 계획 뼈대를 다시 만들고 있어요…」 — 그래프는 `PlanStreamCoordinator.status`, legacy 는 `PlanStreamHandlers.onStatus`). 상품·콘텐츠 단계는 실패해도 계획이 살아 재시도하지 않는다(검색 호출이 길어 300초 상한을 위협한다). e2e 는 모의 서버 `PUT /internal/mock/llm-fail {count, status, only}` 로 529 를 뼈대 호출에만 4연속 주입해 5번째 성공을 검증한다. **뼈대 호출 스키마는 요청마다 두 갈래 중 하나다(2026-09-17)**: 구조화 출력은 스키마를 문법으로 컴파일하는데 크기 상한을 넘으면 API 가 400(「The compiled grammar is too large」)으로 요청을 즉시 거절한다 — v26 이 compare·caution(객체 5종)을 look(spec 객체 7종)과 한 합집합(7종)에 더하자 뼈대 호출이 0.4초 만에 매번 실패해 재시도로도 못 살렸다(운영 09-16~17, 계획 전부 실패). `@ddak/pipeline planSkeletonGenFor({ photo })` 가 `hasPhotoAnswer(survey, answers)`(사진 질문에 답이 있는가 — 프롬프트 가변부의 「얼굴 사진을 올렸습니다」와 같은 조건)로 사진 있음 = guide·look·products·contents·steps(v25 와 같은 문법 크기), 없음 = guide·compare·caution·products·contents·steps 를 고른다(LlmService.generatePlanSkeleton·dry-run 공통). 어느 갈래도 v25 보다 커지지 않는다 — **새 섹션 종류는 반드시 한 갈래에만 넣고** `packages/pipeline/test/skeleton-schema.test.mjs`·e2e(모의 서버가 요청 스키마의 `schemaKinds` 를 기록)로 확인할 것. 사진 쓰레드는 성분 비교표를 못 받는다 — SDK 가 판별자 const 를 문법에 싣지 않아(description 으로 접는다) 스키마만으로는 `kind: compare` 를 못 막고 파싱 실패로 계획이 죽으므로, `buildPlanSkeletonRequest` 가변부가 사진 있는 요청에 「compare·caution 을 만들지 않는다」 한 줄을 붙인다(시스템 프롬프트는 그대로 = 캐시 유지; 반대 방향 「사진 없으면 look 금지」는 시스템 프롬프트에 있다). 스트림 조각 검증·타입은 합집합 `PlanSkeletonSectionGen`/`PlanSkeletonGen` 그대로. 같은 사고의 교훈으로 `LlmGenerationError.detail`(API 상태·오류 문구·파싱 첫 이슈)을 **관리 dry-run·flow-run SSE `error` 이벤트에만** 실어(`sendSseFailure`, 운영 콘솔은 「— 원인: …」으로 덧붙여 보인다) Vercel 로그 없이도 운영 콘솔의 단계 단독 실행으로 원인을 읽는다 — 사용자 SSE 에는 싣지 않는다. `threads/enrich.service.ts` 가 빈 썸네일을 페이지 og:image 로 보강한다(항목 1.5초·전체 3초, 모바일 사파리 UA. **지마켓은 상품 번호로 gdimg 썸네일이 결정적이라 fetch 없이 채운다**(2026-09-14, @ddak/pipeline `gmarketThumbnailOf` — 검증 게이트도 같은 규칙으로 빈 썸네일을 채운다 = 지마켓 50 : 외부몰 50 의 지마켓 몫은 언제나 그림이 있다). **올리브영·쿠팡은 건너뛴다** — 올리브영 모바일 PDP 는 실제 상품 이미지를 og:image 로 주지만 Node(https·undici)의 TLS 지문에는 헤더·ALPN·TLS 버전을 맞춰도 403 「잠시만 기다려 주세요」 봇 도전 페이지가 온다(curl 은 통과, 2026-09-14 실측 — 지문 위장은 안 한다). 올리브영 검색 페이지는 JS 렌더라 몰 검색 링크(urlKind=search) 상품의 PDP 해석도 불가. 유튜브·틱톡·인스타도 건너뜀, `ENRICH_FETCH=0` 으로 끔 — e2e). 원장은 예산을 범위로 읽고(`parseBudgetRange` — 「4만원 이상」은 하한, 이전엔 상한으로 읽어 원하던 상품을 드롭했다) 같은 사용자의 최근 3개 쓰레드에서 이미 추천한 상품 라벨·보여준 콘텐츠 URL·담기 신호를 싣는다(`KnowledgeService.recentSelectionsFor` → 프롬프트 가변부 + 게이트 `already-in-cart`·`duplicate-recent`). src/engine/ = LangGraph 그래프 엔진(전략 문서 8단계 StateGraph — interrupt/재개·병렬·custom 스트림→SSE 브리지): core KV `engine`(legacy|langgraph, 기본 legacy) + 요청 헤더 `x-ddak-engine`으로 병행 배치, checkpointer는 LANGGRAPH_DATABASE_URL 있으면 Neon lg 스키마·없으면 메모리(유실 시 core 스텝 시딩 복구 — 진실 원천은 언제나 core). 노드 로직은 @ddak/pipeline 순수 함수 소비. 지식 5종·블록리스트는 core 설정 KV(knowledge-*·guard-blocklist, 30s 캐시)로 주입 — 시스템 자리표시자({{VOCAB}} 등, 캐시 흡수)와 원장 가변부로 갈라 싣고, 검증 게이트(블록리스트·의학 단정·원장 역대조) 드롭 사유와 원장 스냅샷은 plan 스텝 payload(dropLog·ledger)에 남는다. 게이트를 통과한 상품에는 **매칭율**(`@ddak/pipeline guards/match.ts` `scoreProductMatch` — 상품 프롬프트가 매긴 skin·concern·preference 1~5(+근거 notes, 예산 없으면 price)와 결정적 가중치 피부 25·고민 30·선호 20·가격 15(원장 예산 대비 가격비)·근거 10(카탈로그/PDP+썸네일/PDP)의 가중 합산 0~100, 항목별 점수·근거·계산식 basis)을 와이어 `match`로 붙여 같은 payload 에 남긴다 — LLM 평가가 없으면 원장 facts 태그 대조 휴리스틱으로 채운다
apps/tagging-api/  ← NestJS 서비스 — 사내망 Mongo(올리브영 상품)를 읽고 태깅 검토 결과를 되쓴다 (포트 8790). 문서↔화면 변환·되쓰기 화이트리스트는 src/mapping.ts 한 곳
packages/schema/   ← @ddak/schema — 쓰레드 도메인·internal API zod 계약 (install 시 prepare로 dist 빌드)
packages/pipeline/ ← @ddak/pipeline — LLM 파이프라인 이관 자산: 단계 카탈로그·프롬프트·생성 스키마·결정적 가드(그라운딩·병합·partial)·설문 와이어 조립(survey-wire — 질문 id 부여와 사진 질문 첫 자리 규칙을 legacy·그래프·dry-run이 공유)·LlmPort 계약·원장·지식 소스·홈 첫 화면 재료(`home.ts` — 인기 검색어 시드 표·순위 규칙, 날씨 코드 라벨, 시간대, 휴리스틱 인사말·추천 검색어; FE `lib/homePersonalize.js` 가 거울이라 규칙을 바꾸면 같이 맞출 것)·계획 품질 요약(`quality.ts` `planQualityOf`). **검증 게이트 규칙(guards/grounding.ts, 2026-09)**: 웹 상품 이름 정규화(`normalizeWebProductName` — 대괄호 프로모션·브랜드 중복 제거)와 가격 0 → `priceUnknown`, 판매처 이름은 주소 호스트로 정규화(`normalizeMallName` — 지마켓·올리브영·쿠팡·무신사·화해, 그 밖은 모델이 적은 이름)하고 지마켓 상품의 빈 썸네일은 상품 번호로 gdimg 파생(`gmarketThumbnailOf`, 프로토콜 생략 `//` 이미지는 https 로) — 테스트 `packages/pipeline/test/grounding.test.mjs`, 카탈로그 보충은 매칭율 60% 이상·고민 적합 3/5 이상·섹션당 3개까지(`catalog-low-match`·`catalog-overflow` — 14개 카탈로그가 용도와 무관하게 빈자리를 채우던 것), 담은 상품 재추천 드롭(`already-in-cart`), 콘텐츠는 3년 넘은 것(`stale-content`, meta 연도)·저신뢰 출처(KV `guard-content-hosts`, `low-trust-source`)·같은 출처 3개째(`duplicate-source`)·최근 쓰레드에서 보여준 URL(`duplicate-recent`) 드롭. `merge.ts` **병합 배정(2026-09 개정)**: 검색 섹션(5b 상품·5c 콘텐츠)은 「kind별 k번째 자리」가 아니라 **단계 묶음(guide+바로 뒤 자리들)에 의미 대조·순서 보존으로 배정**한다(`PlanPlacer` — 제목(+reason 0.3 가중)의 글자 2-gram 이 묶음 텍스트(안내 제목·서브타이틀·본문+자리 제목·기준)에 덮이는 비율, 공통 2-gram 은 묶음 간 문서 빈도로 감쇠, 빈자리 있는 묶음·직전보다 뒤 묶음에 +0.05, 같은 kind 의 직전 섹션이 간 묶음 이전으로는 못 간다. 닮은 정도 0.04 미만이면 옛 규칙(첫 빈자리 → 아직 안 받은 다음 묶음)). 자리가 있으면 그 자리, 없으면 묶음에 끼운다(상품은 그 묶음의 상품 자리 뒤·없으면 안내 바로 뒤, 콘텐츠는 연속 구간 끝 — 더는 steps 뒤에 매달리지 않는다) — 예전엔 뼈대가 자리를 1개만 두고 검색이 섹션 2개를 주면 둘째가 steps 뒤에 매달리고 첫 단계 상품이 뒤 단계 자리로 밀렸다(운영 기록 2건). 스트리밍 인덱스(`GeneratedIndexAllocator`)도 같은 배정기를 쓰고(자리 없는 섹션은 뼈대 길이 뒤 인덱스로 보였다가 result 에서 제자리), 스튜디오 dry-run 합성(`PipelineStudio` stagePlanView)은 `composePlanSections`(null 자리+pending)를 소스로 직접 import 한다. 상수는 2026-09 운영 쓰레드 2건으로 맞춘 것 — 바꾸면 `npm run test --workspace=@ddak/pipeline`(`packages/pipeline/test/merge.test.mjs`)로 확인. `consolidateSmallProductSections` 가 상품 1개짜리 섹션을 이웃 상품 섹션에 합친다(운영 기록의 23%가 1개짜리). `match.ts` 가격 적합은 예산 범위(하한·상한)와 가격 미확인(60점)을 본다. 프레임워크(LangGraph)·프로바이더 중립 순수 로직만 — bff는 여기서 import. 설계·페이즈는 DESIGN-PIPELINE-LANGGRAPH.md
api/               ← 스튜디오 서버리스 (Vercel 함수, 루트 고정) — state.js(동기화)·pdp.js(지마켓 PDP iframe 프록시)
middleware.js      ← 스튜디오 엣지 미들웨어 (루트 고정) — /api/bff/* 를 BFF로 rewrite + BFF_SERVICE_TOKEN 주입 (FE는 bff URL을 직접 안 부름. 스튜디오 프로젝트 환경변수 BFF_URL·BFF_SERVICE_TOKEN 필요)
legacy/            ← 옛 HTML 프로토타입 원본 (빌드 시 apps/studio/dist/legacy 로 복사됨)
```

루트 package.json이 워크스페이스 루트(`apps/*`, `packages/*`) — 설치는 항상 **리포 루트에서 `npm install`** 한 번, 락파일도 루트 하나다.

- **배포 (주)**: Vercel `ddak-scenario-studio`가 GitHub main 푸시마다 **원격 빌드·배포**. `https://ddak-scenario-studio.vercel.app` — vercel.json이 루트에서 워크스페이스 전체를 설치하고 apps/studio를 빌드해 outputDirectory `apps/studio/dist`를 서빙한다. API(`api/state.js`)도 같은 프로젝트라, **로컬 Node 없이 소스 푸시만으로 배포된다**
- **배포 (GitHub Pages)**: `.github/workflows/pages.yml`이 main 푸시마다 빌드해 아티팩트로 배포. `https://dlwlsgkdn.github.io/gmkt-intent-home/` (구 `…/docs/` 주소는 아티팩트 안 리다이렉트 스텁이 받는다). 커밋된 빌드 산출물은 더 이상 없다 — 두 배포 모두 push만 하면 각자 최신으로 빌드된다
- **빌드/배포 절차**: 소스 커밋 → 사용자가 `git push origin main` (Claude는 푸시 못 함) → Vercel·Pages가 각각 자동 빌드
- **배포 확장 규칙**: 새 앱(core·bff)은 같은 리포를 연결한 **별도 Vercel 프로젝트**(Root Directory=`apps/<앱>`)로 배포한다. 무관 커밋 스킵은 대시보드가 아니라 `apps/<앱>/vercel.json`의 `ignoreCommand`로 리포에 커밋한다 — Root Directory만 대시보드 설정. **비교 기준은 `HEAD^`가 아니라 마지막 성공 배포(`${VERCEL_GIT_PREVIOUS_SHA:-HEAD^}`)다**(2026-09-17): 머지 커밋을 푸시하면 `HEAD^`는 첫 부모(내 쪽)라 다른 쪽 커밋만 diff 에 잡혀 앱 수정이 든 푸시인데도 빌드를 건너뛰었다(bff 수정 91b3f02 + 스튜디오 커밋의 머지 ddce029 — bff 미배포). 이전 SHA 가 얕은 클론(depth 10) 밖이면 diff 가 실패해 빌드하는 쪽(안전)으로 떨어지고, 변수가 없으면 `HEAD^`로 폴백한다

## 명령어

이 머신은 기본 node(homebrew, v23+)로 빌드된다 (옛 안내의 `/Users/jinhalee/.nvm/...` v24 경로는 이전 머신 것 — 존재하면 그쪽을 써도 무방):
```bash
npm run build --workspace=apps/studio       # 스튜디오 빌드. vite build && cp -R ../../legacy dist/legacy
npm run build --workspace=apps/core         # core(NestJS) 빌드
npm run start:dev --workspace=apps/core     # core 로컬 실행 (환경변수: apps/core/.env.example — 없어도 부팅, DB 라우트만 503)
npm run db:generate --workspace=apps/core   # core 스키마 → drizzle/ 마이그레이션 SQL (오프라인)
npm run db:migrate --workspace=apps/core    # 마이그레이션 SQL 순서 적용 — DB 반영은 push(diff·타입 전환 실패)보다 이쪽 (--status·--baseline 지원)
npm run build --workspace=apps/bff && npm run e2e:mock --workspace=apps/bff  # bff 오프라인 E2E — 모의 core+Anthropic 위에서 LangGraph 엔진·legacy 전 구간 스모크 (네트워크·키 불필요)
npm run build --workspace=apps/tagging-api      # tagging-api(NestJS) 빌드
npm run start:dev --workspace=apps/tagging-api  # tagging-api 로컬 실행 (환경변수: MONGO_URI 등 — apps/tagging-api 참고, 사내망에서만 Mongo에 닿는다)
npm run test --workspace=apps/tagging-api       # tagging-api 단위 테스트 (node --test, 네트워크·Mongo 불필요)
npm run test --workspace=@ddak/pipeline          # 파이프라인 순수 로직 테스트 (node --test — 계획 병합 배정 등, 빌드 포함)
docker build -f apps/bff/Dockerfile -t ddak-bff .  # bff 컨테이너 (컨텍스트=리포 루트, 프로덕션 이관 준비 — DESIGN-PIPELINE-LANGGRAPH.md §6)
```
개발 서버: `.claude/launch.json`의 `scenario-studio` (포트 5173 — 맥에서 기본 `node`가 옛 nvm v12 로 잡히면 같은 서버를 nvm v24 경로로 고정한 `scenario-studio-mac`), 정적 검증용 `pages-static` (포트 8899, apps/studio/dist 서빙 — 빌드 후 사용), 라이브 API 를 목 BFF 로 돌려 화면을 검증할 때는 `scenario-studio-mockbff`(포트 5177 — `/api/bff` 프록시를 `VITE_BFF_PROXY`, 기본 `http://localhost:8799` 로 보낸다. 목 서버는 직접 띄운다: `GET /api/threads/:id` 가 계획 페이지를 돌려주면 `#thread/<id>` 이어보기로 LivePlayer 를 그대로 그릴 수 있다).

워크스페이스 주의: `@ddak/schema`·`@ddak/pipeline`을 수정하면 소비자가 dist를 보므로 `npm run build --workspace=@ddak/schema`(또는 `--workspace=@ddak/pipeline`)로 재빌드할 것(설치 시엔 prepare가 자동 빌드). **루트 devDependencies의 drizzle-orm은 지우지 말 것** — npm이 orm을 apps/core 아래로 중첩 배치해 루트에 호이스팅된 drizzle-kit가 못 찾는 문제를, 루트 선언으로 호이스팅을 강제해 해결한 것이다(core와 버전 범위를 항상 맞출 것).

**데이터 프로필** (`lib/remote.js`): 개발 서버 = `local`(localStorage 전용, 서버 동기화 없음), 빌드 산출물 = `prod`(localStorage + Neon DB 미러링). 로컬에서 운영 DB에 붙으려면 `VITE_DATA_PROFILE=prod npm run dev`. 콘솔 `[remote] 데이터 프로필:` 로그로 확인. 서버 미러링을 운영 DB 없이 검증하려면 목 API(+`VITE_API_PROXY`)를 쓰는 `scenario-studio-mockdb`(포트 5174) 참고. 맥에서는 `scenario-studio-mockdb-mac`(같은 포트, nvm v24 경로) + 목 API `PORT=8123 node apps/studio/scripts/mock-state-api.mjs`(메모리 Map — `api/state.js` 와 같은 GET/PUT 계약. 2026-09-16 「발행 → 새로고침에 탐색 페이지 유실」 재현·수정 검증에 썼다).

**서버 동기화는 자동 다운로드 + 수동 업로드(빌더) + 트랜잭션 자동 싱크(스튜디오 밖)** (`api/state.js` + `hooks/remote/`(useWorkspace가 배선) + `components/SyncButton.jsx`): 접속 시 서버로 하이드레이션하고, 빌더의 연속 편집은 "서버에 저장"으로 올린다 — 저장 상태·수동 저장 UI는 **빌더 상단바 SyncButton 전용**이다. **빌더를 떠나는 길(발행하기·발행 취소·홈으로)은 미저장 변경을 자동으로 올린다**(2026-09-16, `useTopBarActions.pushToServerSoon` — 수동 저장과 같은 `push` 경로라 충돌은 확인 대화상자·실패는 토스트, 전송 중에 눌린 수동 저장은 버리지 않고 큐에 넣는다. 이전엔 발행이 로컬만 바꾸고 홈으로 나가 홈에 저장 버튼이 없었고, 새로고침 때 부트 하이드레이션이 서버의 옛 셸로 탐색 페이지·발행 상태를 되돌렸다 — 실제 사고. 상단바 「로컬 자동 저장 · 시각」은 localStorage 저장 시각이지 서버 저장이 아니다). 홈은 쓰기가 전부 자동 싱크라 저장 UI가 없고, 프로필 버튼 배지로 다운로드 상태만 보여준다(동기화 중 펄스·연결 안 됨). 스튜디오 밖 단발 쓰기 트랜잭션(프로필 생성·삭제, 시나리오 생성·복제·가져오기·삭제·칩 순서, 쓰레드 기록·삭제, 전체 복원, 공유 링크 가져오기 — 프로필 전환은 쓰기가 아니라 제외)은 `requestAutoSync()`가 1.2s 디바운스 뒤 자동 업로드한다 — 전체 자동 미러링은 여러 창이 서로를 덮고 실패가 조용해서 제거했고, 자동 트랜잭션 싱크는 충돌(다른 창의 선행 변경) 시 덮지 않고 멈춰 수동 저장으로 유도하며 실패를 토스트로 알린다. "동기화 중" 펄스(`remoteSync.hydrating`)는 **활성 프로필의 홈 필요분을 받는 동안** 켜진다 — 첫 하이드레이션(homeSynced 전)과 프로필 전환 직후(그 계정 콘텐츠 로드 중, `syncingAccountIds`) 둘 다. 실패 시 "연결 안 됨"을 표시한다(미저장 표시는 빌더 SyncButton만). 저장 직전 `?index=1`(행 목록·updatedAt)로 다른 창의 선행 변경을 감지해 덮어쓰기 확인을 받는다. 키는 **화면 필요 단위의 행**이다 (분해·조립은 `lib/accountRows.js`): `account:<id>`(셸 본문 — 프로필·탐색·시나리오 칩 메타, 홈 첫 화면에 필요한 전부)·`account:<id>:scenario:<sid>`(콘텐츠: stages·planCases — 칩 클릭 체험·빌더용)·`account:<id>:versions:<sid>`(발행 버전 스냅샷 — 빌더 전용)·`account:<id>:threads`·`accounts-meta`(순서만 — **활성 프로필 id는 기기별 상태라 동기화하지 않는다**)·`keywords`·`starters-meta`+`starter:<id>`(기본 시나리오 라이브러리 — 지정 시점 불변 스냅샷이라 기준선·충돌 기계 밖에서 즉시 쓰기(last-write-wins)로 미러링하고(`hooks/remote/starterSync.js`), push의 충돌 비교도 starter 행을 무시한다. 메타는 부트에 실려 오고 스냅샷 본문은 새 프로필 생성 직전에만 받는다). **하이드레이션은 부트 1왕복 + 필요 시점 로드**: `?boot=<계정id>` 한 왕복(행 목록+메타+키워드+활성 계정 셸)으로 홈을 그리고(bootReady — 업로드 허용 시점), 나머지 계정 셸과 활성 계정 콘텐츠·쓰레드는 백그라운드(homeSynced — "동기화 중" 배지 기준), **버전 스냅샷은 빌더 진입 시**(`ensureStudioSynced`), 다른 계정 콘텐츠는 프로필 전환 시(`ensureAccountSynced`)만 받는다. 칩 클릭·복제는 `ensureScenarioSynced`, 내보내기·전체 백업은 `ensureActiveSynced`/`ensureAllSynced`가 선행한다(App.jsx `openSynced` 게이트). 하이드레이션 사이에 손댄 데이터는 서버 값으로 덮지 않는다(쓰레드만 id 합집합 병합 — 추가형 로그라서). **계정 목록은 첫 접속 시 서버(DB)가 원천**: 부트 시점 로컬 캐시 계정 중 서버에 없는 것은 정리하고 토스트로 알린다(이 세션에서 만든 계정과 시딩 가드 세션은 제외) — 다른 기기에서 지운 프로필이 옛 캐시로 부활해 재업로드되는 것을 막는다. 대가로, 프로필 생성 직후 자동 싱크(1.2s 디바운스+전송)가 끝나기 전에 새로고침하면 그 프로필은 서버에 없어 정리된다. **업로드는 행 단위 게이트**: 이 세션에서 서버와 맞춘 행(loaded)과 서버에 없는 새 행만 전송하고(`rowsRef` 기준선 — 안 받은 행을 낡은 로컬로 덮지 않는다), 삭제는 명시적(이 세션에서 지운 계정 + 셸을 맞춘 계정의 사라진 시나리오 행)이다. 예외는 시딩 가드·전체 복원 세션(`localAuthorityRef` — 로컬이 전체 의도라 전 행 전송+서버 잔여 정리). 구 형식(통짜 `accounts` 블롭 → 1차 통짜 행(`threads` 인라인) → 2차 콘텐츠 본문(scenarios에 stages 인라인))은 하이드레이션이 만나는 대로 새 행 체계로 이관한다(부속 행 먼저 → 본문 행 마지막이 완료 표식, 끊겨도 재시도). **객체 참조가 곧 미저장 신호**다: `patchActive`·`updateScenario`·`useStageItems.write`가 무변경 업데이터(같은 참조 반환)를 스킵하는 사슬을 유지할 것 — 끊기면 빌더만 열어도 미저장 배지가 켜진다. 발행 버전 스냅샷은 시나리오 전체 사본 — `VERSION_LIMIT`(5)을 늘리지 말 것. **주의: 구 클라이언트(옛 빌드 배포본)로 "서버에 저장"을 누르면 구형 행을 재저장해 새 행 체계와 어긋날 수 있다** — 지금은 Pages도 push마다 자동 빌드라 상시 위험은 아니지만, 배포가 밀린 옛 창을 열어 두고 저장하는 경우는 여전히 해당된다.

## 아키텍처 (apps/studio/src)

- `App.jsx` — **앱 셸**: 라우팅과 토스트, 그리고 화면들이 쓰는 `api` 객체 조립. 시나리오 CRUD는 여기, 그 밖은 아래로 위임. **페이지형 화면은 해시 URL이 원천** — `#builder/<sid>`(빌더)·`#explore-editor`·`#tagging`·**`#srp/<검색어>`**(검색 결과 페이지 목업 — 홈 검색 라우터가 DDAK 아님으로 가른 검색어의 착지, 주소가 곧 검색어. 진입은 `api.openSrp`, 뒤로가기로 홈 복귀)·`#ops[/<탭>]`(운영 콘솔 — 탭 threads|pipeline|experiment까지 해시 딥링크, 탭 전환도 pushRoute라 새로고침·앞뒤로가기가 탭을 유지. 구 `#admin` 주소는 호환 별칭)·**`#ops/threads/<threadId>`**(쓰레드 상세 — 열면 딥링크가 남고 닫으면 `#ops/threads`로, 뒤로가기가 다시 연다)·**`#thread/<threadId>`**(라이브 쓰레드 이어보기 — 주소가 곧 그 체험이다. 새 체험도 쓰레드가 생기는 순간 `markLiveThread`가 replaceState로 이 주소로 바꾼다: 히스토리는 안 늘리고 체험 도중에도 링크를 복사할 수 있다. 링크 복사 버튼은 쓰레드 패널 카드(라이브만)와 운영 콘솔 상세 헤더에 있다): 진입 함수는 pushState로 해시 엔트리를 만들며 직접 열고(생성 직후 상태 레이스 방지 — pushState는 hashchange 미발화), 주소 입력·앞/뒤로가기는 hashchange 핸들러 한 곳이 받는다(빌더는 ensureStudioSynced 게이트 후 열고 없는 시나리오면 홈 복귀). 이탈(goHome)은 해시 없는 pushState라 뒤로가기로 복귀 가능. 시나리오 체험(player)만 해시 없는 일시 상태로 남는다(로컬 기록이라 공유할 주소가 없다), 공유 링크는 `#s=` 별도 모드
- `hooks/useWorkspace.js` — **계정(프로필별 워크스페이스) 상태의 원본**: 로컬 상태·patchActive(무변경 참조 스킵)·localStorage 자동 저장·프로필 관리·전체 백업. 서버 쪽에는 stateRef·세터만 내어 준다
- `hooks/remote/` — **서버 미러링 한 벌** (위 "서버 동기화" 항목의 구현): `useRemoteSync.js`(공유 ref·동기화 상태·업로드 흐름 배선 — 반환하는 `remoteSync` 한 벌을 useWorkspace가 그대로 노출), `rowAdoption.js`(서버 행 채택 — 행→메모리 병합+기준선), `onDemandSync.js`(필요 시점 로드 syncRow·ensure*), `hydration.js`(부트 1왕복+백그라운드+구형 이관 — 시딩 가드 포함), `push.js`(미저장 감지 accountDirty + 업로드 코어 pushCore — 행 단위 게이트·명시적 삭제·충돌 처리), `starterSync.js`(기본 시나리오 라이브러리 미러링 — 즉시 쓰기·설치 직전 스냅샷 로드)
- `lib/scenarioOps.js` — 시나리오 통째 복사(복제·가져오기·공유 채택)의 **id 재발급 규칙**. 아이템 id는 parentId·계획 조건 questionId·평가 기록 키 세 곳에서 참조되므로 한 곳에서 다시 매단다
- `lib/store.js` — 데이터 계층 **배럴**. 실제 구현은 `lib/store/` 네 모듈:
  - `store/model.js` 시나리오·아이템의 형태와 정규화(`normalizeItems` — 구 좌표 모델 → 순서 모델 이관 관문), STAGES/DEVICE_PRESETS(실기기 CSS 뷰포트 `w`·`h` + 껍데기 종류 `frame` — 키는 저장 데이터가 참조하니 불변, 조회는 `viewerDeviceOf`)/CHIP_COLORS
  - `store/planCases.js` 조건 형태와 평가 규칙. **폴백은 언제나 하나·목록의 끝**이라는 불변식을 강제
  - `store/defaults.js` 첫 실행 기본값(탐색 페이지·프로필·키워드)과 exploreItemsFrom/visibleProfileItems
  - `store/persistence.js` **localStorage를 아는 유일한 곳** + 계정(구 키 마이그레이션)·전체 백업·JSON 입출력 봉투(`ddak-export` + `classifyImportPayload` — 구형 파일 3종도 계속 인식)
- `lib/builder/` — 빌더가 쓰는 순수 로직: `geometry.js`(순서·슬롯·컨테이너 판정 — 모델 좌표가 아니라 렌더된 DOM rect 기준, 필요한 값을 전부 인자로 받는다. reorderTop/topInsertIndexAt/containerAtClient/placeChild 등), `itemClipboard.js`(사본 만들기), `publishing.js`(발행 점검·칩 라벨·버전 스냅샷 — 구 스냅샷은 복원 시점에 normalizeItems로 이관)
- `lib/share.js` — 공유 링크: 시나리오를 `#s=<base64url JSON>` 해시로 인코딩/디코딩 (서버 불필요)
- `lib/evaluation.js` — 평가 계층 **배럴**. 실제 구현은 `lib/evaluation/` 네 모듈:
  - `evaluation/model.js` 평가 레코드 **v2 스키마**(selection/review/components)와 정규화·v1 마이그레이션 관문, remapCaseEvaluation(id 재발급), liveComponentEntries(고아 레코드 필터), plainEvaluationText
  - `evaluation/recommend.js` 대표 케이스 추천 — 자동 점수(rankSignificantCases) + MMR 다양화 + 미평가 로테이션(recommendRotationCaseIds)
  - `evaluation/structure.js` 케이스 → 평가 단위(컴포넌트 인스턴스) 변환. 여기서 만드는 editableFields가 AI 왕복 허용 목록의 원천 (NON_LLM_EDITABLE_FIELDS)
  - `evaluation/stats.js` 선정 CASE A/B/C 진행률 집계 + 케이스 리더보드(evaluationLeaderboard)
- `lib/registry.jsx` — **컴포넌트 레지스트리 배럴 + 조립**. `{ label, stage, icon, defaults, fields[], render(props, ctx), canvasInteractive?, defaultW? }`. ctx.mode='canvas'|'player', ctx.player(실행 API), ctx.profile, ctx.updateProps(캔버스 내 편집), ctx.summaryPreview. 개별 정의는 `lib/registry/` 카테고리 모듈에 있다:
  - `registry/support.jsx` 공용 렌더 도구 — kText() 텍스트 렌더러(키워드+부분 서식+인라인 편집 진입), ScrollTrack(드래그 스크롤), Img/parseCards/isEditView 등
  - `registry/exploreComponents.jsx` · `surveyComponents.jsx` · `planComponents.jsx` · `commonComponents.jsx` · `layoutComponents.jsx` 단계·카테고리별 컴포넌트 정의
  - LIBRARY를 알아야 하는 renderItem/ChildShell/childrenOf/libraryForStage는 배럴(registry.jsx)에 남는다 — 순환 참조 방지
- `lib/liveApi.js` — **라이브 생성 체험 클라이언트** (BFF threads API, API.md §1): 디바이스 id(`ddak-device-id`)·쓰레드 시작·SSE 소비(fetch 스트림 — POST라 EventSource 불가)·행동 기록·이어보기. FE는 same-origin `/api/bff/*`만 부른다 (배포 = 루트 middleware.js, 로컬 = vite 프록시 → 8788). 홈 검색창의 **진입 분기·AI 검색어 추천**(`routeSearch`/`suggestSearch` → BFF `POST /api/search/route|suggest`, API.md §1-2)도 여기 — 실패는 호출자가 `lib/searchCatalog.js` 의 같은 규칙 휴리스틱으로 받는다. **홈 첫 화면 개인화·인기 검색어**(`personalizeHome` → `POST /api/search/home`, `fetchPopularSearches` → `GET /api/search/popular`)는 `hooks/useHomePersonalize.js` 가 부르고 실패·지연은 `lib/homePersonalize.js` 의 휴리스틱·시드 표로 받는다
- `lib/adminApi.js` · `lib/adminReport.jsx` · `components/AdminView.jsx` — **운영 콘솔** (API.md §1-1). 진입은 홈 드로어 도구 행의 🧵 버튼 또는 `#ops` 해시(구 `#admin` 호환 — 탭은 `#ops/<탭>` 딥링크, App.jsx 라우팅 참고), 별도 인증 없음 — 옛 `x-admin-token` 게이트(localStorage `ddak-admin-token`)는 뗐고 BFF admin API도 서비스 토큰 가드만 남았다. 쓰레드 전체 목록(archived 포함)·개별 라이프사이클 로그를 **마크다운 문서로 시각화**(adminReport가 ThreadWithSteps→마크다운 생성 + 같은 서브셋만 아는 미니 렌더러 — 복사 버튼이 원문을 그대로 쓰므로 둘을 같이 유지), **평가 모아보기**(`AdminFeedback.jsx` — BFF `/api/admin/feedback` 한 응답(제출 1회=항목 1개·최신순·같은 (쓰레드,단계) 최신 제출에 latest, 원천은 core `/internal/feedback-steps` — core는 payload 해석 없이 type 필터만)을 받아 요약 타일·별점 분포·쓰레드 총 점수 분포+리더보드(유효본 별점 전체의 쓰레드 평균 — 평가 스튜디오 리더보드와 같은 정렬 문법)·필터(소유(실사용자|admin|전체 — admin=ops-playground 플레이그라운드 제출, 기본 실사용자. 집계·리더보드가 같이 갈려 실사용자 지표가 오염되지 않는다)/단계/코멘트만/이전 제출 포함)·펼침 목록으로 렌더. 집계는 latest 항목만으로 FE가 계산), 쓰레드 상세의 **실제 렌더링 미리보기**(`AdminThreadPreview.jsx` — 상세 다이얼로그 토글 문서/설문 화면/계획 화면. 스텝 payload의 와이어 페이지를 livePage 투영+레지스트리 player 렌더러로 그대로 그림, answers/explore 스텝 값으로 잠금 렌더·읽기 전용 no-op player 스텁), 보관 처리(= core `status=archived`, 사용자 목록에서만 숨김), LLM 모델 조회·변경(카탈로그는 BFF `llm.service.ts` `MODEL_OPTIONS` 소유, 저장은 core 설정 KV `llm-model`, 생성 시 30s 캐시로 조회 — haiku처럼 effort 미지원 모델은 BFF가 effort를 빼고 호출), **시스템 프롬프트 조회·재정의**(카탈로그 6종(intent·survey·plan-skeleton·plan-products·judge·judge-survey)은 @ddak/pipeline `prompts.ts` `PROMPT_DEFS` 소유 — `{{CATALOG}}` 자리표시자는 호출 시점에 상품 카탈로그 목록으로 치환(`renderSystemTemplate`), 재정의는 core 설정 KV `llm-prompt-<id>`에 원문 저장·생성 시 30s 캐시 조회(`resolveSystem`), 기본값과 동일·공백 저장은 설정 삭제 = 기본값 복귀, 재정의로 생성된 스텝은 `llmMeta.promptVersion`에 `+custom` 접미. 프롬프트는 캐시 적중 위해 바이트 고정이 전제 — 재정의 저장값이 곧 시스템 프롬프트다). **(2026-09-13 현황) 콘솔은 사이드바 레이아웃으로 개편돼 탭이 `dashboard|studio|pipeline|prompts|changes|knowledge|tagging|threads` 다** — 아래 "3탭" 서술은 개편 전 기록이고, 특히 "실험"(`ExperimentStudio.jsx`)은 현재 어디에도 마운트되지 않아 `#ops/experiment` 는 빈 화면이다. 엔진별 지연·**품질 KPI 는 대시보드(`#ops/dashboard`) 「운영 인사이트」 모드의 「추천 품질 신호」 카드**(`AdminDashboard.jsx` — BFF `GET /api/admin/metrics/engines` 의 엔진별 `phases` 평균·`quality`)에서 본다. 배포 운영 콘솔의 다이어그램은 「추천 만드는 과정」(`#ops/pipeline`)에서 「전문가 보기」 세그먼트를 눌러야 보인다. **페이지는 3탭 — 탭이 곧 해시**(`#ops`=쓰레드·평가, `#ops/pipeline`, `#ops/experiment`): "쓰레드·평가"(평가 모아보기+쓰레드 목록 — **소유 필터 seg**(전체|실사용자|admin — admin=ops-playground, 상태 흐름 바 개수·목록이 같이 걸리고 admin 행은 사용자 칩으로 표시)와 **상태 흐름 바**(체험 여정 탐색→설문→계획→완료 + 종착 이탈·보관을 칩 행으로, 칩 클릭 = 목록 필터)와 리더보드 평균 미터 바 포함, 상세 다이얼로그에 「평가 케이스로 저장」 승격 버튼), "파이프라인"(`PipelineStudio.jsx`), "실험"(`ExperimentStudio.jsx` — 골든 케이스(eval_cases) 실행·채점(eval_runs) + 전환 판정 계기판(plan 스텝 llmMeta.engine 각인의 엔진별 지연·캐시 집계 — **엔진 비교 카드**: 지연 4종(전체·뼈대·상품·콘텐츠)을 전 엔진 공통 축 가로 막대로, 캐시는 % 미터, 그 아래 **품질 KPI**(llmMeta.quality 를 가진 v22 이후 표본 평균 — 섹션당 상품·1개짜리 섹션·웹 상품 PDP·상품 썸네일·가격 미확인·콘텐츠 누락·콘텐츠 항목·콘텐츠 썸네일·드롭, BFF `engineMetrics` 집계). 케이스 행에는 채점 추이 점(오래됨→최신), 실행 행에는 별점 표시). **실행 채점은 평가 레코드 문법 한 벌**(라이브 피드백 ThreadStageFeedback과 동일): 사람 = 전체 별점(score, null=미채점)·코멘트 + 섹션별 components(`[{id: sec-<index>, label, score, feedback}]` — 실행 페이지 섹션 앵커, label 동봉으로 재해석 가능), 자동 = judge(**source 축 분리** — `✦ 자동 채점` 버튼이 BFF `POST /eval/runs/:id/judge`로 LLM 심사관(PROMPT_DEFS `judge`, 재정의 `llm-prompt-judge` — 편집기는 실험 탭 상단 details)을 불러 루브릭 4차원(근거 충실·맞춤성·단계 구성·실행 가능성, `JUDGE_DIMENSIONS`)+종합 별점+심사평을 `run.judge`에 저장. 사람 채점과 절대 합산·덮어쓰기 없음 — 화면도 보라 판정 칩·패널로 별도 표기. 비교축은 언제나 run.config 스냅샷). **실행에는 단계 축**(config.stage: plan|survey — 실행 설정의 세그먼트로 선택, 구 실행은 필드 없음=plan): survey 실행은 케이스 의도·프로필만으로 설문 페이지를 재생성(스냅샷 없는 케이스도 실행 가능)하고, judge도 단계별 심사관(plan=`judge`, survey=`judge-survey` — 루브릭 질문 절제·의도 적합·답하기 쉬움·말투, 편집기는 실험 탭 "채점 기준" details 2개)으로 갈린다. 설문 run의 채점 앵커는 와이어 질문 id(라이브 피드백과 같은 규칙). **✦ 전수 판정** = 케이스별 최신 실행 중 미판정분을 순차 일괄 judge(회귀 라운드 1회), **설정별 채점 요약**은 불러온 실행을 config 축(단계·promptVersion·what-if)으로 묶어 사람·판정 평균을 나란히(합산 없음) 보여준다). 파이프라인 탭 = **3컬럼**(`.sb-pipe-layout` grid-template-areas + `.sb-admin--wide` 1600px 컨테이너 — 1360px 이하 2컬럼(지식이 다이어그램 아래로), 1000px 이하 1컬럼): 왼쪽 = **지식 소스 → 파이프라인 카드**(붙박이 KV 4종+블록리스트+**운영자 추가 지식**, `knowledge-*`·`guard-blocklist` — 비우면 설정 삭제. 주입 위치로 묶고(시스템 자리표시자·원장 가변부·검증 게이트), 행마다 **「실을 단계」 토글 칩**(LLM 단계 전부가 후보, 켜짐=그 단계 프롬프트에 자리표시자가 들어 있음)을 달아 결선을 읽고 **바로 바꾼다** — 토글 = 그 단계 프롬프트 재정의 저장(`putAdminPrompt`, 토큰은 템플릿 끝에 붙는다. 위치를 손보려면 단계 레이어 편집기), 행 hover = 다이어그램 소비 노드 강조, 행 클릭 = 그 강조 고정. 코드 배선 지식(트렌드 키워드·쓰레드 피드백→원장 2, 블록리스트·콘텐츠 저신뢰 출처(`guard-content-hosts`)→검증 6)은 「고정 배선」 읽기 전용 칩이다. 결선 계산은 `lib/pipelineKnowledge.js`: 자리표시자는 그 단계의 **실효 프롬프트**(재정의 우선, 없으면 코드 기본값)에 토큰이 실제로 있는지로 판정하고(그래서 프롬프트를 직접 고쳐도 결선이 따라온다), 코드 배선은 같은 파일 `STRUCTURAL_ROUTES` 표가 원천 — 파이프라인 패키지 배선이 바뀌면 같이 맞출 것. **새 지식 타입 추가**(카드 머리 `+ 지식 소스` — 이름·자리표시자·제목 줄·값)는 BFF `POST /api/admin/knowledge`: 목록은 core 설정 KV `knowledge-custom`(JSON 배열, 파싱·검증은 @ddak/pipeline `parseCustomSources`), 값은 붙박이와 같은 `knowledge-<id>` 키. 주입은 언제나 시스템 자리표시자라 `renderSystemTemplate`이 `SystemKnowledge.custom`(토큰·제목·값)을 붙박이 4종과 같은 규칙으로 치환한다(값 없으면 자리 삭제). 삭제(`DELETE`)는 재정의 프롬프트에 남은 토큰까지 서버가 청소한다 — 안 지우면 `{{TOKEN}}` 원문이 모델에 나간다), 가운데 = **세로 흐름 다이어그램 카드**(`PipelineFlow.jsx` — 전략 문서 0~7을 실행 토폴로지 0↓1↓2↓3↓⏸답변 대기↓(5a ∥ 4·5b ∥ 5c)↓6↓7 **위→아래**로 렌더, 병렬 블록만 세 레인(뼈대 · 근거+상품 · 참고 콘텐츠, 2026-09). 연결선은 평시에도 중립 대시가 천천히 흐르고(데이터 관 표현), dry-run 실행 중엔 해당 경로가 보라 대시로 빠르게 흐르며 실행 노드가 맥동, 결과 도착 시 노드에 ✓지연·검증 통과/드롭이 각인. planned 점선·재정의 점 표식. **지식 유입 점**: 지식이 실리는 노드는 왼쪽 가장자리(지식 카드가 있는 방향)에 점 줄을 달고 점 하나 = 지식 하나·채움 = 값 있음, 노드 hover는 반대로 지식 카드의 해당 행을 밝힌다. 노드 폭 170px 고정 — 병렬 블록 분기·합류 가로 버스 오프셋(85px)이 이 값 절반에 묶임. 카드 머리의 런타임 설정 상자(`.sb-flow-config`)에 엔진 플래그(legacy|langgraph)·생성 모델 select(구 "생성 모델" 카드가 여기로 녹음 — 카탈로그·저장 경로는 동일)), **노드 클릭 = 단계 레이어 모달**(설명·effort·최근 실행 메타·요약 + **"이 단계에 실리는 지식"**(왼쪽 카드의 결선을 단계 쪽에서 되읽는 자리 — 값 없음·재정의로 빠진 자리표시자 경고와 지식 편집 바로가기) + **"이번 실행 프롬프트"**(플레이그라운드가 실제로 보낸 치환 완료 시스템 전문·user 가변부 — dry-run 결과 `prompt` 필드와 flow-run stage 이벤트가 원천) + LLM 단계는 시스템 프롬프트 열람·수정 편집기 내장 — 구 "시스템 프롬프트" 카드가 여기로 녹음. 저장·기본값 복귀 시 노드 재정의 점 표식도 즉시 동기화. 모델·프롬프트 상태는 PipelineStudio 소유 — AdminView에서 이관); 오른쪽 = **플레이그라운드 2모드**(머리 세그먼트로 전환, 실행이 가운데 다이어그램에 그대로 비친다 — 실행 버튼은 진짜 생성이라 ✦. 공통으로 **프로필 속성(고정 설문) 편집기**(실험 조건 행 편집 — localStorage `ddak-ops-pg-profile`, 계정 동기화 기계 밖. 실행 본문 profile로 실려 원장 facts가 되고 미리보기의 프로필 패널·설문 요약에도 그대로 렌더)를 갖는다): ① **단계 단독**(LLM 단계 dry-run — 그래프·쓰레드 기록 없음: 설문 실행→답변 선택→뼈대/상품/참고 콘텐츠(5c) 실행 — 상품·콘텐츠 결과는 종류별 자리 규칙으로 함께 합성. 결과도 아래 통합 실렌더 미리보기 한 벌로 그린다 — 계획은 뼈대+검색 결과를 자리 규칙대로 맞춘 FE 합성 병합(미실행·전부 드롭 자리는 자리 카드+안내), 드롭 로그·메타는 페이지 전체 말풍선, 임시 프롬프트 what-if는 저장 없이 1회 적용 — 이 모드 전용) ② **전체 플로우**(BFF `POST /api/admin/pipeline/flow-run` — 실제 LangGraph 그래프(병렬 5a∥5b·interrupt·검증 게이트)를 전용 MemorySaver로 통째 실행하고 **admin 프로필(ops-playground)의 core 쓰레드로 실기록**한다(설문·답변·계획 스텝 + llmMeta·dropLog — 쓰레드·평가 탭에서 열람·평가·케이스 승격 가능. core 미연결이면 쓰레드 생성 실패를 삼키고 flow- 임시 id로 기록 없이 강등, 결과 recorded 플래그·record 노드 요약에 저장 여부 표기). 플로우 시작(0→3·답변 대기까지)→답변 선택(실렌더 질문 카드·말풍선 칩)→계획 이어서 실행(재개·유실 시 body 시딩 재실행). SSE `stage` 이벤트(노드 완료마다 실시간 — 병렬 노드는 각자 완료 시점, LLM 단계 start에 실행 직전 재구성한 실제 프롬프트)가 다이어그램 라이브(`flowLive` — 다중 노드·유입 링크)·노드 각인과 **흐름 카드의 "이번 실행 관측"**(단계 수명 타임라인 — 행 클릭 = 단계 레이어 모달)을 채우고, `state` 이벤트(노드가 덮은 ThreadGraphState 채널 패치 — LastValue 누적 = 시점 스냅샷)가 **그래프 상태 패널**(타임라인 아래 접힌 details — 시점 칩 선택·채널 요약·JSON 펼침·이번에 덮인 채널 하이라이트, 시작 입력·체크포인트 재개는 합성 시점)을 채우며, `content` 이벤트(부분 페이지 조각)는 **실렌더 스트리밍 미리보기**(`FlowRunPreview.jsx` — 프로덕션 LivePlayer와 같은 문법: livePage 투영+레지스트리 player 렌더러 도착 즉시 렌더·kText revealFade·시작 스켈레톤·진행 꼬리·뼈대 조기 확정 뒤 자리 로딩 카드, 설문/계획 seg는 실행 구간 따라 자동 전환)가 그린다. 미리보기 오른쪽 레일은 **컴포넌트를 만든 재료·근거 데이터 말풍선**(.sb-bubble 재사용 — 평가 입력이 아니라 관측. 렌더에 이미 보이는 콘텐츠 원문은 되풀이하지 않고 "왜 선택·구성됐나"를 싣는다: 와이어 필드·투영 타입(head.intro→surveyIntro, sections[i]·kind→투영)·질문 id/단복수(+답변 선택 칩), 상품·콘텐츠는 근거(카탈로그 id·출처 몰·URL 도메인·썸네일 유무) + **자리 선정 기준**(뼈대 5a의 slot reason — 최종 병합에서 검색 5b reason에 밀려 렌더에 안 남는 원문을 그래프 상태 skeleton 채널·뼈대 dry-run에서 건져 자리 인덱스로 매단다), 말풍선마다 **"생성 쿼리" 링크**(만든 LLM 단계(설문 3/뼈대 5a/검색 5b)의 레이어 모달 → 이번 실행 프롬프트 전문). 페이지 전체 말풍선에는 의도 해석(1) 한 줄 + **생성 제약 근거(원장) 접기**(facts 출처 칩(프로필|답변|의도 해석|피드백|신호)·예산 상한·기피·트렌드 키워드·직전 피드백 — details 토글 시 말풍선 재배치). 자리엔 검색 진행 문구. 앵커 실제 렌더 높이 배치는 LivePlayer layoutFbBubbles와 같은 규칙 + 좁은 컨테이너(660px 미만·뷰포트 1100px 이하)는 is-stacked로 일반 흐름 전환). 페이지 전체 말풍선(--case)이 단계 메타·원장 키워드·검증 게이트 통과/드롭+드롭 로그를 담고, **설문 답변 선택(재개 입력)은 실렌더 질문 카드와 말풍선 칩 양쪽**(같은 pgAnswers), "계획 이어서 실행" 버튼은 설문 전체 말풍선 foot에 있다 — 구 "플로우 최종 계획" 평면 블록(AdminThreadPreview 합성)은 이 미리보기가 대체했고. 이 미리보기 한 벌을 두 모드가 공용하고 단계 실행 버튼(뼈대/상품)도 페이지 전체 말풍선 foot으로 옮겨졌다 — AdminThreadPreview는 쓰레드 상세 전용으로 남는다). 스타일은 `styles/admin.css`(배럴 끝 @import — 쓰레드 상태 흐름 바 연결선은 `.sb-flow__link`가 세로가 된 뒤 자체 가로 스타일)
- `lib/livePage.js` — 라이브 와이어 페이지 → 스튜디오 아이템 투영. **Figma [PP1K] 대조로 잡은 순서·형태(2026-09)**: 설문 헤더 제목 "설문 단계"·계획 헤더 "AI 맞춤 계획", 설문 인트로는 `splitIntro`가 문장이 둘 이상이면 첫 문장=제목·나머지=설명으로 가른다(한 문장이면 설명 비움), 계획은 planTitle(인용 “질의”만 — headline 은 보조 제목 자리) → surveySummary(둘이 붙어 플레이어에서 한 보라 밴드) → LLM 정리 문단(textBlock, id `live-plan-summary`는 평가 말풍선 앵커라 유지) → 섹션들 → 끝에 feedbackCard(`live-plan-helpful`, 평가 대상 아님). 상품 카드 170·콘텐츠 카드 260(콘텐츠가 **하나면 트랙 없이 최상위 전폭 카드** — Figma VideoCard 는 단계 본문 안 전폭 한 장이다. 카드 id 는 같아 스트리밍 중 둘 이상으로 늘어 트랙으로 바뀌어도 자리는 이어진다). question→surveyQuestion(아이템 id = 와이어 질문 id — answers 왕복에 재매핑 없음. 선택지는 와이어 `"제목|부제"` 문자열(FE 옵션 문법 splitOptions 와 동일 — `@ddak/pipeline` `optionWire`/`optionParts`): v17 설문 프롬프트가 선택지마다 제목(짧은 명사구)+부제(판단 기준 한 줄)를 한 벌로 만들고, 답변(choices)·프롬프트 가변부·심사 요청은 제목만 본다. **`kind:'photo'` 질문은 surveyPhoto**로 투영 — 얼굴을 봐야 답이 달라지는 의도(가상 메이크업·룩 제안)면 설문 생성이 머리 필드 photoQuestion으로 요청하고 BFF(@ddak/pipeline `buildSurveyPage`)가 앞머리에 세운다 — **스타일링 범위 질문(id `s1`, 스캐폴드 고정 「어디까지 스타일링해 볼까요?」 메이크업만/+헤어/+옷차림)이 먼저, 사진 질문(id `p1`)이 그 다음**(2026-09, v24 — 범위를 먼저 알아야 사진 안내를 얼굴 정면/머리 전체/상반신으로 바꿀 수 있다. `photoLeadQuestions`·`lookScopeFromAnswer`, FE 거울 `lookScopeOfAnswers`. 답은 뼈대 프롬프트가 룩 사양 `spec.scope` 로 옮기고 헤어·옷 사양은 범위 안일 때만 채운다). **사진 원본은 서버로 가지 않는다** — 기기(localStorage `ddak-live-photos-v1`)에만 남고 와이어·워크스페이스 쓰레드 기록에는 표식(`PHOTO_ANSWER`='사진 제출됨')만 실린다. 이어보기는 기기 보관분으로 되돌린다), look→**beforeAfter**(가상 메이크업 결과 — BEFORE는 올린 사진, AFTER는 `opts.photoAfter`(얼굴 랜드마크 합성 결과, `lib/makeupComposite.js` — 섹션 `spec` 대로 칠한다)가 있으면 그것, 없으면 같은 사진에 `tone` 프리셋(coral·rose·red·peach·brown·plum)을 올린 CSS 합성이다(사양이 있으면 `tint` 로 립 hex 를 프리셋 색에 싣는다) — 합성 이미지가 오면 tone을 비워 겹칠하지 않는다. 포인트 문구는 와이어 points(BFF 가 spec note 에서 파생), 없으면 FE `lookPointsOf` 폴백. 사진이 없으면(관리 페이지 미리보기·보관분 유실) noticeCard로 강등한다), guide→planStep(제목·서브타이틀(단계 목적 한 줄 — v17 뼈대 프롬프트가 반드시 채운다, 옛 페이지는 없음)·본문. 단계 번호는 투영이 자동 부여 — 뼈대 프롬프트가 안내를 2~3단계로 나눈다), products→hscroll+productCard(`priceUnknown`(판매가 미확인 웹 상품)은 가격을 비워 카드가 「가격 확인 필요」를 보인다 — 0원 노출 금지. `imageUrl` 있으면 실제 썸네일 — 카탈로그는 검증된 지마켓 gdimg, 웹 상품은 BFF http(s) 검증 통과분. 없거나 로드 실패면 이모지 목업 폴백(registry ProductThumb). `mall` 있는 상품은 웹 검색으로 찾은 상품(지마켓에서 찾은 것은 external 아님, 그 밖은 external — 몰 표기·담기 툴팁만 가른다). **어느 몰의 추천 상품이든 「담기」로 쓰레드에 담을 수 있다(2026-09-14 — 쓰레드의 담은 상품은 지마켓 장바구니가 아니라 이 체험의 픽 목록이고 Figma CartButton 도 어느 카드든 보라. 옛 외부몰 「상세보기/몰에서 찾기」 버튼은 담기로 바뀌었다)**, 상세 페이지·몰 검색은 썸네일 클릭 = 상세보기 패널. `url`은 상세보기 사이드 패널로 투영. **`urlKind: 'search'`**(v21 — 검색 결과에 PDP 주소가 없어 몰 검색 결과 주소를 대신 실은 상품)는 상세보기 패널이 모바일 검색 경로(올리브영·지마켓·쿠팡)로 열리며 안내 줄을 단다(카드 버튼은 담기). 검증 게이트는 이 표식이 있을 때만 검색 페이지 주소를 통과시키고(표식 없는 검색 주소는 예전처럼 드롭) 근거 신뢰 점수를 25로 낮춘다 — 이전 "PDP 만" 정책에서는 모델이 확인한 상품도 PDP 를 못 찾으면 버려 섹션당 상품이 1개로 말라 붙었다(2026-09 운영 기록 29섹션 중 16섹션이 1개, 드롭 로그는 비어 있었다). 같은 이유로 카탈로그(지마켓) 상품은 웹 상품이 4개 미만일 때 섹션당 최대 3개까지 채운다. 상품마다 검증 게이트가 계산한 `match`(매칭율 %·항목별 점수·근거·계산식)가 실려 「매칭율 n%」 배지(Figma MatchTag 자리, badge/blue 알약)가 서고, 배지를 누르면 계산 기준 팝오버(Figma 5-1/5-2 — 소개·항목별 막대·근거·종합 점수, 썸네일이 overflow hidden 이라 body 포털 fixed)가 뜬다. match 없는 옛 페이지만 `tag: 'AI 추천'` 문구 배지로 남는다), contents→hscroll+videoCard/articleCard(참고 콘텐츠 — 웹 검색으로 확인한 게시글·영상, 카드 클릭 = 새 탭. 5c 가 답변을 인용해 적은 `why` 는 카드 `note` 줄(보라 12px)로, 게시글은 note 가 있으면 snippet 대신 note. 썸네일은 `ContentThumb`이 고른다: imageUrl → 유튜브 공개 썸네일 → 틱톡은 브라우저가 oEmbed로 직접 받아옴(`tiktokThumbnail`, 서명 URL이라 저장하지 않고 렌더 시점 조회) → 끝내 없으면 매체명 자리 카드. 무관한 스톡 사진 폴백은 쓰지 않는다), steps→checklist, **compare→ingredientCompare(성분 비교표)·caution→cautionIngredients(주의 성분)**(v26, 2026-09-16 — 뼈대 5a 가 **성분이 곧 고르는 기준인 의도**(면도 뒤 붉어짐·따가움 같은 자극·민감 고민, 저자극·성분 비교 요구)에서만 원인·기준을 짚는 단계 안내 바로 뒤·그 단계의 상품 자리 앞에 [compare → caution] 으로 만든다. **제품 유형 수준의 대조**(기존/일반 제품 유형 vs 추천 기준 제품 유형)라 검색 없는 뼈대 몫이고 「구체 상품명 금지」를 지킨다 — 특정 상품의 전성분을 확인 없이 단정하지 않기 위해 상품 사진도 붙이지 않는다. 설문 프롬프트는 이런 의도에서 지금 쓰는 제품 유형을 한 질문으로 묻는다(비교표의 「기존 제품」 열). 병합(merge.ts planGroupsOf)은 둘을 단계 연속 구간으로 보아 자리 없는 상품·콘텐츠를 그 뒤에 끼우고 성분·제품 유형 텍스트를 배정 대조에 쓴다. 부분 스트리밍은 `@ddak/pipeline` `partialSkeletonSection`(guide·steps·compare·caution 공용 — 표·목록은 완성 행만, legacy·그래프 뼈대 노드가 같이 쓴다). 와이어 변환 `skeletonSectionWire` 가 빈 short·desc 를 뗀다. 테스트 `packages/pipeline/test/merge.test.mjs`·`skeleton-partial.test.mjs`, e2e 8.3). `livePlanItems(page, { pendingSlots })`의 pendingSlots(빈 자리 인덱스)는 레지스트리 밖 `livePending` 아이템으로 투영 — LivePlayer가 로딩 카드로 직접 그린다. **섹션 순회는 forEach가 아니라 인덱스 for문이다** — 스트리밍 중 `partial.sections`는 도착한 인덱스에만 값이 있는 희소 배열이라 forEach가 구멍을 건너뛰고, 그러면 아직 안 온 상품·콘텐츠 자리를 제자리에 그릴 수 없다(LivePlayer가 도착 최대 인덱스 앞의 구멍을 pendingSlots로 미리 넘겨 뼈대 확정 때 카드가 하단에서 튀어 오르지 않게 한다 — 자리 id가 같아 그대로 이어진다). 단계 안내(guide) 뒤에 **연쇄로 이어지는 상품·콘텐츠 섹션(빈 자리 포함)**은 `stepSub` 표식을 달아 LivePlayer가 그 단계에 붙여 배치한다(`sb-player__item--stepsub` — 묶음 표시는 **간격만**이다: 들여쓰기·연결선을 두면 가로 스크롤 트랙이 본문보다 한 단 밀려 보여 좌우를 본문과 딱 맞췄다 — v20 뼈대 프롬프트가 단계마다 [안내 → 상품 자리 → 콘텐츠 자리]를 두어 콘텐츠 카드가 계획 끝이 아니라 단계 본문 사이에 선다. 검색 단계는 콘텐츠 섹션을 단계 순서로 1~2개 만들고 병합이 제목을 단계 안내와 대조해 그 단계 자리(없으면 묶음 끝)에 넣는다). 새 렌더 계층을 만들지 않는다
- `lib/makeupComposite.js` — **가상 메이크업 합성(기기 안)**: MediaPipe FaceLandmarker(478점)로 얼굴을 잡아 **룩 사양(spec)대로** 칠한 데이터 URL을 만든다. **룩 사양은 한 원천이다(2026-09, v23)**: 뼈대 LLM 이 look 섹션에 `spec`(강도 natural|glam · 립 hex/마감/기법 · 치크 색/위치/발색 · 눈 섀도 0~3색/라이너/속눈썹/눈썹 · 베이스 마감/커버력/컨투어/하이라이트, 부위마다 한국어 note)을 채우면(@ddak/schema `LookSpec`, 생성 스키마 `LookSpecGen`, 와이어 전 `sanitizeLookSpec` 이 깨진 hex 를 tone 기본색으로 정규화), 기기 합성은 부위별로 그것을 직접 칠하고(베이스: 얼굴 윤곽 안 흐린 사본 얇게(커버력)·컨투어·하이라이트·매트 → 치크: `color`+`multiply` 타원 그라데이션 → 눈: 눈꺼풀→눈썹 방향 섀도 영역·라이너/윙·볼륨 띠·눈썹 채움 → 립: `color` 치환+`multiply`, gradient 는 방사 마스크, overlined 는 외곽 5% 확장, glossy 는 아랫입술 윤광. 부위별 try/catch), 정밀 렌더 지시문(`buildLookRenderPrompt`)도 같은 사양에서 영어로 생성한다 — 이전엔 tone 키 하나만 합성에 닿고 프롬프트는 고정 풀글램 템플릿 뒤에 한국어 포인트를 붙여 룩과 모순됐다(틴트 그라데이션 vs "never a sheer tint"). 화면 포인트(points)는 BFF `skeletonSectionWire` 가 note 에서 파생하고(`lookPointsOf`, FE livePage 거울), 사양 없는 옛 페이지·시나리오 목업은 tone 기본 사양(`specFromTone`)으로 옛 인상(틴트 그라데이션 립+볼 앞 블러셔)을 유지한다. **범위(`spec.scope` makeup|hair|outfit, v24)**: 헤어(스타일·기장·컬러·앞머리)·옷(상의·색·핏·넥라인)은 범위 안일 때만 사양에 실리고(`sanitizeLookSpec` 이 범위 밖은 떼고 범위 안 빈 부위는 keep 으로 세운다) 정밀 렌더 지시문에 「Styling beyond makeup」 줄로 실려 보존 목록에서 빠진다 — 기기 합성은 랜드마크가 얼굴뿐이라 메이크업만 그리고, 카드 고지 문구가 정밀 렌더 전까지 「헤어(·옷차림)은 정밀 렌더에서 반영돼요」를 덧붙인다. 샘플 얼굴 4종은 정사각 얼굴 크롭이라 옷차림 범위엔 부족하다(상반신 샘플 미추가). LivePlayer가 룩 섹션이 있을 때만 부르고 결과를 AFTER로 갈아끼운다(효과 의존성은 tone+spec 을 값으로 묶은 `lookSig` 문자열). **언제 실패해도 되는 향상 계층**이다 — 모델 로드 실패·얼굴 미검출이면 null을 돌려주고 화면은 tone 프리셋(CSS)으로 남는다. 자산은 전부 same-origin: 모델은 `public/mediapipe/face_landmarker.task`(커밋됨), wasm은 패키지 exports가 막아 `scripts/copy-mediapipe-wasm.mjs`가 predev/prebuild에 `public/mediapipe/wasm`으로 복사한다(12MB — .gitignore). **합성 자체는 기기 안에서 끝난다**(모델·wasm 전부 same-origin). 그 뒤 **2단계 정밀 렌더는 자동으로 이어진다** — BFF `GET /threads/capabilities`의 `imageEdit`이 true면 `POST /threads/:id/look-render`로 사진을 보내 외부 이미지 편집 모델의 결과로 AFTER를 갈아끼우고 배지가 "AI 메이크업 · 정밀"이 된다(false면 시도하지 않고 1단계에서 멈춘다). 편집 지시문(`@ddak/pipeline` `buildLookRenderPrompt`)은 **사양(spec)이 있으면 사양에서 생성**한다(2026-09 v23 — 메이크업 지시가 먼저, 강도(intensity)는 "얼마나"만 정하는 수식어, "사양에 없는 요소는 바르지 말 것" 명시, 동일성 보존·얼굴 보정 금지는 뒤에 짧게). 사양 없는 옛 페이지·호출만 옛 풀글램 템플릿(립 불투명 풀커버·진한 치크·크리즈 섀도+윙 라이너+볼륨 래시·컨투어+하이라이터)을 그대로 쓴다. **결과 보관 키는 색조+사양+사진 길이 서명**(`lookCache` row `key`) — 같은 쓰레드에서 설문을 다시 골라 사진이 바뀌면 옛 렌더를 새 사진 위에 올리지 않는다(옛 key 없는 행은 사양 없는 페이지에 한해 tone 일치로 받는다). 편집 모델은 출력 크기를 표준 규격(1024×1536 등)으로 맞추느라 원본(3:4 등)을 세로로 살짝 늘려 돌려주므로, FE가 `matchAspectTo`로 결과를 원본 비율로 되샘플링해 쓴다(2026-09 — 안 하면 슬라이더 두 층이 어긋나 애프터 얼굴이 길어 보인다. 옛 보관분도 불러올 때 같은 보정을 거친다). 진행은 AFTER 자리의 단계 표시로만 보인다: `skeleton`(합성 전 — 원본은 왼쪽에 바로 보이고 오른쪽만 로딩) → `landmark` → `refining`(1단계 그림 위 반투명 로딩) → `precise`. 실패는 1단계 그림을 그대로 두고 콘솔 경고로 끝낸다. **결과는 쓰레드+색조 키로 기기에 보관한다**(`lib/lookCache.js` — IndexedDB `ddak-live`/`look-renders`, 원본 화질 그대로 최근 12건. localStorage는 ~5MB라 렌더 몇 장에 조용히 넘쳐 유실됐던 사고의 교훈이고, 구 `ddak-live-look-v1` 키는 첫 조회 때 1회 이관된다) — 이어보기마다 유료 호출이 반복되고 그림이 매번 달라지는 것을 막는다. 카드의 「⬇ 메이크업 사진 저장」이 합성 결과를 기기 다운로드 폴더로 내려 주고(자동 저장은 브라우저가 불허 — 명시적 버튼), 저장한 파일은 사진 선택 시트의 "앨범에서 사진 선택"으로 되가져올 수 있다
- `lib/richtext.jsx` — 인라인 리치텍스트 엔진: `{{옵션|텍스트}}`/`[[키워드]]` 마크업 ↔ contentEditable 변환, 서식 적용/병합, InlineEditor, FONT_OPTIONS/TEXT_COLORS
- `lib/templates.js` — 새 시나리오 템플릿 (빈/뷰티 브리프/선물 추천)
- `lib/prompt/` — **AI 왕복 계층**. 스튜디오는 LLM API를 호출하지 않는다: 모든 AI 기능이 "프롬프트 복사 → 쓰던 AI에 붙여넣기 → 결과 가져오기" 한 가지 왕복이다
  - `chatPrompt.js` 채팅창용 출력 규칙 봉투(코드블록 하나·스키마 밖 키 금지·붙여넣을 위치) + 왕복 공용 유틸(`personaFromProfile` 프로필→페르소나 문장, `chunkList` 배치 분할)
  - `jsonAnswer.js` 붙여넣은 응답에서 JSON만 건져내기 — 모든 검증기가 공유
  - `scenarioDraft.js` 빠른 초안: 레이아웃은 코드 스캐폴드가 소유, AI는 텍스트·상품 배치만
  - `scenarioDb.js` 전체 구성: 레지스트리에서 컴포넌트 사양을 자동 추출해 DB JSON 전체를 요청하고, 가져오기 시 조건이 실제 질문 id·선택지를 가리키는지 검증
  - `planCases.js` 조합별 케이스: 설문 축 데카르트 곱, 골든 케이스 슬롯 추출(사실 필드 제외), 카탈로그 파싱, 프롬프트·검증·조립(id 재발급+parentId 재매핑)
  - `productSearch.js` 상품 리서치 프롬프트 + 결과 파싱(마크다운 표·번호·불릿 제거)·카탈로그 병합
  - `revision.js` 평가 피드백 → 필드 단위 수정안. 허용 목록(caseId·itemId·fieldKey) 밖은 전부 차단
  - `caseRevision.js` 케이스 **통째 재생성** (컴포넌트 추가·삭제·순서까지). 안전 모델이 다르다: 유지 컴포넌트는 id 보존(평가 기록이 id에 묶임)+원본 props에서 시작해 편집 가능 키만 덮음(사실 필드·팩 메타데이터 보존), 새 상품은 카탈로그 대조, 조건은 불변, 부분 적용 없음(전부/전무 + ⌘Z)
- `components/Builder.jsx` — **편집기 오케스트레이터**. 편집 상태만 갖고 규칙은 전부 아래로 위임한다
- `components/builder/hooks/` — `useStageItems`(아이템을 어디서 읽고 어디에 저장할지: 탐색/설문/계획), `useItemOps`(추가·수정·삭제·복제·순서 이동·클립보드 — 순서 변경은 전부 geometry.reorderTop 경유), `useBuilderHistory`(Undo 스택), `usePlanCases`(케이스 CRUD·평가), `useStackDrag`(최상위 순서 드래그 — 삽입 인덱스 미리보기 라인 + 컨테이너 삽입, 드롭 한 번으로 커밋), `useContainerNesting`(컨테이너 자식 넣기/꺼내기/슬롯 — 자식을 컨테이너 밖으로 끌면 최상위 순서 드래그로 전환), `useBuilderShortcuts`(키 매핑 — ↑↓ = 순서 한 칸 이동), `useTopBarActions`(시나리오 명령 — 기기 폭·발행·버전 복원·JSON 입출력·공유 링크), `useCanvasInteractions`(캔버스 표면 이벤트 — 우클릭·팔레트 DnD·줌), `useEvaluationBridge`(평가↔편집 이동 — feedbackTarget·인스펙터 포커스 신호, 상단 문맥 바는 `FeedbackFocusBar.jsx`)
- `components/builder/EvaluationPanel.jsx` — 평가 스튜디오 오케스트레이터 (말풍선 배치·케이스 탭·AI 진입점). 표현 컴포넌트는 `components/builder/evaluation/`: StarRating(+SCORE_GUIDE)/Rubric/Leaderboard/CommentBubble/PreviewBoundary
- `components/builder/BuilderTopBar.jsx` / `PlanCaseBar.jsx` / `BuilderCanvas.jsx` — 상태 없는 표현 컴포넌트
- `components/builder/PromptExchange.jsx` — "프롬프트 복사 → 결과 붙여넣기" UI 한 벌. 세 AI 다이얼로그가 공유
- `components/builder/LlmDialogShell.jsx` — AI 다이얼로그 공용 셸(백드롭 클릭 닫기·헤더·AiRoundTripNote 배지) + `DialogSteps`(진행 breadcrumb) + `BatchBar`(배치 진행 바). 여섯 AI 다이얼로그(시나리오/조합 케이스/문구 다듬기/재구성/전체 반영/AiFixChooser)가 공유
- `components/builder/CanvasItem.jsx` — 스택 위 최상위 아이템 한 개 (선택/순서 드래그/잠금/숨김, 우클릭 — 문서 흐름이라 좌표·리사이즈·높이 측정 없음)
- `components/builder/Palette.jsx` — 팔레트(검색·클릭 추가·캔버스로 드래그)/레이어 패널(잠금·숨김·순서)
- `components/builder/Inspector.jsx` — 속성 편집 / 필드 드래그 선택 서식 툴바 (크기 슬라이더는 컨테이너 자식 카드 전용). 목록형 필드(kind: options·stringList·cards·table)는 `ListEditors.jsx`의 행 단위 GUI 편집기로 위임 — 저장 형식은 기존 구분자 문자열 그대로, 줄바꿈 직렬화로 항목 안 쉼표 보존
- `components/builder/CanvasTextToolbar.jsx` — 캔버스 인라인 편집 중 선택 위에 뜨는 서식 툴바
- `components/ui/Dropdown.jsx` — 드롭다운 공용 래퍼 (버튼은 호출부, 메뉴/백드롭 담당)
- `components/Frame.jsx` — 공통 프레임 조각: BgBlobs, **FloatingBar**(Figma [PP1K] 계획 2-1·5-4 기준 — 홈·마이는 빠지고 쇼핑 쓰레드 히스토리 버튼 하나만 남은 56px 검정 원형 FAB(`sb-fabbar`, 흰 ≡, 테두리 없음 — 옛 68px 흰 면·파란 링은 이전 목업 파일 것, 2026-09-12 교체). **무엇을 여는지는 화면이 정한다(2026-09)**: 홈(검색 페이지)은 쓰레드 히스토리 패널, 설문·계획 체험 화면(Player·LivePlayer)은 **지금 진행 중인 쓰레드의 담은 상품 시트**(`components/ThreadCartSheet.jsx` — 쓰레드 패널 카드가 여는 것과 같은 Figma ThreadMoreSheet. 체험 화면에선 파트가 그 계획(케이스 아이템/라이브 guide)의 단계 목록, ⊖가 화면 로컬 담기 상태를 고치고(기록은 recordThread 효과가 따라간다), 계획이 아직 없으면 빈 상태 + CTA 「설문 이어서 답하기」, 푸터 밑 「다른 쇼핑 쓰레드 보기」 링크가 패널로 가는 유일한 길. 단계 제목은 `plainStepTitle`로 리치텍스트 마크업을 걷어 묶고 그린다). `position: fixed`라 기기 프레임 안에서는 화면 우하단(홈 인디케이터 위)에 붙고, 플레인 모드(평가 모드·폰 창)에서는 ViewerDeviceControl이 알린 `--sb-viewer-w`로 가운데 정렬된 프레임의 우하단을 계산한다. 패널은 항상 오른쪽에서 등장), ViewerDeviceControl(기기 선택 — `w×h` 표기), ProfileControl(프로필 전환/추가/삭제), StudioFab
- `components/DeviceFrame.jsx` + `lib/deviceScreen.js` + `styles/device.css` — **실행 화면 기기 프레임(2026-09)**: 홈·시나리오 체험·라이브 체험·SRP 네 화면이 고른 기기(DEVICE_PRESETS `frame`)의 껍데기 안에 **실기기 화면 크기(w×h)** 로 그려진다. 껍데기는 전부 CSS(FRAME_SPECS — iphone-island 다이나믹 아일랜드·iphone-home 홈 버튼·android 펀치홀·android-ultra 각진 울트라·fold 접힘선·tablet·browser 창)이고 상태 바(현재 시각·신호·배터리)·컷아웃·홈 인디케이터는 pointer-events 없는 기기 층이다. **화면(`#sb-device-screen`)은 `contain: paint`라 fixed 자손의 컨테이닝 블록** — 플로팅 버튼·쓰레드 패널·상품 상세 패널은 CSS 그대로 화면 안에 붙고, body 포털이던 시트·검색 화면은 `overlayRoot()`(프레임 없으면 body)로 포털한다. 스크롤은 화면 안 `.sb-device__scroll`이 돈다(창은 안 움직임) — 코드에서 창 스크롤 대신 `scrollScreenTo`/`screenScrollY`. 콘텐츠는 `--sb-safe-top/--sb-safe-bottom`(세이프 에어리어) 안쪽에서 시작하고, 창 기준이던 높이(`100dvh`)는 `--sb-vh`(=화면 높이, 플레인 모드엔 없어 100dvh 폴백), fixed 요소 크기의 `vh/vw`는 `%`로 바꿨다(컨테이닝 블록이 창일 수도 화면일 수도 있어서). 창이 기기보다 작으면 transform 으로 통째 축소(`--dev-fit`, 확대 없음 — 레이아웃 px·컨테이너 쿼리는 실기기 그대로). **플레인 모드(`.sb-stage.is-plain`)**: 라이브 평가 모드(말풍선 레일을 창 스크롤 기준으로 옆에 놓아야 함)와 640px 이하 창(브라우저가 곧 기기)에서는 껍데기를 벗고 예전 평면 흐름 — DOM 은 같고 CSS 만 바뀌어 페이지가 다시 마운트되지 않는다. 화면 안에는 DDAK 요소만 두고 스튜디오 크롬(기기 선택·프로필·스튜디오 버튼·스테퍼)은 프레임 밖 창 고정이다. 새 fixed 오버레이를 만들면 화면 안에 둘지(프레임 자식 또는 overlayRoot 포털) 창에 둘지(스튜디오 다이얼로그) 정할 것
- `components/ThreadPanel.jsx` — 쇼핑 쓰레드 히스토리 패널 (Figma [PP1K] Shopping Threads 룩 — 머리(제목+개수·새 쓰레드·닫기) 아래 bg/subtle 바닥에 카드 리스트: 제목(Gumi)+⋯ · 태그 칩(#칩/✦ 라이브·단계·평가) · **담은 상품 요약**(상품 카드 썸네일을 48px 동그라미로 잘라 겹침+"+n", 목업은 이모지·배경, 옛 이름-만 기록은 첫 글자 — 누르면 **상세 바텀시트**(`components/ThreadCartSheet.jsx` 로 분리 — 체험 화면의 플로팅 버튼도 지금 쓰레드로 같은 시트를 연다, 2026-09. Figma "내 프로필 · 쇼핑쓰레드 바텀시트" ThreadMoreSheet — 2026-09-06 대조: 제목 Gumi 16/21 왼쪽 + ✕ 24 · 요약 행 "k/n 파트 · m개 담음" 13/18 `--dk-label` | 합계 Gumi 14/20, 전폭 밑줄 · 파트 카드 bg/subtle r8(헤더 12/12/4: 제목 Gumi 12/17 + 18px 코랄 배지 `--dk-coral`) · 상품 행 8/12: 썸네일 60 r8(자리 그라데이션)·[브랜드] 500 + 상품명 14/20 Pretendard·가격 12/17 `--dk-muted-2`·몰 16px 원 아이콘+11/15·⊖ 24px · **담은 상품이 없는 계획 단계는 빈 파트 행**(64px 점선 자리 + 단계 제목·배지 + 안내 + 쉐브론 — 단계 목록은 `productLookupFromItems/FromPlanPage`가 `map.steps`(`{title, badge, products, pending?}` — 단계별 실린 상품 수, 라이브는 `pendingSlots` 로 아직 안 찬 자리 표식)로 함께 돌려준다. **빈 파트 행은 네 상태다(2026-09)**: 그 단계에 카드가 「담기」를 주는 상품이 실려 있으면 「상품을 추가해 보세요」(누르면 계획으로), 라이브 조기 확정 뒤 자리가 아직 안 찼으면 「상품을 찾고 있어요」(✦ 맥동), 상품이 하나도 안 실렸으면 「이 단계엔 추천 상품이 없어요」, 실렸지만 전부 담을 수 없는 상품(품절 = 카드 버튼 비활성 — `steps[].addable/soldOut`. 외부몰 상품은 2026-09-14부터 담기므로 라이브 상품은 전부 addable)이면 「품절 상품 n개 · 담을 수 없어요」(뒤의 둘은 플러스 없음)이고 요약 행 "k/n 파트"의 n 도 담을 수 있는 파트만 센다. 어느 단계에도 담을 수 있는 상품이 없고 담은 것도 없으면 파트 목록 대신 빈 상태 — 추천이 아예 없으면 「이 계획에는 담을 상품이 없어요」(요약 「추천 상품 없음」), 추천은 있지만 전부 품절이면 「담을 수 있는 상품이 없어요 · 추천 상품 n개가 모두 품절이라…」(요약 「담을 수 있는 상품 없음」) — 검증 게이트가 상품을 전부 드롭한 계획에서 고를 상품이 있는 것처럼 보이던 것을 고쳤다(2026-09). 시트의 `pending` prop(계획 생성 중·자리 대기)이 켜져 있는 동안은 「없음」으로 단정하지 않는다. 시나리오 쓰레드의 단계 목록은 체험한 케이스 것만) · 푸터 「뷰티 맞춤 계획 보기」=이어보기)) **체험 화면(Player·LivePlayer)의 시트에서는 파트가 곧 계획 단계 링크다(2026-09-14)**: 파트 머리(담은 상품이 있는 파트 — 버튼+쉐브론)·빈 파트 행(추천 상품 없음 행까지) 클릭 = 시트를 닫고 계획의 그 단계로 앵커 스크롤(`onOpenStep(part)` — `part.id` = 단계 아이템 id: 시나리오는 planStep 아이템 id, 라이브는 `livePlanSectionId(i)` 투영 id, 둘 다 `productLookupFromItems/FromPlanPage` 의 `steps[].id`). 스크롤은 `lib/deviceScreen.js` `scrollScreenToEl` 이 `.sb-player__item[data-item-id]` 래퍼를 찾아 한다(프레임 안이면 화면 스크롤러 기준 세이프 톱+12px 아래, 축소 프레임(--dev-fit)은 rect 를 레이아웃 px 로 되돌림, 플레인 모드는 창 스크롤 + 스테퍼 아래. 스택이 통째로 바뀐 커밋의 효과에서 낸 smooth 스크롤이 시작되지 않으면 두 프레임 뒤 다시 낸다). 설문 화면에서 누르면 계획으로 넘어간 뒤 효과에서 스크롤한다(LivePlayer 는 이미 만든 계획만 여는 goPlan — LLM 재호출 없음). 홈 쓰레드 패널의 시트는 onOpenStep 이 없어 예전 동작(빈 파트 행 → 이어보기, 추천 상품 없음 행은 못 누름)이다 · CTA(설문 중이면 「이어서 답하기」 Primary, 계획을 봤으면 「계획 보기」 Secondary). **⋯ 는 바텀시트**(라이브면 링크 복사 · 삭제하기) → 삭제는 가운데 **확인 모달**(「정말로 삭제하시겠습니까?」 삭제할게요/아니오 — 전체 지우기도 같은 모달, window.confirm 안 씀). 담은 상품 재료는 `lib/cart.js` — 상품 카드가 `addToCart(name, cartEntryFromProduct(p, { itemId, mall }))` 로 이름과 함께 브랜드·가격·몰·이미지·이모지·배경을 싣고, Player/LivePlayer 가 `stepInfoOfItem` 으로 카드 자리(컨테이너 앞의 가장 가까운 planStep)의 단계 제목·배지를 붙여 기록한다(옛 기록의 이름 문자열과 섞여도 `cartEntries()` 가 같은 형태로 읽는다. 라이브 cartAdd 이벤트도 같은 항목, 이어보기는 기기 쓰레드 기록의 cart 를 되살린다). 빼기는 `api.updateThread`. 좌/우/중앙 등장은 래퍼(.sb-thread-panel) 몫, 스타일은 home.css `.sb-thread*`·`.sb-cart-*`. **지난 쓰레드 더 보기(무한스크롤, 2026-09)**: 워크스페이스 기록은 최근 30개만 남기므로(App `recordThread` slice — 계정 행 하나에 통째로 실려 동기화되는 크기 때문에 늘리지 않는다), 기록이 30개에 닿으면 목록 끝 센티널(IntersectionObserver, 스크롤 상자 `.sb-thread__body`)이 BFF `GET /threads`(이 기기 기준 updatedAt 키셋 커서)를 30개씩 이어 받아 기록에 없는 라이브 쓰레드만 점선 카드(`.sb-thread-card--server` — 담은 상품·평가 마커 없음, ⋯ 는 링크 복사만)로 붙인다. **총 개수는 스크롤 전에 미리 구한다**: 패널을 열 때 목록 첫 페이지(limit 1)의 `total`(core count, archived 제외)을 받아 기록의 시나리오 쓰레드 수와 합쳐 머리 배지·「전체 n개 · 최근 30개」·「더 보기 (m개 남음)」에 쓰고, 남은 수가 0이면 더 보기를 접는다. 서버 목록은 기기 단위라 다른 프로필의 라이브 쓰레드도 섞일 수 있고, 시나리오 체험 쓰레드(기기 기록 전용)는 30개 밖으로 밀리면 복구할 수 없다)
- `components/ProductDetailPanel.jsx` — 상품 상세보기 사이드 패널: productCard "상세보기"가 외부몰 상품 페이지를 iframe으로 연다 (데스크톱 = 우측 패널, 모바일 640px 이하 = 전체화면 — viewer.css). Player/LivePlayer가 `ctx.player.openProduct({name, mall, url})`로 열고, url 없으면 토스트 폴백. **iframe에는 모바일 PDP 원본 URL을 그대로 싣는다**(`frameFor` — 지마켓 goodsCode→`m.gmarket.co.kr/vi/product/{code}`, 올리브영 goodsNo→`m.oliveyoung.co.kr` 모바일 경로, 그 외 몰은 원본): 구 프록시(`api/pdp.js` — 모바일 UA 대리 수신+base 주입)는 스크립트·서브리소스가 깨져 실제로는 빈 화면이 대부분이라 2026-08 FE에서 뗐다(함수 파일은 옛 배포 호환으로 유지). 한계: 데스크톱 브라우저 iframe은 데스크톱 UA라 몰이 데스크톱 페이지로 되돌리며 차단할 수 있다(지마켓 item.gmarket X-Frame-Options — 올리브영은 데스크톱도 차단 없음). 그 경우 "새 탭에서 열기"(항상 원본 URL)와 하단 안내 폴백이 받는다. productCard의 `url`은 사실 필드라 AI 왕복 제외 목록(NON_LLM_EDITABLE 등)에 이미 포함돼 있다
- `components/StarterPanel.jsx` — 기본 시나리오 패널 (전역 라이브러리 목록·내리기 — 진입은 드로어 하단 도구 행의 ⭐ 버튼, 지정은 시나리오 행)
- `components/TaggingStudio.jsx` + `lib/taggingCatalog.js` — **상품 태깅 검토 스튜디오** (진입은 드로어 도구 행의 🏷️ 버튼): 라이브 생성의 상품 매칭 근거인 카탈로그 태그의 "AI 1차 분류 → 담당자는 애매한 항목만 점검" 화면. 3컬럼 — 좌: 작업 단위 목록(파생 상태 칩: 검토 완료/미검토/수정 필요/승인됨/반려됨 + 헤더 필터 알약)+상품 정보(상세 문구·리뷰 요약·카탈로그 원본 태그), 가운데: 필드별 태깅 편집(FIELD_DEFS 7필드 — 대분류/세부유형/부위/타입/고민/결과/조건, 사전 기반 칩 토글·복수 선택 시 대표 태그 ★·AI 확신도 바·선택 근거 접기·미검토 "확인 완료로 표시"·사전에 없는 값은 "태그 추가 요청", 대분류 변경 시 종속 사전(세부유형·타입) 자동 정리), 우: 태그 게이지(최소 3·최대 10)+최종 적용 태그+규칙 검증(필수·대표 미지정·허용 목록 밖·충돌 조합)+검토 메모+승인/반려(승인은 오류·미검토 잔여 시 차단, 필드 수정 시 결정 초기화). 데이터 원천은 **사내망 Mongo(`<MONGO_DB>.products`)**다 — `apps/tagging-api`(NestJS, 포트 8790)가 읽고 되쓴다(문서↔화면 변환·되쓰기 화이트리스트는 `src/mapping.ts` 한 곳). 사전은 Python 저장소(`oliveyoung-collector-poc`)의 `taxonomy.json`을 `TAXONOMY_PATH`로 읽어 실어 보낸다 — 사본을 만들지 않는다. 승인/반려는 Flask 대시보드와 공유하는 `review_status`(`reviewed`/`needs_fix`)에, 화면 전용 상태(필드별 검토 표시·메모·AI 원본 스냅샷)는 추가형 `review_meta` 하나에 담는다. **Mongo가 사설 IP라 Vercel 서버리스에서는 닿지 못한다** — 배포본에서는 API 호출이 실패하고 예시 데이터(`TAGGING_SEED`)로 폴백한다. 검증 규칙은 아직 Flask `tagging_rules.py`와 두 벌이다(다만 규칙 문구·심각도 구분과 태그 개수 상한이 서로 다르다). 필드별 계약(문서 필드 대응·개수 규칙·되쓰기 화이트리스트·채움률)은 [TAGGING-DATA-CONTRACT.md](TAGGING-DATA-CONTRACT.md)에 따로 있다. 스타일은 `styles/tagging.css`(배럴 끝 @import)
- `components/Player.jsx` — 시나리오 실행(설문→계획 스테퍼). **설문은 질문 하나씩**: 질문형 컴포넌트(`registry/support.jsx`의 `QUESTION_TYPES` — 설문 질문·사진 업로드·날짜 입력)만 `qStep` 인덱스로 하나씩 보이고, 헤더·프로필 패널 같은 비질문 항목은 매 화면에 남는다. **앞으로 가는 버튼은 질문 컴포넌트 안에 있다** — 컴포넌트가 `ctx.player.nextQuestion()`을 부르면 플레이어가 커서를 넘기고, 마지막 질문이면 다음 단계로 보낸다(그래서 플레이어 하단 내비에는 「홈으로 / 이전 질문」만 남고, 질문이 하나도 없는 설문일 때만 하단에 다음 단계 버튼이 선다). **계획 페이지에는 하단 내비가 아예 없다**(2026-09-15 — 옛 「이전 단계 / 시나리오 완료·체험 완료」 줄 제거, Figma 계획 화면은 플로팅 버튼뿐: 뒤로는 화면 헤더·스테퍼가 맡고, 완료 기록(status completed·라이브 complete 이벤트)은 담기 요약·결제 바(ctaBar)에 버튼 문구가 있을 때 그 버튼으로만 남는다. 계획 섹션엔 `sb-player--plan` 이 붙어 플로팅 버튼 높이만큼 바닥 여백을 준다). LivePlayer도 같은 규칙 — 마지막 질문의 버튼이 계획 생성을 부른다. 기기 폭 반영, hidden 아이템 제외, 다시 시작, 응답/프로필 제외 상태를 ctx.player로 공급, **쓰레드 자동 기록**(체험 1회 = 쓰레드 1개)
- `components/LivePlayer.jsx` — **라이브 생성 체험** (설문→계획을 BFF의 LLM이 실시간 생성). 진입은 홈 자유 검색(칩 = 시나리오 체험 — 매칭 겹치면 선택 시트로 명시 선택), `✦ AI 실시간 생성` 배지 상시, **토큰 단위 부분 스트리밍**(SSE head/question/section — 자라는 중인 컴포넌트가 같은 index로 반복 도착하고 **도착 즉시 렌더**한다(별도 타이핑 페이싱 없음 — 클로드 앱 방식). 부드러움은 kText `revealFade`의 글자 단위 마운트 페이드(`sb-live-ch`)가 담당: 글자마다 위치 고정 키 span이라 새 글자만 페이드인된다. + 진행 꼬리(✦ 문구 한 줄 — 회색 자리 상자는 2026-09 제거, 처음 아무것도 안 온 동안의 전체 스켈레톤과 상품 자리 로딩 카드는 유지). BFF stream-parse가 미완성 원소를 복구 파싱해 ~120ms 스로틀로 재전송, 상품·콘텐츠 섹션은 **항목 단위 증분**: 완성·그라운딩 통과한 항목만 실은 섹션이 `final:false`로 같은 index에 반복 전송되고(threads.service `completeSearchSection` — 복구 파싱된 조각에서 버퍼상 마지막 키 배열의 마지막 원소(잘렸을 수 있음)를 버리고 항목 스키마 개별 검증, 항목 목록은 언제나 최종본의 접두라 한 번 나간 카드는 사라지지 않는다), 섹션이 닫히면 `final:true`로 마감한다. **계획은 뼈대 조기 확정**: 뼈대(텍스트)가 끝나면 `skeleton` 이벤트가 완성 섹션+자리(pending 인덱스)를 보내고, FE는 즉시 확정 렌더로 전환(planKey·설문 잠금·평가 초기화)하며 자리는 로딩 카드로 둔다 — 웹 검색(상품+참고 콘텐츠)이 계획 렌더를 막지 않고 `section`이 자리를 비동기로 채운 뒤(첫 증분이 로딩 카드를 대체하고 카드가 한 장씩 붙는다) result가 마감한다. pending이 남은 동안 재생성 트리거는 막는다(저장 경합 방지) — 증분(final:false)으로 채워진 자리도 최종본 도착까지 pending 유지. **클라이언트 소유 컴포넌트는 선렌더**: 설문은 프로필 패널을 생성 시작 즉시 그리고, LLM 산출물인 제목/인트로는 도착 전까지 감춘다. **계획은 첫 LLM 조각(제목·정리·섹션)이 오기 전까지 Figma "1. 로딩 & 진입" 대기 화면**(`LiveAnalyzing` — 화면 헤더 아래 진행률 원(시간 기반 근사, 90% 상한)·「분석 중이에요...」·분석 진행 상황 카드(3단계, 서버 status 가 검색 문구면 상품 단계로)·✦ status 한 줄. 20초가 지나면 「예상보다 시간이 걸리고 있어요」 + 「계속 기다리기 / 나중에 다시 시도」(취소 = 진행 중 스트림 이벤트를 버리고 설문으로 — 요청 자체는 끊지 못한다), 2026-09-12)을 보이고, 조각이 오면 요약 패널 선렌더 + 스트리밍으로 이어받는다 (Figma 1-3 "분석 완료" 화면은 스트리밍이 대신한다). `partial` 상태는 미리보기일 뿐 확정·기록은 언제나 result — planKey·설문 잠금도 result 시점), 실패는 정직한 안내(retryable 재시도 + 발행 칩 폴백은 사용자 클릭으로만 — 부분 도착분도 버린다), 답변 변경 시 계획은 자동 재생성하지 않고 안내 바의 버튼으로만. **이미 만든 계획은 어떤 단계 이동으로도 재생성하지 않는다**: 스테퍼는 항상 만든 계획을 그대로 열고, 재생성은 명시적 버튼(계획 stale 바·설문 하단 CTA)뿐. 계획 스냅샷 키(planKey)는 서버가 돌려준 answers 원문이 아니라 로컬 재구성과 같은 경로(`wireFromAnswers`)로 만든다 — 직렬화 차이로 무변경 답변이 "바뀜"으로 오판되면 이어보기 후 이동만 해도 LLM을 다시 부른다. **계획이 만들어진 설문은 잠긴다**(surveyQuestion locked, 선택된 답만 또렷): 해제는 "설문 다시 선택" 확인 다이얼로그를 거쳐서만, 새 계획이 생성되면 다시 잠긴다. 워크스페이스 쓰레드에 `live: true`로 upsert — ThreadPanel이 ✦ 배지로 구분하고 이어보기는 `GET /api/bff/threads/:id` 복원. 렌더는 livePage 투영 + 레지스트리 재사용. **피드백(평가)**: 스테퍼 알약 오른쪽 「평가」 토글(2026-09-16 — 옛 화면 헤더 아이콘은 뗐다, 「새로 생성」도 같은 자리) → **평가 스튜디오와 같은 주석(annotation) 문법** — 페이지 오른쪽 레일에 LLM 산출 컴포넌트(설문 인트로·질문, 계획 요약·섹션)별 말풍선(`.sb-bubble` 재사용 — 점선 연결선·별점·앵커 활성 외곽선·피드백 점, `LiveFeedback.jsx`)을 앵커의 실제 렌더 높이로 배치(layoutFbBubbles — 매 렌더 동기+rAF 보정, 1100px 이하는 CSS가 일반 흐름 전환), 페이지 전체 말풍선(케이스 스타일)에 저장 버튼. "피드백 저장" 1회 = events `type=feedback` 제출 1회(append 로그 — 최신 제출이 유효본, API.md §1). 이어보기가 단계별 최신 피드백을 복원하고, 계획 재생성 시 로컬 계획 평가는 비운다(대상 소멸). 계획 단계 레일의 **"✦ 반영해 다시 생성"**은 미전송 피드백을 저장한 뒤 같은 payload를 `POST /:id/plan`의 `feedback`으로 실어 **피드백 반영 재생성**을 요청한다 — BFF가 직전 계획(plan 스텝)+피드백을 프롬프트 가변부에 실어(시스템 고정 — 캐시 유지, `prompts.ts` `PlanRevisionContext`) 지적된 상품을 빼고 웹 검색 대안으로 교체한다. 워크스페이스 쓰레드 기록에 `feedback` 마커(단계별 전체 별점)를 남겨 ThreadPanel이 "💬 설문 ★n · 계획 ★n" 배지와 "💬 평가한 쓰레드" 모아보기 필터를 제공한다. 관리 페이지 문서엔 별점·코멘트로 렌더
- `components/HomeView.jsx` — 홈. **첫 화면 히어로**: 탐색 스택 앞머리의 인사말·검색창·발행 칩·키워드 칩(`HOME_HERO_TYPES`) 연속 구간을 `.sb-home-hero`(뷰포트 높이 상자)로 묶는다 — 인사말은 상단에 그대로, 검색창·칩 묶음은 `margin-top:auto`+55dvh 기준 아래 패딩으로 화면 높이의 45% 근처(정중앙보다 조금 위, Figma Search 1-1)에 오고, 스토리 카드 등 나머지는 그 아래로 흘린다. **첫 화면 개인화(2026-09)**: 인사말은 탐색 아이템(`greeting`)의 기본 문구가 먼저 보이고, `hooks/useHomePersonalize.js` 가 BFF `POST /api/search/home`(이름·프로필·기기 현지 시각·브라우저가 이미 허용한 위치(권한 팝업 없음)·최근 쓰레드 요약(`lib/homePersonalize.js` `threadDigests`)·최근 검색어 → BFF 가 날씨를 붙여 LLM 1회)로 받은 개인화 문구를 `components/ui/CrossFade.jsx`(grid 한 칸에 두 층을 포개 이전 문구는 흐려지고 새 문구가 떠오른다)로 얹는다 — 7초 안에 안 오면 같은 재료의 FE 휴리스틱으로 바꾸고 늦게 온 결과는 캐시에만 남긴다. 결과는 세션 캐시(프로필·쓰레드 상태·날짜·시 단위 키)라 홈 복귀마다 LLM 을 다시 부르지 않고 인사말이 흔들리지 않는다(캐시가 있으면 처음부터 개인화 문구). 인사말 컴포넌트의 `personalize` 토글로 끌 수 있다. **3영역 역할(2026-09)** — 세 자리가 전부 "무엇을 검색할지"를 제안해 겹치던 것을 한 단계씩 나눴다: ① 인사말 = **상태 인사**("당신을 알고, 지금 어디까지 왔는지" — 검색어·상품 제안 금지, 프롬프트·휴리스틱 모두. 최근 쓰레드를 가리키는 「」 부분이 응답 `threadIndex`→쓰레드 id 로 옮겨져 탭 대상이 된다: `greetingNodes` + `ctx.home.resumeThread` — 라이브는 `resumeLive`, 시나리오 체험은 `playScenario` 이어보기) ② 검색창 플레이스홀더 = **입력 형식 안내**(AI 아님 — 저자 예문 목록 `placeholders` 를 `rotateSec` 간격으로 로테이션, `{피부타입}`·`{이름}` 같은 프로필 라벨 토큰 치환 `fillProfileTokens`, 기본 예문은 defaults.js `DEFAULT_SEARCH_PLACEHOLDERS`, 기존 계정 검색창에는 persistence.js `seedSearchPlaceholders` 가 저자 문구를 첫 줄에 두고 한 번 채운다 — `explore.seeded.searchPlaceholders`) ③ 추천 칩 = **유일한 AI 추천 자리**(다음 행동 선택지 — 역할 캡션 없이 보라 ✦·파랑 🔍 칩을 **한 줄에 순서 없이 섞어** 세운다(2026-09-15, Figma ChipSection — 종류는 아이콘·색이 가른다. `mixChips` 결정적 셔플: 칩 내용+접속 1회 소금이 시드라 같은 내용이면 같은 순서, 새로고침마다 다르게. 옛 `showCaptions` 캡션 「내 쓰레드에서 이어서 / 지금 인기 검색어」와 `personal.basis` 는 뗐다)). 검색 화면 안 ✦ AI 추천 행은 타이핑 중 입력 확장이라 그대로다. 검색창 아래 **추천 검색어 칩**(`recommendChips` 탐색 컴포넌트 — Figma ChipSection 두 톤. 기존 계정의 탐색 페이지에는 persistence.js `seedRecommendChips` 가 발행 칩 목록 뒤에 **한 번만** 끼워 넣고 `explore.seeded.recommendChips` 표식을 남긴다 — 지워도 되살아나지 않는다): 보라 = ✦ + 같은 응답의 개인화 자연어 검색어(휴리스틱 칩 → LLM 칩 크로스페이드), 파랑 = 돋보기 + `GET /api/search/popular` 인기 검색어(옛 `#키워드` 표기는 2026-09-15 돋보기 아이콘으로 대체. core KV `search-popular` 후보 표 상위 3, 모듈 캐시 5분, 실패면 같은 시드 표). 아이콘은 `SearchOverlay.jsx` 의 `SparkIcon`·`SearchIcon` 재사용. **칩 클릭은 칩 종류가 곧 목적지다**(`homePlayer.submitSearch(text, to)` → `useSearchEntry.runSearch(q, { to })` — 라우터 판정·인기 count 반영 없음): 보라 = 맞춤 설문(DDAK) 직행(발행 칩과 겹치면 선택 시트), 파랑 = 검색 결과 페이지(SRP) 직행. 홈 개인화 호출은 서버 하이드레이션(`remoteSync.hydrating`) 중에는 보내지 않고 시그니처 변화를 500ms 모아 한 번만 보낸다(prod 첫 접속에 프로필·쓰레드가 순차로 채워지며 3번 나가던 것). 캔버스에는 점선 자리표시자. 발행 칩(색상/드래그 순서변경/클릭 실행), 좌상단 기기+프로필 컨트롤, 시나리오 드로어(템플릿·복제·**JSON 통합 입출력** — 내보내기는 범위 선택(시나리오 목록/전체 백업), 가져오기는 `classifyImportPayload` 자동 감지 후 동작별 확인 다이얼로그(추가/전체 교체). 빌더의 현재 시나리오 입출력은 별도), 쓰레드 패널. **검색창 진입(2026-09, Figma Search 1-2·1-3·2-1)**: 검색창을 누르는 순간 `components/SearchOverlay.jsx`(body 포털, 프레임 폭의 한 장 화면)가 덮는다 — 입력이 비면 **최근 검색어**(프로필별 기기 저장 `ddak-recent-searches-v1` — persistence.js `loadRecentSearches/saveRecentSearches`, 최신순 10개·시각(timeAgo)·✕ 삭제, 계정 동기화 기계 밖), 글자가 있으면 **자동완성 행**(`lib/searchCatalog.js` — 서버 없이 뷰티 어휘·조합·최근 검색어로 최대 4개, 입력어 부분만 굵게)과 그 아래 **✦ AI 추천 행**(BFF `/api/search/suggest`, 450ms 디바운스·순번 가드·세션 캐시, 실패면 대체 문장 `fallbackSuggest`). **제출은 `hooks/useSearchEntry.js` 한 벌**(홈·SRP 공용): 최근 검색어 기록 → BFF 검색 라우터 `POST /api/search/route`(LLM — 뷰티 카테고리 한정 설문→계획을 요구하는 검색어면 `ddak: true`) → true 면 기존 경로(발행 칩 매칭 시트 / `api.playLive`), false 면 `api.openSrp`. **검색 화면은 제출해도 닫히지 않는다** — 판정이 끝날 때까지 목록 자리에 ✦ 상태(「…살펴보고 있어요」 + 진행 바, 입력·뒤로 잠금)를 보이고 목적지 화면(라이브 생성·SRP)으로 바로 넘어간다(홈으로 되돌아왔다가 가면 검색창이 깜빡여 보였다, 2026-09-08). 칩 매칭 시트로 갈 때만 닫는다. 한글 조합 중 Enter(`isComposing`)는 제출하지 않는다. → **검색 결과 페이지 목업** `#srp/<검색어>`(`components/SearchResults.jsx` — **Figma Search "2-1. 검색 결과 — 전체 탭" 대조(2026-09-08)**: 검색 바(뒤로 + 44px 알약 필드, 누르면 같은 검색 화면이 검색어를 품고 다시 뜨고 ✕ 는 빈 채로) → 연관 검색어 칩(36px 알약 — 결과 상위 브랜드 「헤라 쿠션」·어휘 조합 `COMBOS`·카테고리 수식을 번갈아, 누르면 라우터를 거쳐 새 검색) → 탭 전체·뷰티톡·쇼츠·라이브·추천 상품(Gumi 16, 활성 `--dk-heading` #333 + 2px 밑줄, 비활성 text/disabled — **탭은 전부 동작한다(2026-09-17)**: 전체 = 섹션 4개 미리보기(4·4·4·8), 나머지 탭 = 그 종류 전체, 추천 상품 = 개수·정렬(랭킹·낮은/높은 가격·리뷰 많은순)·배송 필터(무료배송·스마일배송) 클라이언트 정렬, 「전체보기 ›」= 탭 전환) → ✦ 「맞춤 계획 받기」 배너(=playLive, Figma 밖 한 조각 — 라우터가 잘못 갈랐어도 여기서 복구) → 섹션 4개(머리 「제목 n · 전체보기 ›」, 2열 그리드 패딩 8·간격 8 = 카드 183): 쇼츠·라이브(183×278 r16 사진 위 그라데이션 + 아바타·11px + 제목 13px 2줄 + 메타(조회수·시점 / 시청자 수·「예정 · 오늘 오후 8시」 검정 알약), 라이브는 coral LIVE 알약 — 사진은 `public/sample-faces` 와 결과 상품 사진(contain, 위 72%)을 번갈아), 뷰티톡(accent-subtle r16 패딩 12: 아바타 24 + 이름 Gumi 10 + 시점 · 본문 13/18 · 사진 r8 = 그 글이 말하는 상품 · ♥ 💬 Gumi 10 tertiary), 상품(r16 1px border/subtle: 사진 200 + 엠블럼 배지(실데이터 「빅세일」 — Figma 「슈퍼딜」 자리)·AD·품절 + 44px 원 썸네일(+, 옵션 묶음만) · 제목 13/18 500 2줄 · 할인율 Gumi 14 action/primary + 가격 Gumi 14 + 정가 취소선 · ★별점(리뷰)·구매 수 · 🚚 무료배송|배송비 + 오늘출발·스마일배송·공식 태그, 클릭 = `ProductDetailPanel`(지마켓 모바일 PDP iframe — Player 와 같은 패널)). **결과 재료는 `lib/srpMock.js`(2026-09-17)**: 상품은 **실제 지마켓 검색 결과 스냅샷** `src/data/srpSnapshot.json`(`scripts/snapshot-srp.mjs` — `npm run snapshot:srp --workspace=apps/studio`, 모바일 검색 공개 페이지 `__NEXT_DATA__` 에서 뷰티 카테고리(화장품/향수 100000005·바디/헤어 100000071)만 41개 어휘 × 24개, 일반 상품 우선·광고 3개까지 `ad` 표식, 사내망은 `NODE_EXTRA_CA_CERTS` 필요. 값은 굳힌 시점의 가격·리뷰라 갱신은 스크립트 재실행) — 썸네일 `gdimg.gmarket.co.kr/{no}/still/280`·상세 `item.gmarket.co.kr/Item?goodscode={no}` 는 상품 번호로 결정된다. 청크는 SRP 에서만 동적 import(`loadSrpSnapshot`, 약 50KB gzip). 검색어 → 어휘는 `resolveSrpKeyword`(어휘·별칭(썬크림·파데·아이섀도우…) 중 가장 긴 것 — 「수분크림」이 「크림」보다), 순위는 노출 순서 뼈대 위에 검색어 어절(브랜드·이름 조각 +2)·수식어 동의어(촉촉→수분·글로우 +1)를 앞으로·광고 둘까지·품절 뒤로. 어휘 밖 검색어(「비오레 워터리」)는 전 스냅샷 이름 대조(걸린 상품이 많은 어휘가 문구 풀 기준), 그래도 없으면 데모 카탈로그 14종 폴백. 쇼츠·라이브·뷰티톡은 **카테고리 묶음(base·sun·skin·cleanse·lip·eye·cheek·body·hair·scent)별 문구 풀**에서 검색어 해시 시드로 뽑는다(같은 검색어 = 같은 화면. 검색어를 기계적으로 끼우지 않고 `{k}` 어휘·`{b}` 결과 브랜드·`{p}` 결과 상품 짧은 이름을 조사 맞춤 `{k:는}` 으로 채운다 — 옛 「촉촉한 {검색어}」·전부 38개·23.5K 식 목업을 뗐다). 개수는 상품 = 스냅샷 `itemCount`(수식 검색어는 해시 비율), 나머지는 해시 범위). 디스플레이 서체는 원본 CSS `*`/.clean-home-page * 가 font-family 를 !important 로 박아 같은 힘으로 덮는다(viewer.css 적용 블록과 같은 이유), Figma 600 은 굵기 규칙대로 500). 라우터가 막히면 같은 규칙의 휴리스틱(`heuristicRoute`)으로 가른다 — 검색은 언제나 어디론가 간다. 탐색 검색창(`registry/exploreComponents.jsx` searchBox)은 player 모드에서 `onFocus` → `ctx.player.openSearch`. 스타일은 `styles/search.css`(배럴 끝)
- `components/ExploreFrame.jsx` — 구버전 설정 기반 탐색 렌더러 (explore.items가 없을 때의 안전망 전용)
- `components/ExploreEditor.jsx` — 사용자 프로필 + 키워드 사전 편집기 (탐색 콘텐츠 편집은 빌더 "탐색" 탭으로 이동)
- `studio.css` — 스튜디오 전용 스타일 (`sb-` 접두사) + 원본 CSS 클래스의 절대배치 무력화 오버라이드. 실제 규칙은 `styles/` 15개 섹션 파일(마지막 `device.css`가 실행 화면 기기 프레임)에 있고 studio.css는 @import 배럴 — **@import 순서가 곧 캐스케이드**라 파일 간 이동·순서 변경 금지, 새 규칙은 맞는 섹션 파일 끝에 추가

## 기능 현황 (2026-07 기준 — 상세는 FEATURES.md)

1. **프로필별 워크스페이스**: 계정 = 프로필+탐색 페이지+시나리오+쓰레드. 홈 좌상단 프로필 컨트롤로 전환/추가/삭제
2. **홈**: 발행 칩(클릭 실행·드래그 순서·색상), 검색 매칭, 검색창 말줄임/멀티라인 옵션
3. **쓰레드 히스토리**: 햄버거 → 원본 룩 패널(버튼 위치 방향에서 등장), 체험 자동 기록, 이어보기
4. **빌더**: 순서 기반 세로 스택(드래그 = 삽입 인덱스 재정렬, 컨테이너 위 드롭 = 안에 배치), 다중 선택(⇧/⌘A), 우클릭 메뉴, ⌘C/X/V(단계 간), 줌, 팔레트 드래그 배치, 인라인 WYSIWYG 편집+서식 툴바(볼드/폰트/크기/색/키워드 밑줄), Undo/Redo, 기기 프리셋(프레임 폭), 발행/버전 복원(versionAt으로 현재 사용 중 버전 표시), 현재 시나리오 JSON 파일 입출력(상단 JSON 메뉴), 공유 링크
5. **컴포넌트 21종** (+탐색 7종 — 인사말·검색창·발행 칩·추천 검색어 칩·키워드 칩·스토리 카드 2종, 레이아웃 5종): 설문 5(헤더·질문·사진 업로드·날짜 입력·프로필 패널). **질문형 3종(질문·사진·날짜)은 화면 한 장이 곧 컴포넌트 하나** — `QuestionShell`이 진행 표시(n/전체) + 질문 문구 + 항목 + 「다음」 버튼을 한 벌로 그린다(`sb-survey-card`). 몇 번째 질문인지는 `questionPosition(ctx)`가 ctx.allItems에서 스스로 세므로 캔버스·플레이어가 같은 답을 낸다. **답을 고르기 전에는 「다음」이 잠긴다**(플레이어 한정 — 캔버스는 목업이라 항상 활성). 유일한 우회로는 날짜 질문의 「건너뛰기」(`skipLabel`)다. **화면 꽉 채우기(`fillScreen`, 기본 켜짐 — 값이 없으면 켜진 것)**: 실행 화면에서 질문 카드가 남은 높이를 다 먹고 「다음」이 화면 맨 아래에 붙는다(Figma Body fill + BottomBar — Player/LivePlayer 가 섹션에 `sb-player--fill` 을 달고, 화면 헤더가 있으면 하단 내비(이전 질문·홈으로)를 숨긴다. 헤더의 뒤로·홈이 같은 일을 하기 때문. 플로팅 버튼은 버튼 위로 올라가고 시트가 열리면 숨는다). 사진·날짜 질문은 **바텀 시트 동선**을 갖는다(`BottomSheet` — `.sb-player`가 z-index 스택 문맥을 만들어 시트가 하단 플로팅 바에 깔리므로 **기기 프레임 화면으로 포털**한다(`lib/deviceScreen.js` `overlayRoot` — 프레임이 없으면 body)): 사진은 드롭존(1px 점선 action/primary · 80px 연보라 원 카메라 · 안내+보조 문구 `hint`)→옵션 시트「카메라 바로 촬영/앨범에서 사진 선택(실제 file input)/샘플 얼굴 선택」(아이콘 행)→샘플은 **가운데 모달** 「모델 얼굴 선택」(`BottomSheet variant="center"`, `samplesTitle`/`samplesHint`) 2×2 그리드(고른 카드 보라 테두리)→고르면 드롭존이 200×200 둥근 미리보기 + `pickedTitle`/`pickedHint` 로 바뀐다(문구는 라이브 투영이 싣지 않으므로 undefined=기본 문구·''=숨김). **사진을 고른 뒤 「다음」을 누르면 얼굴 분석이 먼저 돈다**(2026-09-12, Figma "사진 분석 중 → 업로드 완료 / 얼굴 인식 실패 모달" — `PhotoQuestion`이 `QuestionShell`의 `onNext`를 가로챈다): 드롭존이 80px 원형 사진 + 「사진을 분석하고 있어요」(보라 점선, 「다음」 잠김)로 바뀌고, 계획 단계의 가상 메이크업이 쓰는 기기 안 얼굴 랜드마커(`lib/makeupComposite.js` `detectFace` — 모델을 미리 데우는 효과도 있다)가 얼굴을 찾으면 「사진이 등록되었어요」(초록 `--dk-green`) 뒤 다음 질문으로, 못 찾으면 가운데 모달 「얼굴이 잘 보이지 않아요 · 다시 시도」(답을 비우고 선택 시트를 다시 연다). 검출기를 못 쓰면(모델 로드 실패) 그냥 통과시키는 향상 계층이고, 표식만 남은 답(이어보기)은 분석 없이 넘어간다. Figma 의 카메라 뷰파인더·앨범 그리드 화면은 만들지 않았다(기기 카메라·파일 선택기가 대신한다). 샘플 얼굴 원본(`public/sample-faces/`, Figma 원본 시트에서 잘라낸 627px 4종 — `samples` 필드로 교체 가능. **해상도가 곧 가상 메이크업 입력 품질이다**: 옛 77px 썸네일로는 랜드마크가 잡을 결도, 정밀 렌더가 보존할 이목구비도 없었다), 날짜는 필드(달력 아이콘 · 고르면 보라 테두리 · "2025년 3월 12일 (수)" 요일까지)→달력 시트(제목 Gumi 18 · 월 이동 · 요일 행 일요일 빨강 `--dk-red` · 날짜 탭 = 임시 선택 보라 32px 원 · 「확인」(`confirmLabel`)이 답으로 굳힌다). `calendarMode: 'inline'` 이면 시트 대신 필드 아래 달력을 펼쳐 둔다(캔버스에서도 보이는 달력 컴포넌트 — 탭 즉시 답). 시트 껍데기(`BottomSheet`)는 Figma Sheet 한 벌(그립·제목 가운데·✕ 없음, 배경 탭·Esc 로 닫힘), 계획 12(요약(칩은 한 줄 가로 스크롤 — Figma ChipGrid)·타이틀·항목·상품 카드(Figma ProductCard 170 — 썸네일 1:1 radius 12 바닥 `--dk-thumb-2`(#F1F1F1) · 본문 머리줄은 몰 이름 10px 회색(썸네일 위 몰 배지는 Figma 에 없어 2026-09-12 뗐다, 브랜드는 같은 줄에 가운뎃점) · 담기 보라/담음 `--dk-brand-added`, 매칭율 배지는 badge/blue 알약 — 클릭 = 계산 기준 팝오버, 시나리오 카드는 인스펙터 `matchFactors` "라벨|점수|근거" 줄로 항목 표를 채운다. `soldOut` 토글 = Figma 5-4 품절: 썸네일 흐림 + 빨간 「품절」 표식, 배지 없음, 버튼 비활성)·영상/게시글 카드(Figma VideoCard — bg/subtle radius 12, 썸네일 2:1 위 + 출처 12 · 제목 14 아래. 폭과 무관하게 세로형, 옛 가로 모드는 뗐다)·성분 비교표(Figma 계획 4-1 — 머리 문구 → 두 제품 카드(왼쪽 비교(기존) 빨강 알약 · 오른쪽 추천 보라 알약, 이미지 `altImage`/`pickImage`) → 표 성분 | 기존 | 추천 | 위험도(행 4번째 셀, 있을 때만). 값 색은 뜻으로: 추천 열 보라, 기존 열 '있음·높음' 빨강 `--dk-danger`, 위험도 높음 빨강 · 중간 주황 `--dk-amber`. 데이터 문법 pick=추천·alt=비교는 그대로, 화면 순서만 기존→추천)·주의 성분(살구색 `--dk-amber-soft` 카드 · 주황 제목 · 항목마다 흰 카드)·비포애프터·도움 여부(Figma FeedbackSection — 배경 없는 섹션에 「👍 좋아요 / 👎 별로예요」 40px 알약, 플레이어에서는 위에 8px 두께의 bg/subtle 전폭 구분 띠)·체크리스트·담기 요약), 공통 4(**화면 헤더**(뒤로 · 화면 제목 · 홈 — `ctx.player.goBack/goHome`, 라이브 도구(평가·새로 생성)는 헤더가 아니라 프레임 밖 스테퍼 알약 `.sb-player-stepper__tools` 에 있다 — 2026-09-16, Figma TopBar 는 뒤로·제목·홈뿐이라 옛 헤더 아이콘을 뗐다)·텍스트 블록(`.sb-textblock` — 키커 12px·제목 16px 디스플레이·본문 14px)·안내 카드(`.sb-notice` — bg/subtle 8px 카드)·이미지). 계획 타이틀은 보라 밴드(`--dk-brand-deep`) 위 흰 인용 `“질의”`(20/32 가운데) **한 줄뿐이다**(2026-09-12 Figma 계획 2-2·3-1·4-1 대조 — 옛 기본값의 "…에 대한 / 딱 맞춤 계획입니다." 보조 줄과 밑줄 바는 Figma 에 없어 뗐다. 옛 저장분이 그 기본 문구(`LEGACY_PLAN_TITLE`)를 들고 있어도 안 그리고, 저자가 직접 적은 보조 제목만 아래 줄로 남긴다. AI 안내 ⓘ 점은 인용 끝에 붙는다). 답변 칩은 흰 radius 8 · 라벨 12 text/muted · 값 14/500 보라이고 질문 번호("1. ")는 뗀다 — 설문 요약 패널이 바로 앞이나 뒤에 오면 플레이어에서 스택 간격을 삼켜 한 밴드로 이어 붙는다(viewer.css 끝의 `:has` 인접 규칙, 캔버스는 둥근 카드 둘). **전폭 밴드의 블리드는 프레임 패딩 토큰 `--sb-phone-pad`(`.sb-phone`, 16px)만큼만** — 값을 직접 쓰면(옛 -24px) 프레임 밖으로 새어 나온다(2026-09 수정). 라이브 플레이어 프레임은 평가 레일과 가로 flex(`.sb-live-annotate`) 안에 있어 1100px 이하 column 모드에선 `margin: 0 auto` 로 가운데를 잡는다(안 잡으면 폭 고정 프레임이 왼쪽에 붙고 스테퍼·FAB 만 가운데 남는다). 계획 항목은 `stepPosition(ctx)`가 페이지 안 순서로 세는 단계 번호 원(24px 보라)을 제목 앞에 달고, 「중요」 배지는 코랄 알약(`--dk-coral`/`--dk-coral-soft`)이다. 단계 사이 구분선(1px border/subtle + 24px)은 플레이어에서 프레임 패딩만큼 밖으로 펴 전폭 선이다(캔버스는 카드 안쪽 선). **룩의 기준은 Figma "[PP1K] EEVEE 공유용"의 Search·설문·계획 섹션**(2026-09, 파일 키 `iswri6cxsr0rsRlfAeDAmG` — 뷰어 권한만 있어 Figma MCP는 "편집 권한 필요"로 막히고, Chrome 로그인 세션에서 레이어를 골라 속성 패널을 읽었다. **프레임 전체는 Figma 「내보내기」(뷰어도 가능)로 PNG 1x 를 받아 보는 편이 훨씬 빠르고 정확하다** — 2026-09-12 계획 섹션 전 프레임을 이 방법으로 대조했다) — 색·간격은 `--dk-*` 토큰(base.css)에 Figma 변수 이름을 주석으로 달아 옮겼고(브랜드 보라 `--dk-brand`(#7950F2)·`--dk-brand-2`·`--dk-brand-deep`·`--dk-brand-soft`, 옛 이름은 값만 바뀜), 설문 선택지는 전폭 리스트형(1.5px `--dk-line` 테두리·radius 8·선택 시 연보라 면+보라 테두리)이 기본, 「다음」은 52px 보라(radius 8, 비활성 `--dk-line`/`--dk-disabled`)다. 홈 검색창은 60px 알약(2px 보라 테두리·14px 플레이스홀더·원형 면 없는 아이콘 버튼), 홈 칩은 44px 알약(테두리 없음, 라벨 12px, 기본색 바이올렛)이다
6. **플레이어**: 설문→계획 스테퍼, 프로필 배지 제외, 설문 요약, 담기/완료, 쓰레드 기록, 상품 상세보기 사이드 패널(외부몰 iframe — 모바일 전체화면)
7. **공유**: URL 해시 링크(즉시 체험, 가져오기), JSON 백업/이관
8. **AI 기능** (전부 API 키 없이 프롬프트 왕복): 시나리오 만들기 / 조합 케이스(설문 조합별 일괄 생성) / **문구 다듬기**(필드 단위) / **페이지 재구성**(케이스 통째). 평가 탭 진입점은 `⇄ AI에게 수정 요청` 하나 — `AiFixChooser`가 문구 다듬기·페이지 재구성으로 갈라 주고, 문구 다듬기에서 처리 못 한 피드백은 재구성으로 이어진다(onSwitchToRevise). **평가 탭의 목적은 전파다**: 사람이 73개 케이스를 다 볼 수 없으니 라운드당 3개(CASE A/B/C)씩 평가하고, 그 피드백을 전체에 퍼뜨린다. **선정은 로테이션**: "다음 3개 선정"은 평가 흔적(별점·피드백)이 있는 케이스를 후보에서 빼고 미평가에서 뽑는다(`recommendRotationCaseIds`, 모자라면 평가된 케이스로 채움) — 이미 본 케이스를 다시 뽑으면 정보 이득이 없기 때문. 쌓인 평가는 **케이스 리더보드**(`evaluationLeaderboard`, 평균 별점 내림차순)에 남고, 이 순위가 전파 씨앗의 우선순위가 된다. 문구·톤은 `lib/prompt/propagation.js`("피드백 전체 반영": 씨앗 = 평가한 모든 케이스의 피드백+현재 값 — 리더보드 순 정렬, 다이얼로그에서 상위 N개 케이스로 제한 가능(caseLimit), 반영 완료(resolved)면 exemplarTrusted로 구분 전달 — 를 모범 예시 삼아 미평가 케이스를 배치 왕복. 대상은 씨앗 제한과 무관하게 평가 흔적 있는 케이스 전부 제외 — 사람 손을 탄 케이스를 배치 수정이 덮지 않는다. revision과 같은 허용 목록·before 대조·개별 선택 적용), 구조 변경은 "페이지 재구성으로 대표를 고침 → 조합 케이스 재생성의 **교체 모드**(같은 comboSignature의 기존 케이스 대체, applyGeneratedCases replace 옵션)"가 맡는다. **평가는 주석(annotation) 방식**: 왼쪽에 케이스 페이지를 실제 렌더하고 오른쪽 레일에 컴포넌트별 말풍선(별점+코멘트). 말풍선 top은 앵커(최상위 아이템)의 실제 렌더 높이로 배치하고 겹치면 아래로 민다 — 컨테이너 자식은 부모에 앵커. 배치는 매 렌더 동기 실행(rAF만 쓰면 연속 렌더에서 계속 취소돼 한 번도 실행 안 될 수 있음). 점수는 별점 UI지만 데이터는 기존 0~5 그대로: 별 1~5 클릭=점수, **같은 별 재클릭=별 다 끈 0점**, 옆 배지가 비(非)별 상태를 표시(미평가=회색·0점=빨강)하고 누르면 미평가로 초기화. 빈 별만으로는 0점과 미평가가 안 갈리므로 배지가 그 구분을 담당한다. **피드백 입력은 평가 페이지 한 곳**: 컴포넌트 말풍선 + 케이스 전체 말풍선(`evaluation.review.score`/`feedback`, nullable) — 케이스 코멘트는 재구성 요청에 지시로, 문구 다듬기에는 맥락(caseNote, 구조 요청 무시 규칙)으로 자동 포함된다. **평가 스키마는 v2**(`evaluation/model.js`): `selection`(작업 상태 — active/slot/round/at) · `review`(케이스 전체) · `components`(itemId 키) 3분리, 모든 평가 레코드에 `{round, at}`(선정 시각과 평가 시각 분리, round 0=구버전). v1 평면 스키마와 AI 가져오기의 `{selected, slot}`은 normalize 관문에서 자동 마이그레이션(criteria 폐기). **고아 레코드 규칙**: 판정(`caseHasEvaluationInput`)·리더보드 집계는 `liveComponentEntries`로 실제 아이템과 교차 검증한다 — 삭제·재구성으로 사라진 컴포넌트의 평가가 케이스를 로테이션에서 영구 제외시키거나 평균을 부풀리지 않게 (데이터는 지우지 않음 — 같은 id가 돌아오면 되살아난다)

## 디자인 시스템 (studio.css)

`:root`의 토큰이 스튜디오 UI의 유일한 값 출처다. 규칙 안에 색·반경·그림자를 직접 쓰지 말 것.

- **폰트**: 스튜디오 크롬은 메인(clean-home)과 동일한 스택(`--sb-font-sans`)을 직접 선언한다. **본문 웹폰트는 절대 로드하지 말 것** — 스택 1순위가 Pretendard지만 원본 프로토타입부터 로드 없이 시스템 폴백(맥 Apple SD Gothic Neo **Light 300**)으로 렌더돼 왔고, 그 얇은 시스템 폰트 룩이 DDAK 메인의 인상이다. Pretendard CDN을 추가하면 메인 페이지가 통째로 다르게 보인다(실제 사고 이력 있음). 리치텍스트 인라인 서식 폰트는 별개 토큰 `--sb-font`(FONT_OPTIONS 7종, Google Fonts 로드). 프롬프트·JSON 영역만 ui-monospace 예외. **유일한 예외 — 디스플레이 서체 낭만있구미체(Gumi Romance)**: Figma "[PP1K] EEVEE 공유용"이 제목·질문 라벨·선택지 제목·버튼 라벨·칩·인용 제목·상품명에 쓰는 "Gumi Romance TTF"를 `public/fonts/GumiRomance.woff2`(구미시 무료 서체, OTF→woff2 405KB, 단일 굵기 400, 라이선스는 같은 폴더 txt)로 same-origin 로드해 `--dk-font-display` 토큰으로만 쓴다. 적용 자리는 viewer.css 끝 "디스플레이 서체 적용" 블록 한 곳(원본 `.clean-home-page *` 가 font-family를 !important로 박아 같은 힘 + `X *`로 덮는다) — Pretendard 자리(설명·부제·진행 라벨·요약 칩·몰 이름·단계 번호)와 스튜디오 크롬·원본 프로토타입 영역에는 싣지 않는다. 단일 굵기라 이 서체를 쓰는 요소는 400으로 둔다(500이면 가짜 볼드)
- **굵기**: 메인 밴드(본문 300 · 카드 제목 340~360 · 칩 500)에 맞춘 3단계만 — 본문 **300** / 강조 **400** / 강한 강조 **500**. 600 이상 금지. **함정**: 원본 CSS의 `.clean-home-page *`가 weight 300을 모든 요소에 *직격*하므로(상속 아님) 버튼에 준 굵기가 안쪽 span에서 300으로 풀린다 → 스튜디오 루트들 아래 `* { font-weight: inherit }`로 상속을 재정립해 두었다(제거 금지). strong/b/h1~h4 기본은 400. 예외는 리치텍스트 사용자 볼드(`.sb-rich-b`/`.sb-style-bold`, 700 유지)뿐
- **색**: 중립 11단계(`--sb-n-0`~`--sb-n-900`) + 텍스트 4단계(`--sb-ink`/`--sb-ink-2`/`--sb-muted`/`--sb-subtle`) + 면 3단계 + 역할색(`--sb-accent` 브랜드, `--sb-info` 평가·AI 왕복, `--sb-success`/`--sb-warn`/`--sb-danger`, `--sb-ai` AI 트리거)
- **--sb-qa-\*** 는 구 이름의 별칭일 뿐이다. 새 코드는 `--sb-info` 등을 쓸 것
- **라운드** `--sb-r-xs`~`--sb-r-2xl`/`--sb-r-pill`, **그림자** `--sb-shadow-sm`~`xl`, **타이포** `--sb-t-micro`(11px)~`--sb-t-title`(16px) 5단계 — 크롬 폰트 크기는 리터럴 대신 `--sb-t-*` 토큰만 사용(캔버스 콘텐츠 컴포넌트의 목업 크기만 리터럴 허용), **모션** `--sb-dur`/`--sb-ease`. 다이얼로그 본문 블록의 세로 리듬은 `margin: 12px 24px` 기준
- **라벨 언어**: 스튜디오 크롬에 영문 아이브로우("영어 타이틀 + 한글 서브타이틀" 스택 — 예: 옛 `SCORING RUBRIC`/`STEP 1`) 금지. 패널·섹션 제목은 한글만, 왕복 단계 번호는 "n단계 · 스튜디오에서" 형식. 예외: `CASE A/B/C` 같은 식별자와 원본 프로토타입 CSS 영역(`keyword-detail-card` 등)
- **포커스**: 개별 규칙에 `outline: none`을 쓰지 말 것. 전역 `[class^='sb-']:focus-visible` 규칙이 키보드 포커스 링을 담당한다
- **버튼**: `.sb-btn` + 성격(`--primary`/`--ghost`/`--danger`/`--ai`/`--open`) + 크기(`--small`/`--tiny`). hover·active·disabled는 기본 정의에만 있다. AI를 부르는 버튼은 전부 `--ai`
- **AI 왕복 표기 규칙**: AI 기능은 "누르면 만들어진다"고 약속하지 않는다. 트리거는 `✦`(생성)가 아니라 **`⇄`(왕복)** 를 쓰고 라벨에 "프롬프트"를 넣는다. 다이얼로그는 머리말에 `AiRoundTripNote`를 두고, `PromptExchange`가 1 복사 → 2 **스튜디오 밖에서** 붙여넣기 → 3 결과 가져오기를 항상 펼쳐 보인다. 번호는 이 왕복(`.sb-handoff`)에만 쓰고 다이얼로그 단계(`.sb-steps`)는 번호 없는 breadcrumb이다. 성격이 비슷한 AI 기능을 나란히 버튼으로 두지 말 것 — 진입점을 하나로 합치고 선택 카드(`AiFixChooser` 참고)에서 "무엇을 고치고 싶은지" 예시 문장으로 가른다. **`✦`(생성)는 라이브 생성 체험 전용이다** — 진짜로 눌러서 만들어지는 곳(LivePlayer 배지·선택 시트·쓰레드 라이브 배지)에만 쓰고, 시나리오 칩·스튜디오 왕복 기능에는 쓰지 않는다 (시나리오 칩의 옛 스파크는 이 이유로 뗐다)

## 핵심 설계 결정

- **실행 화면은 실기기 프레임 안에서 돈다** (2026-09): 홈·체험·SRP는 `DeviceFrame`이 고른 기기의 껍데기·화면 크기(w×h)로 그리고, 스크롤·fixed 오버레이·세이프 에어리어가 전부 그 화면 기준이다. 빌더 캔버스는 여전히 폭만 쓴다(문서 흐름 스택). 상세는 위 `components/DeviceFrame.jsx` 항목
- **탐색은 공통 페이지** (시나리오 소유 아님, 계정 소유). 칩 클릭 = 탐색 완료 → 플레이어는 설문부터 시작. **탐색 콘텐츠도 아이템 기반**: `explore.items[]`를 빌더의 "탐색" 탭에서 설문/계획처럼 스택 편집(자동 저장·즉시 홈 반영). 구버전 설정(greeting/stories 등)은 `exploreItemsFrom()`으로 최초 1회 아이템 변환. 발행 칩은 `scenarioChips` 컴포넌트 자리에 렌더
- **프로필/설문 요약 패널은 일반 컴포넌트** (`profilePanel`, `surveySummary`) — 고정 아님, 드래그로 순서 배치. 둘은 같은 톤이다(보라 `--dk-brand-deep` 카드 + 흰 라벨/값 칩 한 줄 가로 스크롤, viewer.css `.sb-profile-panel`/`.sb-summary`) — 프로필 칩만 토글 표식(✓/+)과 제외 상태(반투명·취소선)가 있다. **설문 요약 패널은 제목 라벨 없이 칩 줄만 그린다**(2026-09 — Figma AIIntro 처럼 인용 제목 밴드 바로 아래 답변 칩이 이어진다. 구 데이터·라이브 투영의 `title` 은 무시). 노출 항목은 인스펙터 칩 관리(`ChipManagers.jsx`)로 조절: profilePanel은 props.hidden(캔버스 배지 클릭과 동일)+계정 프로필 항목 편집(문구·추가·순서는 계정 공통), surveySummary는 props.hiddenProfile(라벨 매칭)·hiddenQuestions(질문 문구 매칭, 줄바꿈 직렬화). hidden 계열은 AI 제외 목록(NON_LLM_EDITABLE/FACT/NON_GENERATED) 포함
- **아이템 모델은 순서 기반 스택**: `{ id, type, props, hidden?, locked?, parentId?, slot?, w?, h? }` — **배열 순서 = 최상위 렌더 순서**, 좌표(x/y)와 최상위 크기(w/h)는 없다(전폭·자동 높이). w/h는 **컨테이너 자식 전용**(카드의 콘텐츠 크기 — 예: hscroll 상품 카드 232px). 이 모델은 BFF 와이어 형식(순서 있는 의미 목록)과 정합을 위해 좌표 자유 배치에서 전환한 것(2026-08). **구 좌표 데이터는 `normalizeItems`가 이관**: 최상위에 숫자 x/y가 보이면 y→x 순으로 재배열해 배열 순서로 굳히고 좌표 필드를 버린다(멱등). 이 관문을 지나는 경로 = localStorage 로드·서버 행 채택·가져오기·공유 링크·스타터 설치·버전 복원(`scenarioFromSnapshot`) — 스냅샷·서버 행 원본은 그대로 두고 읽을 때마다 이관되는 lazy 방식이라 별도 일괄 마이그레이션이 없다. 캔버스 드래그는 `useStackDrag`: 드래그 아이템은 제자리에서 흐려지고(삽입 라인이 위치 미리보기), 드롭 시 `reorderTop` 한 번으로 커밋. 판정은 전부 렌더된 DOM rect 기준(`geometry.js`)
- **컨테이너(레이아웃) 컴포넌트**: 가로/세로 스크롤·그리드·캐러셀은 `container: true` — 다른 컴포넌트를 자식으로 수용. 자식은 같은 스테이지 배열에 `parentId + slot`으로 저장(플랫 유지), 스택 순서는 최상위만 본다. 팔레트/캔버스 드래그를 컨테이너 중앙부(상/하 `NEST_EDGE_ZONE` 18px 밴드 제외)에 놓으면 중첩, 꺼내기는 인스펙터·레이어 패널·컨테이너 박스 밖으로 드래그. 삭제·복제·복사는 자식 연쇄. 레이아웃끼리 중첩 불가. 팔레트는 `category`(content/layout)로 그룹 표시. **기기 폭 전환은 프레임 폭만 바뀐다** — 최상위는 전폭이라 환산이 없고, 자식 카드 크기는 그대로 유지된다. 좁은 기기에서 넘치는 문제는 슬롯 CSS가 맡는다: 가로 스크롤(`hscroll`)은 카드 크기를 유지해 스크롤로 흡수하고, 교차 축(`vscroll`·`gridPanel`)과 `carousel`은 `max-width: 100%`로 눌러 담는다. **주의: 아이템 id 재발급 시 parentId 매핑 필수** (`lib/scenarioOps.js` 참고 — parentId·계획 조건 questionId·평가 기록 키 세 곳을 함께 다시 매단다)
- **원본 룩 유지**: `public/`에 원본 CSS(gmarket-advanced*.css) 복사본 + Tailwind CDN. 원본 클래스 그대로 재사용
- **프로필별 워크스페이스(계정)**: `ddak-accounts-v1`에 `{accounts[], activeId}` — 계정 = 프로필+탐색 페이지+시나리오+쓰레드 묶음. 프로필 전환은 홈 드로어. 구 단일 키(`ddak-scenarios-v1`, `ddak-explore-page-v1`, `ddak-profile-v1`, `ddak-threads-v1`)는 최초 1회 첫 계정으로 마이그레이션 (발행은 브라우저 로컬 한정)
- **컴포넌트 텍스트는 kText()로 렌더**: 인스펙터 서식 툴바가 모든 텍스트 필드에 `{{서식|텍스트}}` 마크업을 넣을 수 있으므로, 레지스트리에서 사용자 노출 텍스트 prop은 반드시 `kText(p.x, ctx, 'x')`로 감쌀 것 (안 그러면 마크업 원문이 그대로 노출됨)

## 주의사항

- 원본 CSS가 `header` 태그를 전역 숨김 → 스튜디오 UI에 `<header>` 쓰지 말 것
- 실행 화면(홈·체험·SRP) 코드에서 `window.scrollTo`/`window.scrollY`·`100vh`·`vw` 를 쓰지 말 것 — 기기 프레임 안에서는 화면이 곧 뷰포트다. 스크롤은 `scrollScreenTo`/`screenScrollY`(lib/deviceScreen.js), 높이는 `var(--sb-vh, 100dvh)`, fixed 요소 크기는 `%`
- pointerup 커밋은 `setTimeout(0)`으로 미룸 (React 렌더 중 setState 경고 방지)
- 드로어의 발행 버튼 텍스트 매칭 시 "발행 취소"와 "발행하기" 구분 필요
- 히스토리 스냅샷은 `{stages, planCases, device, exploreItems}` JSON (Builder가 `useBuilderHistory`에 주입) — 시나리오 필드 추가 시 undo 포함 여부 검토
- 아이템 목록을 바꾸는 함수는 항상 **업데이터 함수**를 넘길 것. 드래그 커밋이 `setTimeout`으로 미뤄지므로 값으로 덮으면 낡은 클로저가 최신 상태를 지운다
- 최상위 순서를 바꾸는 모든 경로는 `geometry.reorderTop()`을 통과시킬 것 — index는 "이동 아이템을 뺀 나머지" 기준이고, 자식은 배열 끝으로 통과한다
- 아이템에 x/y/w/h를 새로 넣지 말 것 (최상위 기준). 좌표가 들어가면 `normalizeItems`가 구형 데이터로 판정해 재정렬한다 — 자식 카드 크기만 w/h 허용
- 캔버스 줌은 `transform: scale`이 아니라 CSS `zoom` — 문서 흐름 스택이라 확대·축소가 레이아웃 크기(스크롤 범위)에 반영돼야 한다
