{
  "name": "@ddak/schema",
  "version": "0.1.0",
  "private": true,
  "description": "DDAK 공유 계약 — 쓰레드 도메인 + internal API zod 스키마 (FE·BFF·core 공용)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
