# DDAK Scenario Studio — 기능 명세

지마켓 뷰티 AI 쇼핑 컨셉의 **시나리오 목업 제작·발행·체험 도구**.
"공통 탐색(홈) → 칩 클릭 → 설문 → 계획" 플로우를 노코드로 편집하고, 발행하면 홈에 칩으로 노출되어 실제처럼 체험할 수 있다.

- 라이브: `https://dlwlsgkdn.github.io/gmkt-intent-home/docs/`
- 데이터는 전부 브라우저 localStorage (서버 없음). 공유는 JSON 파일 또는 URL 링크로.

---

## 0. 사용자 프로필별 워크스페이스 (계정)

**계정 = 프로필 + 탐색(DDAK) 페이지 + 시나리오 + 쇼핑 쓰레드 묶음.** 프로필을 전환하면 네 가지가 통째로 바뀐다.

| 기능 | 설명 |
|---|---|
| 프로필 컨트롤 | 홈 **좌상단, 기기 폭 컨트롤 옆** 필 버튼(아바타 이니셜+이름) → 드롭다운 |
| 전환 | 드롭다운에서 프로필 클릭 — 행마다 시나리오·쓰레드 개수 표시, 사용 중 표시 |
| 추가 | "+ 새 프로필" → 이름 입력 → 기본 탐색 페이지(인사말에 이름 반영)·빈 시나리오로 시작 |
| 삭제 | 행의 ✕ (확인 후) — 마지막 1개는 삭제 불가 |
| 저장 | `ddak-accounts-v1` = `{accounts[], activeId}`. 구 단일 키는 최초 1회 첫 계정으로 자동 마이그레이션 |

## 1. 홈 (공통 탐색 페이지)

원본 `gmarket-advanced-clean-home` 프레임을 그대로 유지한 화면. 모든 시나리오의 시작점. **프로필(계정)마다 별도.**

| 기능 | 설명 |
|---|---|
| 발행 칩 노출 | 발행된 시나리오만 검색창 아래 `✦#칩라벨` 칩으로 표시 |
| 칩 클릭 = 실행 | 칩 클릭이 곧 "탐색 완료" — 플레이어가 설문 단계부터 시작 |
| 칩 드래그 순서 변경 | 칩을 끌어 노출 순서 조정 (6px 이하 움직임은 클릭으로 처리) |
| 칩 색상 | 시나리오별 지정 색상으로 글자·테두리·배경 틴트 표시 |
| 검색 실행 | 검색어로 시나리오 매칭 (공백/언더스코어/대소문자 무시) → 실행 |
| 검색창 긴 텍스트 | 한 줄+말줄임(기본, input 렌더) 또는 여러 줄 자동 확장(textarea, `field-sizing`) — 탐색 편집기 옵션 |
| 플로팅 바 | 하단: 홈 / 마이(목업) / **쇼핑 쓰레드 히스토리** · 우상단: 시나리오 스튜디오 진입 |
| 좌상단 컨트롤 | 기기 폭 프리셋(전역 `ddak-viewer-device-v1`, 기본 iPhone 15 390px) + **프로필 전환** |

## 2. 쇼핑 쓰레드 히스토리 패널

하단 플로팅 바의 햄버거 버튼으로 여닫는 패널. **원본 clean-home history-sidebar 룩** 재사용. 홈·플레이어 어디서든 열림.

| 기능 | 설명 |
|---|---|
| 쓰레드 = 체험 1회 | 칩 클릭으로 시나리오를 시작하면 자동 기록 — 단계 이동·담기·완료 때마다 갱신 |
| 등장 방향 | 여는 버튼이 그룹 안에서 왼쪽/가운데/오른쪽 어디냐에 따라 그 방향에서 등장 (좌/우 사이드, 중앙 바텀시트) |
| 아코디언 카드 | #칩 라벨(색), 경과 시간, "마지막 페이즈: 설문 작성 중/계획 확인 중/체험 완료", 담은 상품 목록 |
| 쓰레드 이동 | 카드의 "쓰레드 이동" → 해당 시나리오 재실행 (삭제된 시나리오면 안내) |
| 관리 | 카드별 삭제, 전체 지우기, "+" = 홈으로 이동해 새 쓰레드 시작 유도 |
| 저장 | 계정별 `threads` (최근 30개) |

## 3. 시나리오 드로어 (스튜디오 입구)

우상단 "시나리오 스튜디오" 버튼으로 열림.

| 기능 | 설명 |
|---|---|
| 탐색 페이지 편집 진입 | 모든 시나리오가 공유하는 공통 홈 편집기로 이동 |
| 템플릿 갤러리 | 새로 만들기: **빈 시나리오 / 뷰티 브리프 / 선물 추천 플로우** (설문·계획 프리필) |
| JSON 내보내기/가져오기 | 전체 시나리오를 파일로 백업·공유, 가져오면 새 ID로 병합 |
| 시나리오 목록 | 상태 배지(발행됨/작성 중), 칩 미리보기(색상), **시험·편집·복제·삭제** |
| 시나리오 복제 | 내부 컴포넌트 ID까지 재발급된 완전한 사본 (작성 중 상태) |

## 4. 탐색 페이지 편집 (빌더 "탐색" 탭) + 프로필·키워드 편집기

**탐색(홈) 콘텐츠는 설문/계획과 동일한 캔버스 편집**: 빌더의 "탐색" 탭에서 컴포넌트를 자유 배치·편집한다 (`explore.items[]`, 계정 소유·자동 저장·모든 시나리오 홈에 즉시 반영). 발행 칩은 "발행 칩 목록" 컴포넌트 자리에 자동 표시. 구버전 설정(인사말/검색창/스토리)은 최초 1회 아이템으로 자동 변환.

별도의 **프로필 · 키워드 사전 편집기**(드로어에서 진입):

| 기능 | 설명 |
|---|---|
| 사용자 프로필 관리 | 고정 설문 정보(이름 + 라벨·값 항목) 추가/수정/삭제 — 프로필 요약 패널에 사용 |
| 키워드 사전 | `[[키워드]]` 점선 밑줄 클릭 시 뜨는 설명 모달의 사전(단어/설명/포인트) — 전역 `ddak-keywords-v1` |

## 5. 빌더 (시나리오 편집기)

### 5-1. 상단 바 (3행 헤더 — 단계 탭 포함)

- **1행**: 문서 정보(뒤로·제목·칩·색·상태·자동 저장) + 발행 관련(시험해보기/버전/발행)
- **2행**: 편집 도구 그룹 — 히스토리(언두/리두) | 캔버스 줌 | 레이아웃(기기·컴팩트·자동 정렬) | 공유 — 구분선으로 분리
- **3행**: 단계 탭 — 🧭 탐색(공통 캔버스, 직접 편집·즉시 홈 반영) | 1 설문 / 2 계획 (모두 컴포넌트 수 표시)

### 5-2. 상단 바 상세
| 기능 | 설명 |
|---|---|
| 제목·칩 라벨 편집 | 인라인 입력 (칩 공백은 자동 `_` 변환) |
| 칩 색상 피커 | 6색 프리셋 (세이지/로즈/블루/앰버/플럼/슬레이트) |
| Undo / Redo | ⌘Z / ⇧⌘Z, 60단계, 연속 변경 500ms 병합, 기기 전환도 복원 |
| **🧲 컴팩트 방향** | compactType 컨벤션 — **세로**(기본, 위로 스택) / **가로**(같은 줄에서 왼쪽으로, 줄이 차면 세로 겹침만 해소) / **끄기**(자유 배치). 배치가 바뀔 때마다 적용 + 드래그 중 실시간 미리보기. 시나리오별 저장 |
| **캔버스 줌** | − / % / + 컨트롤 + ⌘+/⌘-/⌘0 (50~150%, 드래그·리사이즈·선택 좌표 자동 보정) |
| **✏️ 편집 / 👁 미리보기 모드** | Figma clip-무시 + Webflow 미리보기 토글 패턴. 편집(기본): 컨테이너 경계를 반투명 점선으로 표시하고 클리핑 해제 — 가로 패널은 줄바꿈 전개, 세로 패널은 높이 제한 해제(+실제 높이 점선 마커), 캐러셀은 슬라이드 세로 전개 → 자식이 가려지지 않음. 미리보기: 스크롤·스냅·클리핑 등 실사용 모습 그대로 + 편집 크롬(태그·핸들·자식 셸) 숨김 |
| 공유 링크 복사 🔗 | 시나리오 전체를 URL 해시에 담은 링크 — 받는 사람은 링크만 열면 즉시 체험 |
| 기기 프리셋 | 데스크톱 720 / 갤럭시 360 / iPhone SE 375 / iPhone 15 390 / Pro Max 430 / 태블릿 768 — 전환 시 비례 스케일 + 겹침 보정 |
| 자동 정렬 3모드 | 1단 세로 / 2단 그리드(마소너리) / 위로 컴팩트(빈 공간 제거) |
| 시험해보기 / 버전 N / 발행 | 발행 시점 스냅샷(최근 10개) 복원, 빈 단계 경고, 빈 칩 라벨 자동 생성 |

### 5-3. 팔레트 (왼쪽)
| 기능 | 설명 |
|---|---|
| 컴포넌트 탭 | 단계별 컴포넌트 카드 + 검색. **클릭 = 맨 아래 추가, 캔버스로 드래그 = 원하는 위치에 배치** |
| 레이어 탭 | 배치 순서 목록: 선택(⇧ 다중), 🔒 잠금, 🚫 실행 시 숨김, ↑↓ 순서 변경, ✕ 삭제 |
| 단축키 안내 | 치트시트 상시 표시 |

### 5-4. 캔버스 (가운데)
| 기능 | 설명 |
|---|---|
| 자유 드래그 배치 | 기기 폭 프레임 안에서 자유 이동, 실시간 겹침 회피(밀려남) |
| 스마트 스냅 가이드 | 캔버스 가장자리·중앙, 다른 컴포넌트 모서리·표준 간격(14px)에 자석 스냅 + 가이드라인 |
| **러버밴드 다중 선택** | 빈 캔버스 드래그 = 선택 박스, 걸치는 컴포넌트 모두 선택 (⇧: 기존 선택에 추가) |
| **우클릭 컨텍스트 메뉴** | 컴포넌트: 복제/복사/붙여넣기/잠금/숨김/삭제 (다중 선택에 일괄 적용). 빈 곳: 여기에 붙여넣기/전체 선택 |
| 리사이즈 핸들 | 우하단 모서리 드래그로 너비·높이 조절 |
| 더블클릭 인라인 편집 | 컴포넌트 안에서 바로 편집(WYSIWYG) — 드래그 선택 시 서식 툴바, 즉시 미리보기 |
| 캔버스 내 편집 | 프로필 요약 패널의 배지를 캔버스에서 직접 클릭해 노출 토글 |
| **컨테이너 내부 전체 보기** | 컨테이너나 자식을 선택하면 캔버스 위에 내부 컴포넌트 전용 목록 표시. 번호·종류·대표 문구·크기를 한눈에 확인하고 선택/순서 변경/잠금/숨김/복제/꺼내기/삭제 가능, 6개 이상이면 검색 제공. 자식을 선택해도 목록 유지 |
| 미리보기 편집 잠금 | 최상위·컨테이너 자식 드래그, 선택, 리사이즈, 드롭, 인라인·인스펙터·팔레트·단축키 편집을 차단하되 스크롤·캐러셀의 마우스 드래그는 허용 |

### 5-5. 인스펙터 (오른쪽)
| 기능 | 설명 |
|---|---|
| 속성 편집 | 컴포넌트별 필드 (텍스트/멀티라인/토글) 실시간 반영 |
| 선택 서식 툴바 | 문구 드래그 선택 → 볼드/**폰트 7종**(나눔명조·고운바탕·송명·주아·나눔손글씨·헤드라인·모노, Google Fonts)/크기/색(6색)/**밑줄(키워드 모달 연결)**/서식 지우기 — `{{서식\|텍스트}}` 마크업으로 저장 |
| 키워드 자동 등록 | '밑줄' 적용 시 사전에 없는 단어 자동 등록 |
| 크기·상태 | 너비/높이 슬라이더("자동 높이" 복귀), 잠금/숨김, 복제/삭제 |
| 다중 선택 도구 | 정렬(왼쪽/가운데/오른쪽/세로 간격 균등) + 모두 복제/모두 삭제 |

### 5-6. 키보드 단축키
| 키 | 동작 |
|---|---|
| ⌘Z / ⇧⌘Z | 실행 취소 / 다시 실행 |
| ⌘A | 현재 단계 전체 선택 |
| ⌘C / ⌘X / ⌘V | 복사 / 잘라내기 / 붙여넣기 (**단계 간 이동 가능**) |
| ⌘D | 선택 복제 |
| ⌘+ / ⌘- / ⌘0 | 캔버스 확대 / 축소 / 100% |
| Delete/Backspace | 선택 삭제 |
| 방향키 / ⇧+방향키 | 8px / 1px 이동 (다중 선택 지원) |
| Esc | 선택 해제 (컨텍스트 메뉴 닫기) |
| ⇧+클릭 / 빈 곳 드래그 | 다중 선택 |
| 더블클릭 | 인라인 편집 / 인스펙터 포커스 |

## 6. 플레이어 (시나리오 체험)

| 기능 | 설명 |
|---|---|
| 설문→계획 스테퍼 | 상단 플로팅 스테퍼로 단계 이동, "#칩으로 진입 · 탐색 완료" 표시 |
| 기기 폭 반영 | 전역 뷰어 기기 폭의 모바일 프레임 — 좌상단 컨트롤로 즉시 전환 |
| 설문 응답 | 진행률 바, 단일/복수 선택 카드, 프로필 배지 클릭으로 회차별 제외 |
| 설문 요약 반영 | 계획 단계 요약 패널에 프로필(제외 반영) + 각 질문의 답(미응답 = "아무거나") |
| 쓰레드 기록 | 체험 시작·단계 이동·담기·완료가 쓰레드 히스토리에 자동 기록 |
| 숨김 제외 / ↺ 처음부터 / 담기·완료 | 빌더에서 숨긴 컴포넌트 제외, 초기화, 결제 CTA → 완료 → 홈 |
| 이 시나리오 편집 | 우상단 버튼으로 빌더 직행 |

## 7. 공유 링크 모드

- 빌더의 🔗 버튼으로 복사한 `#s=<인코딩>` URL을 열면 **저장 없이 즉시 플레이어 실행**
- "이 시나리오 편집" 클릭 시 내 스튜디오로 가져와(새 ID, 작성 중) 편집 시작

## 8. 컴포넌트 카탈로그

탐색 6종 + 설문 3종 + 계획 8종 + 공통 8종. 팔레트는 **콘텐츠 / 레이아웃** 두 그룹으로 구분. 탐색 아이템은 시나리오 기기 폭과 무관하게 저장되므로, 캔버스보다 넓으면 진입/기기 전환 시 폭에 맞게 자동 보정.

**컨테이너(레이아웃) 컴포넌트** — 가로/세로 스크롤 패널·그리드·캐러셀은 다른 컴포넌트를 안에 배치할 수 있다:
팔레트 카드나 캔버스 컴포넌트를 컨테이너 위로 드래그하면 하이라이트 후 **포인터 위치의 슬롯**에 들어감.
컨테이너를 선택한 뒤 팔레트 컴포넌트를 클릭하면 컨테이너 끝에, 자식을 선택한 뒤 클릭하면 바로 다음 슬롯에 삽입.
**안의 자식도 캔버스 컴포넌트처럼 직접 조작** — 클릭 선택, 더블클릭 인라인 편집/인스펙터,
우하단 리사이즈 핸들·인스펙터 슬라이더로 크기 조절(자식 고유 크기 유지, 슬롯이 강제 축소하지 않음),
드래그 시 컨테이너 안에서는 원래 구조를 유지한 채 **삽입 캐럿+포인터 고스트로 위치를 미리보기**하고 놓을 때 한 번만 순서를 변경.
컨테이너의 제목·패딩·슬롯 사이 여백은 내부로 판정하며, 부모 전체 경계를 명확히 벗어나면 자동으로 꺼내짐.
드래그로 컨테이너에 넣을 때는 대상 레이아웃 이름과 삽입 위치 가이드 라인 표시. 기본은 비어 있고(드롭존 표시, 실행 화면에선 숨김)
텍스트 카드 목록(`제목|설명|이미지URL`)은 선택 사항. 레이어 패널에 자식이 ↳ 들여쓰기로 표시되고
⠿ 드래그로 부모/슬롯 순서를 정밀 변경할 수 있으며, ↑↓·꺼내기(⤴)·잠금/숨김도 지원. 인스펙터에도 "레이아웃에서 꺼내기" 제공.
컨테이너나 자식을 선택하면 **편집 전용 내부 전체 보기**가 캔버스 위에 고정되어 자식 번호·유형·요약·크기와 관리 액션을 제공하고, 6개 이상은 검색할 수 있다. 캔버스 안 자식에도 같은 번호·유형 배지를 표시해 목록과 실제 위치를 즉시 대응시킨다.
컨테이너 삭제·복제·복사(⌘C/V)는 자식 포함, 레이아웃끼리 중첩은 불가.

| 단계 | 컴포넌트 | 주요 플레이스홀더 |
|---|---|---|
| 탐색 | 💬 인사말 배너 | 인사 문구 |
| 탐색 | 🔍 탐색 검색창 | 플레이스홀더, 여러 줄 토글(끄면 말줄임) — 실행 시 시나리오 매칭 |
| 탐색 | ✦ 발행 칩 목록 | 발행된 시나리오 칩 자동 표시 (자리만 배치) |
| 탐색 | 🏷️ 키워드 칩 목록 | 해시태그 칩 (클릭 시 검색어 입력) |
| 탐색 | 🖼️/🃏 스토리 카드 (대형/소형) | 키커/제목/설명/이미지 |
| 설문 | 📋 설문 헤더 | 키커/제목/설명 |
| 설문 | ❓ 설문 질문 | 질문, 선택지(`메인\|서브` 쉼표 구분), 복수 선택, 한 줄 최대 1~6개, 카드/알약/정사각/원형, 가로 스크롤 토글 |
| 설문 | 🪪 프로필 요약 패널 | 원본 saved-profile 스타일 — 안내 문구, 숨길 항목(배지 클릭 동기화) |
| 계획 | 🧾 설문 요약 패널 | 원본 clean-survey-lock 스타일 — 프로필+응답 자동 요약 |
| 계획 | 🧭 계획 타이틀 | 키커/제목 |
| 계획 | 🪜 계획 단계 카드 | 번호 원(●) + 제목 + 설명 + 체크포인트 |
| 계획 | 🛍️ 추천 상품 카드 | Match n% 배지, Gmarket/외부몰 태그, 상세보기+담기(외부몰 담기불가) — 세로↔가로 적응형 |
| 계획 | 🎬 외부 영상 카드 | 출처/제목/채널/길이/썸네일(▶) — 클릭 시 이동 목업 |
| 계획 | 📰 외부 게시글 카드 | 출처/제목/본문 미리보기/작성자 |
| 계획 | ✅ 체크리스트 카드 | 제목/항목 |
| 계획 | 💳 결제 CTA 바 | 선택 요약/금액/버튼 문구 → 완료 트리거 |
| 공통 | 📝 텍스트 블록 | 키커/제목/본문 |
| 공통 | 📌 안내 카드 | 제목/내용 |
| 공통 | ↔️ 가로 스크롤 패널 | 제목 + 카드 목록(`제목\|설명\|이미지URL` 쉼표 구분) + 카드 너비 + **스크롤바 상시 표시 토글** |
| 공통 | ↕️ 세로 스크롤 패널 | 고정 높이(px) 안에서 카드 세로 스크롤 — 같은 카드 목록 형식 + 스크롤바 토글 |
| 공통 | 🔲 **그리드 패널** | N열(1~4) 카드 그리드 — 같은 카드 목록 형식 |
| 공통 | 🎠 싱글 스크롤 캐러셀 | 한 장씩 스냅되며 넘기는 가로 카드(16:9 썸네일) + 장수 힌트 — **마우스 드래그 스크롤 + 좌우 화살표 버튼 토글** |
| 공통 | 📊 **테이블** | 헤더(\| 구분) + 행(줄바꿈 구분, 셀 안 쉼표 허용) 표 — 좁으면 가로 스크롤 |
| 공통 | 🌄 이미지 카드 | 이미지 URL/캡션 |

컴포넌트 공통: 자유 배치(x/y), 너비/높이, 잠금, 숨김. 추가는 `src/lib/registry.jsx`에 정의 하나로 끝.

**텍스트 서식/키워드**: 모든 사용자 노출 텍스트는 `kText()`로 렌더 —
`[[키워드]]`(점선 밑줄 → 설명 모달), `{{b,s24,c#hex,fserif|텍스트}}`(부분 서식) 마크업이 캔버스·플레이어에서 동일하게 보인다.

## 9. 데이터 & 저장

| 항목 | 내용 |
|---|---|
| 저장 위치 | localStorage — **`ddak-accounts-v1`** `{accounts:[{id, profile, explore, scenarios, threads}], activeId}` + 전역 `ddak-keywords-v1`, `ddak-viewer-device-v1` |
| 마이그레이션 | 구 단일 키(`ddak-scenarios-v1` 등)는 최초 실행 때 첫 계정으로 이관 (원본 보존) |
| 자동 저장 | 모든 변경 즉시 저장 (새로고침 안전) |
| 시나리오 모델 | `{ id, title, chip, color, device, status, versions[], stages: { survey[], plan[] } }` |
| 컴포넌트 모델 | `{ id, type, x, y, w, h(null=자동), locked, hidden, props }` |
| 쓰레드 모델 | `{ id, scenarioId, title, chip, color, stage, stageLabel, cart[], status, startedAt, updatedAt }` |
| 공유 수단 | ① URL 해시 링크 (1개, 즉시 체험) ② JSON 파일 (전체 백업/이관) |
| 한계 | 발행은 각 사용자 브라우저 로컬 한정 (정적 호스팅, 서버 없음) |
