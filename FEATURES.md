# DDAK Scenario Studio — 기능 명세

지마켓 뷰티 AI 쇼핑 컨셉의 **시나리오 목업 제작·발행·체험 도구**.
"공통 탐색(홈) → 칩 클릭 → 설문 → 조건에 맞는 계획 케이스" 플로우를 노코드로 편집하고, 발행하면 홈에 칩으로 노출되어 실제처럼 체험할 수 있다.

- 라이브: `https://dlwlsgkdn.github.io/gmkt-intent-home/docs/`
- 데이터는 전부 브라우저 localStorage (서버 없음). 공유는 JSON 파일 또는 URL 링크로.

---

## 0. 사용자 프로필별 워크스페이스 (계정)

**계정 = 프로필 + 탐색(DDAK) 페이지 + 시나리오 + 쇼핑 쓰레드 묶음.** 프로필을 전환하면 네 가지가 통째로 바뀐다.

| 기능 | 설명 |
|---|---|
| 프로필 컨트롤 | 홈 **좌상단, 기기 폭 컨트롤 옆** 필 버튼(아바타 이니셜+이름) → 드롭다운 |
| 전환 | 드롭다운에서 프로필 클릭 — 행마다 시나리오·쓰레드 개수 표시, 사용 중 표시 |
| 추가 | "+ 새 프로필" → 이름 입력 → 기본 탐색 페이지(인사말에 이름 반영)·기본 시나리오 1개로 시작 |
| 삭제 | 행의 ✕ (확인 후) — 마지막 1개는 삭제 불가 |
| 저장 | `ddak-accounts-v1` = `{accounts[], activeId}`. 구 단일 키는 최초 1회 첫 계정으로 자동 마이그레이션 |
| 기본 시나리오 | `scenario-studio/src/data/scenarios/date-makeup-plan-cases.json`을 **모든 프로필에 하나씩** 설치. 설문 조합별 계획 72케이스+기본 케이스와 외부 영상 콘텐츠 포함. 삭제는 잠금 처리하며, 구 72개 개별 팩·수동 통합본은 프로필별로 중복 없이 자동 이관 |

## 1. 홈 (공통 탐색 페이지)

원본 `gmarket-advanced-clean-home` 프레임을 그대로 유지한 화면. 모든 시나리오의 시작점. **프로필(계정)마다 별도.**

| 기능 | 설명 |
|---|---|
| 발행 칩 노출 | 발행된 시나리오만 검색창 아래 `✦#칩라벨` 칩으로 표시 |
| 칩 클릭 = 실행 | 칩 클릭이 곧 "탐색 완료" — 플레이어가 설문 단계부터 시작 |
| 칩 드래그 순서 변경 | 칩을 끌어 노출 순서 조정 (6px 이하 움직임은 클릭으로 처리) |
| 칩 색상 | 시나리오별 지정 색상으로 글자·테두리·배경 틴트 표시 |
| 검색 실행 | 검색어로 시나리오 매칭 (공백/언더스코어/대소문자 무시) → 실행 |
| 검색창 긴 텍스트 | 한 줄+말줄임(기본, input 렌더) 또는 여러 줄 자동 확장(textarea, `field-sizing`) — 탐색 편집기 옵션 |
| 플로팅 바 | 하단: 홈 / 마이(목업) / **쇼핑 쓰레드 히스토리** · 우상단: 시나리오 스튜디오 진입 |
| 좌상단 컨트롤 | 기기 폭 프리셋(전역 `ddak-viewer-device-v1`, 기본 iPhone 15 390px) + **프로필 전환** |

## 2. 쇼핑 쓰레드 히스토리 패널

하단 플로팅 바의 햄버거 버튼으로 여닫는 패널. **원본 clean-home history-sidebar 룩** 재사용. 홈·플레이어 어디서든 열림.

| 기능 | 설명 |
|---|---|
| 쓰레드 = 체험 1회 | 칩 클릭으로 시나리오를 시작하면 자동 기록 — 단계 이동·담기·완료 때마다 갱신 |
| 등장 방향 | 여는 버튼이 그룹 안에서 왼쪽/가운데/오른쪽 어디냐에 따라 그 방향에서 등장 (좌/우 사이드, 중앙 바텀시트) |
| 아코디언 카드 | #칩 라벨(색), 경과 시간, "마지막 페이즈: 설문 작성 중/계획 확인 중/체험 완료", 담은 상품 목록 |
| 쓰레드 이동 | 카드의 "쓰레드 이동" → 해당 시나리오 재실행 (삭제된 시나리오면 안내) |
| 관리 | 카드별 삭제, 전체 지우기, "+" = 홈으로 이동해 새 쓰레드 시작 유도 |
| 저장 | 계정별 `threads` (최근 30개) |

## 3. 시나리오 드로어 (스튜디오 입구)

우상단 "시나리오 스튜디오" 버튼으로 열림.

| 기능 | 설명 |
|---|---|
| 탐색 페이지 편집 진입 | 모든 시나리오가 공유하는 공통 홈 편집기로 이동 |
| 템플릿 갤러리 | 새로 만들기: **빈 시나리오 / 뷰티 브리프 / 선물 추천 플로우** (설문·계획 프리필) |
| 시나리오 JSON 내보내기/가져오기 | 현재 프로필의 전체 시나리오를 파일로 백업·공유, 가져오면 새 ID로 병합 |
| 전체 데이터 JSON 백업/복원 | 모든 프로필의 프로필 정보·탐색·시나리오·쓰레드와 키워드·기기 설정을 한 파일로 백업, 확인 후 현재 로컬 데이터를 교체 복원 |
| 시나리오 목록 | 상태 배지(발행됨/작성 중), 칩 미리보기(색상), **시험·편집·복제·삭제** |
| 시나리오 복제 | 내부 컴포넌트 ID까지 재발급된 완전한 사본 (작성 중 상태) |

## 4. 탐색 페이지 편집 (빌더 "탐색" 탭) + 프로필·키워드 편집기

**탐색(홈) 콘텐츠는 설문/계획과 동일한 캔버스 편집**: 빌더의 "탐색" 탭에서 컴포넌트를 자유 배치·편집한다 (`explore.items[]`, 계정 소유·자동 저장·모든 시나리오 홈에 즉시 반영). 발행 칩은 "발행 칩 목록" 컴포넌트 자리에 자동 표시. 구버전 설정(인사말/검색창/스토리)은 최초 1회 아이템으로 자동 변환.

별도의 **프로필 · 키워드 사전 편집기**(드로어에서 진입):

| 기능 | 설명 |
|---|---|
| 사용자 프로필 관리 | 고정 설문 정보(이름 + 라벨·값 항목) 추가/수정/삭제 — 프로필 요약 패널에 사용 |
| 키워드 사전 | `[[키워드]]` 점선 밑줄 클릭 시 뜨는 설명 모달의 사전(단어/설명/포인트) — 전역 `ddak-keywords-v1` |

## 5. 빌더 (시나리오 편집기)

### 5-1. 상단 바 (기본 3행 + 계획 케이스 행)

- **1행**: 문서 정보(뒤로·제목·칩·색·상태·자동 저장) + 발행 관련(시험해보기/버전/발행)
- **2행**: 편집 도구 그룹 — 히스토리(언두/리두) | 캔버스 줌 | 레이아웃(기기·컴팩트·자동 정렬) | 공유 — 구분선으로 분리
- **3행**: 단계 탭 — 🧭 탐색(공통 캔버스, 직접 편집·즉시 홈 반영) | 1 설문 / 2 계획 / 3 평가·보강
- **계획 탭 4행**: 계획 케이스 탭(케이스별 독립 캔버스) + 새 케이스 + 조건·우선순위 설정. 긴 목록은 좌우 버튼·가로 스크롤·마우스 휠로 이동하며 선택 케이스를 자동으로 중앙에 노출

### 5-2. 상단 바 상세
| 기능 | 설명 |
|---|---|
| 제목·칩 라벨 편집 | 인라인 입력 (칩 공백은 자동 `_` 변환) |
| 칩 색상 피커 | 6색 프리셋 (세이지/로즈/블루/앰버/플럼/슬레이트) |
| Undo / Redo | ⌘Z / ⇧⌘Z, 60단계, 연속 변경 500ms 병합, 기기 전환도 복원 |
| **🧲 컴팩트 방향** | compactType 컨벤션 — **세로**(기본, 위로 스택) / **가로**(같은 줄에서 왼쪽으로, 줄이 차면 세로 겹침만 해소) / **끄기**(자유 배치). 배치가 바뀔 때마다 적용 + 드래그 중 실시간 미리보기. 시나리오별 저장 |
| **캔버스 줌** | − / % / + 컨트롤 + ⌘+/⌘-/⌘0 (50~150%, 드래그·리사이즈·선택 좌표 자동 보정) |
| **✏️ 편집 / 👁 미리보기 모드** | 편집(기본): 컨테이너 경계를 반투명 점선으로 표시하되 실제 뷰포트 크기·스크롤·스냅을 유지하고, 전체 자식은 오른쪽 인스펙터 Navigator에서 관리. 미리보기: 실사용 모습 그대로 + 편집 크롬(태그·핸들·자식 셸) 숨김 |
| 공유 링크 복사 🔗 | 시나리오 전체를 URL 해시에 담은 링크 — 받는 사람은 링크만 열면 즉시 체험 |
| 기기 프리셋 | 데스크톱 720 / 갤럭시 Z 폴드 커버 344 / 갤럭시 S 360 / iPhone SE 375 / 갤럭시 S 울트라 384 / iPhone 15 390 / iPhone 16 Pro 402 / 갤럭시 플립·A·노트 412 / iPhone Pro Max 430 / iPhone 16 Pro Max 440 / 갤럭시 Z 폴드 펼침 690 / 태블릿 768 — 실기기 CSS 뷰포트 폭 기준, 전환 시 비례 스케일 + 겹침 보정 |
| 자동 정렬 3모드 | 1단 세로 / 2단 그리드(마소너리) / 위로 컴팩트(빈 공간 제거) |
| 시험해보기 / 버전 N / 발행 | 발행 시점 스냅샷(최근 10개)에 모든 계획 케이스·조건 포함, 빈 케이스/미완성 조건 경고, 빈 칩 라벨 자동 생성 |

### 5-3. 팔레트 (왼쪽)
| 기능 | 설명 |
|---|---|
| 컴포넌트 탭 | 단계별 컴포넌트 카드 + 검색. **클릭 = 맨 아래 추가, 캔버스로 드래그 = 원하는 위치에 배치** |
| 레이어 탭 | 배치 순서 목록: 선택(⇧ 다중), 🔒 잠금, 🚫 실행 시 숨김, ↑↓ 순서 변경, ✕ 삭제 |
| 단축키 안내 | 치트시트 상시 표시 |

### 5-4. 캔버스 (가운데)
| 기능 | 설명 |
|---|---|
| 자유 드래그 배치 | 기기 폭 프레임 안에서 자유 이동, 실시간 겹침 회피(밀려남) |
| 스마트 스냅 가이드 | 캔버스 가장자리·중앙, 다른 컴포넌트 모서리·표준 간격(14px)에 자석 스냅 + 가이드라인 |
| **러버밴드 다중 선택** | 빈 캔버스 드래그 = 선택 박스, 걸치는 컴포넌트 모두 선택 (⇧: 기존 선택에 추가) |
| **우클릭 컨텍스트 메뉴** | 컴포넌트: 복제/복사/붙여넣기/잠금/숨김/삭제 (다중 선택에 일괄 적용). 빈 곳: 여기에 붙여넣기/전체 선택 |
| 리사이즈 핸들 | 우하단 모서리 드래그로 너비·높이 조절 |
| 더블클릭 인라인 편집 | 컴포넌트 안에서 바로 편집(WYSIWYG) — 드래그 선택 시 서식 툴바, 즉시 미리보기 |
| 캔버스 내 편집 | 프로필 요약 패널의 배지를 캔버스에서 직접 클릭해 노출 토글 |
| **컨테이너 내부 전체 보기** | 컨테이너나 자식을 선택하면 오른쪽 인스펙터 상단에 내부 컴포넌트 Navigator 표시. 번호·종류·대표 문구·크기를 한눈에 확인하고 선택/순서 변경/잠금/숨김/복제/꺼내기/삭제 가능, 6개 이상이면 검색 제공. 자식을 선택해도 목록 유지 |
| 미리보기 편집 잠금 | 최상위·컨테이너 자식 드래그, 선택, 리사이즈, 드롭, 인라인·인스펙터·팔레트·단축키 편집을 차단하되 스크롤·캐러셀의 마우스 드래그는 허용 |

### 5-5. 인스펙터 (오른쪽)
| 기능 | 설명 |
|---|---|
| 속성 편집 | 컴포넌트별 필드 (텍스트/멀티라인/토글) 실시간 반영 |
| 선택 서식 툴바 | 문구 드래그 선택 → 볼드/**폰트 7종**(나눔명조·고운바탕·송명·주아·나눔손글씨·헤드라인·모노, Google Fonts)/크기/색(6색)/**밑줄(키워드 모달 연결)**/서식 지우기 — `{{서식\|텍스트}}` 마크업으로 저장 |
| 키워드 자동 등록 | '밑줄' 적용 시 사전에 없는 단어 자동 등록 |
| 크기·상태 | 너비/높이 슬라이더("자동 높이" 복귀), 잠금/숨김, 복제/삭제 |
| 다중 선택 도구 | 정렬(왼쪽/가운데/오른쪽/세로 간격 균등) + 모두 복제/모두 삭제 |

### 5-6. 키보드 단축키
| 키 | 동작 |
|---|---|
| ⌘Z / ⇧⌘Z | 실행 취소 / 다시 실행 |
| ⌘A | 현재 단계 전체 선택 |
| ⌘C / ⌘X / ⌘V | 복사 / 잘라내기 / 붙여넣기 (**단계 간 이동 가능**) |
| ⌘D | 선택 복제 |
| ⌘+ / ⌘- / ⌘0 | 캔버스 확대 / 축소 / 100% |
| Delete/Backspace | 선택 삭제 |
| 방향키 / ⇧+방향키 | 8px / 1px 이동 (다중 선택 지원) |
| Esc | 선택 해제 (컨텍스트 메뉴 닫기) |
| ⇧+클릭 / 빈 곳 드래그 | 다중 선택 |
| 더블클릭 | 인라인 편집 / 인스펙터 포커스 |

### 5-7. 계획 케이스와 설문 조합 조건

계획 단계는 단일 페이지가 아니라 `planCases[]`의 여러 독립 페이지로 구성된다. 각 케이스는 고유한 아이템 캔버스와 설문 조건을 가지며, 플레이어는 일반 케이스를 위에서부터 평가해 처음 일치한 페이지를 실행한다. 아무 조건도 맞지 않으면 기본 케이스를 실행한다.

| 기능 | 설명 |
|---|---|
| 케이스별 독립 캔버스 | 케이스마다 계획 컴포넌트·레이아웃·위치가 완전히 독립 |
| 조건 조합 | 여러 조건을 **모두 만족(AND)** / **하나라도 만족(OR)** 으로 결합 |
| 질문별 판정 | 선택지 중 하나 / 모두 선택 / 모두 미선택 / 응답함 / 응답하지 않음 |
| 복수 선택 지원 | 한 설문 질문의 여러 선택지를 `모두 선택` 조건으로 판정 가능 |
| 우선순위 | 케이스 순서가 곧 우선순위. 조건이 겹치면 위에 있는 첫 케이스 선택 |
| 기본 케이스 | 일반 조건이 하나도 맞지 않을 때 실행되는 폴백 페이지. 항상 하나만 유지 |
| 관리 | 새 케이스, 이름 변경, 복제, 삭제, 우선순위 이동, 기본 케이스 지정 |
| 구데이터 마이그레이션 | 기존 `stages.plan[]`은 최초 로드 시 내용이 유지된 `기본 계획` 케이스로 자동 변환 |

### 5-8. 계획 케이스 평가 · 보강

계획 케이스가 많을 때 품질·조건 다양성이 높은 3개를 `CASE A / B / C`로 자동 선정하고,
각 케이스의 **실제 노출 콘텐츠 컴포넌트 인스턴스**를 0~5점과 수정사항으로 평가한다.
자식을 가진 레이아웃 컨테이너는 중복 평가에서 제외하고, 그 안의 상품 카드 등 실제 콘텐츠는 각각 독립 평가한다.

| 기능 | 설명 |
|---|---|
| CASE A/B/C 추천 | 설문 세그먼트 구체성·콘텐츠 완성도·케이스 차별성·폴백 중요도를 계산하고, 유사한 조건 조합이 겹치지 않게 대표 3개 선정 |
| 미평가 로테이션 | `다음 3개 선정`은 평가 흔적(별점·피드백)이 있는 케이스를 후보에서 빼고 미평가에서 뽑는다(모자라면 평가된 케이스로 채움). 기존 평가는 지워지지 않고 리더보드·전파 씨앗으로 누적 |
| 케이스 리더보드 | 평가된 케이스를 평균 별점 내림차순으로 순위 매겨 말풍선 레일 옆 **상시 사이드바**(스티키)로 표시(별점·피드백 수, 현재 CASE A/B/C 배지, 케이스 열기). 이 순위가 피드백 전체 반영의 씨앗 우선순위 |
| 피드백 전체 반영 씨앗 범위 | 전파 다이얼로그에서 씨앗을 리더보드 상위 N개 케이스(3/5/10/전체)로 제한 가능. 대상은 씨앗 제한과 무관하게 평가 흔적 있는 케이스 전부를 제외한 미평가 케이스만 — 반영 완료(✓) 여부에 따라 현재 문구를 검증된 모범 예시/참고용으로 구분 전달 |
| 실제 컴포넌트 단위 | `planStep`은 제목·설명·체크포인트를 포함한 카드 전체를 한 번만 평가. `productCard`는 상품마다 별도 점수와 메모를 저장하며, 다른 콘텐츠 컴포넌트도 실제 item ID별로 한 번씩 평가 |
| 실제 모양 미리보기 | 레지스트리의 실제 컴포넌트를 읽기 전용으로 렌더. 추천 상품 평가 카드는 가로 트랙에서 드래그·터치·휠·방향키로 이동 |
| 빠른 점수 입력 | 각 항목에 0·1·2·3·4·5 버튼을 한 번에 노출. 0점도 유효한 완료 평가이며 미평가와 구분 |
| 수정사항 | 오류, 누락, 추가 내용, 더 좋은 대안, 교체 문구·상품을 자유 입력. 입력 즉시 자동 저장 |
| 일관된 평가 기준 | 고정 집계 헤더의 `0~5 평가 기준`에서 언제든 확인. 특히 `5점=수정·추가 없음`, `4점=오류는 없지만 더 좋은 대안 있음`을 별도 강조 |
| 자동 집계 | 실제 평가 컴포넌트 수에 따라 CASE·STEP 최대점수와 전체 진행률을 동적으로 계산하고, 컴포넌트 유형별 평균을 표시 |
| 피드백 기반 수정 | `컴포넌트 수정`으로 해당 실제 item과 인스펙터를 열고, 상단에 카드 전체 수정사항·점수를 유지해 반영 완료 처리 |
| AI 수정 요청 | 미반영 피드백(선택 시 완료 피드백 포함)을 실제 필드 값·허용 ID와 함께 구조화한 프롬프트로 복사. 쓰던 AI에서 받은 JSON 수정안을 스키마·화이트리스트·현재값으로 검증하고 개별 선택 후 실제 컴포넌트에 일괄 적용 |
| 적용 범위 제한 | 요청에 담긴 허용 목록(caseId·itemId·fieldKey) 밖의 필드, before가 현재 값과 다른 수정안, 컴포넌트 추가·삭제·순서·조건·가격·URL 변경은 전부 차단 |
| 내부 메모 보호 | 평가 데이터는 로컬 백업·시나리오 복제·발행 버전에는 보존하되, 외부 체험용 URL 공유 링크에서는 제외 |

### 5-9. AI로 시나리오 만들기

홈 드로어 `새로 만들기`의 **`✦ AI로 시나리오 만들기`** — 검색어·페르소나·추천 상품만으로 설문 화면과 계획 화면을 한 번에 만든다.
계획 케이스 만들기(5-10)가 "골든 케이스가 있을 때 조합을 늘리는" 도구라면, 이쪽은 **그 골든 케이스 자체를 처음부터** 만든다.

| 기능 | 설명 |
|---|---|
| 입력 | 검색어(홈에서 검색할 문장, **필수**), 페르소나(현재 프로필로 프리필), 추천 상품 카탈로그(**선택**), 설문 질문 수(2~4)·계획 단계 수(2~4), 추가 지시사항 |
| ✦ AI 프롬프트 생성 | 시나리오 생성 버튼 옆. **시나리오 DB JSON 전체**(내보내기와 같은 형식)를 만들어 달라는 프롬프트를 생성한다. 레지스트리에서 컴포넌트 목록(type·props 기본값·컨테이너 여부)을 자동 추출해 넣으므로 컴포넌트가 늘면 프롬프트도 따라 늘어난다. 아이템 모델·배치 규칙·조건 규칙 포함 |
| 계획 구성 규칙 | 질문 1개 ↔ 단계 1개의 기계적 매핑을 **금지**하고, 각 조합의 **모든 답변 + 페르소나 속성을 함께 보고** 가장 유효한 계획을 LLM이 스스로 설계하도록 지시. 조합에 따라 단계 주제·순서가 달라져야 하며, 답변끼리 상충하는 조합은 그 점을 문구에서 짚도록 요청 |
| 국내 기준 웹 검색·외부 콘텐츠 | 검색을 **대한민국 기준**(한국어 검색, 유튜브 한국 채널·네이버 블로그·국내 매체·올리브영/지마켓 등)으로 하도록 지시하고, 찾은 실제 영상·게시글·이미지를 `videoCard`/`articleCard`/`imageCard`로 계획에 배치하도록 요청. **url·imageUrl은 확인된 실제 주소만, 아니면 빈 문자열**(주소 날조 금지) |
| 전 조합 케이스 요청 | 질문마다 만들 **선택지 수를 고정**시켜(2~5개) `선택지 수 ^ 질문 수` 조합 전부에 대한 계획 케이스를 빠짐없이 만들도록 지시. 다이얼로그가 예상 케이스 수를 실시간 계산해 보여주고, 60개를 넘으면 한 번에 다 만들지 못할 수 있다고 경고 |
| 평가 A/B/C 자동 지정 | 시나리오를 가장 잘 대표하는 케이스 3개에 `evaluation: {selected, slot}`을 넣도록 요청(조합이 겹치지 않고·실제로 자주 고를 법하고·내용 차이가 뚜렷한 기준). 가져오면 **평가 탭이 선정 화면을 건너뛰고 바로 CASE A/B/C로 열린다**. 슬롯이 A·B·C 하나씩이 아니면 경고 |
| JSON 파일로 받기 | 프롬프트가 `ddak-scenario.json` **파일**로 만들어 달라고 요청(케이스가 많아 붙여넣기가 어렵기 때문). STEP B의 `JSON 파일 올리기`로 업로드하면 자동 검증까지 진행되며, 붙여넣기 입력도 접이식으로 유지 |
| 만드는 방식 선택 | STEP 3에서 두 카드 중 하나를 고른다. **빠른 초안** = 레이아웃을 코드 스캐폴드가 소유해 구성이 고정적이고 안정적(설문 + 기본 계획 1개). **전체 구성** = 컴포넌트 선택·좌표까지 AI가 정해 자유롭고 모든 조합의 케이스와 평가 A/B/C까지 만들지만, 배치가 어긋나면 빌더의 `자동 정렬`로 보정 |
| API 키 불필요 | 스튜디오는 LLM API를 직접 호출하지 않는다. **모든 AI 기능이 "프롬프트 복사 → 쓰던 AI에 붙여넣기 → 결과 가져오기"** 한 가지 왕복으로 통일돼 있어, 구독만 있으면 검색·생성·케이스 확장 전 구간을 진행할 수 있다 |
| 채팅용 프롬프트 보정 | 채팅창에는 응답 스키마를 강제할 장치가 없으므로 `lib/prompt/chatPrompt.js`가 프롬프트 앞뒤로 출력 규칙(코드블록 하나만·스키마 밖 키 금지·한국어)을 덧대고, 결과를 붙여넣을 위치까지 안내. 돌아온 응답에서 JSON만 건져내는 일은 `lib/prompt/jsonAnswer.js`가 한 곳에서 처리 |
| 상품 찾기 프롬프트 | `✦ 상품 찾기 프롬프트` — 웹 검색이 가능한 AI에 붙여넣을 상품 리서치 프롬프트를 복사한다. 결과를 상품 입력란에 그대로 붙여넣고 `결과 정리`를 누르면 마크다운 표·번호·불릿을 걷어내고 상품 줄만 남기며 상품명 기준으로 중복을 제거한다. 생성에 바로 들어가지 않아 **사용자가 검토·수정** 가능 |
| 상품 없이 생성 | 카탈로그를 비우면 상품을 지어내지 않는다. 대신 각 단계에 "어떤 종류의 상품을 넣을 자리인지" 안내 카드(`productHint`)를 배치하고 CTA의 금액·수량은 비워둔다. 블록 높이가 달라지므로 단계 간격도 그에 맞춰 좁혀 배치 |
| LLM이 만드는 것 | 시나리오 제목, 설문 안내·질문·선택지, 계획 제목·단계(제목/설명/체크포인트)·상품 묶음 제목·추천 이유, CTA 문구 |
| 코드가 소유하는 것 | 레이아웃 전체(좌표·컴포넌트 구성·컨테이너 중첩)와 상품 사실(브랜드·가격·정가·링크). LLM은 좌표를 만들지 않아 레이아웃 엔진과 충돌 불가 |
| 검증 | 질문 2~4개·선택지 2~6개로 클램프, 선택지 내 쉼표·중복 제거, 단계 최소 2개, 카탈로그 밖 상품·단계 간 중복 상품 차단 |
| 결과 | 설문 화면 + `기본 계획`(폴백) 케이스 1개가 채워진 새 시나리오를 만들고 빌더로 이동. 칩 라벨·색상 자동 부여 |
| 이어서 | 계획 탭의 `✦ 케이스 만들기`로 이 케이스를 골든 케이스 삼아 설문 조합별 케이스로 확장 |

- `lib/prompt/scenarioDraft.js`(레이아웃 스캐폴드·프롬프트/스키마·검증·조립), `lib/prompt/scenarioDb.js`(DB JSON 전체 프롬프트·가져오기 검증), `components/builder/ScenarioGenerationDialog.jsx`. 프롬프트 복사↔결과 붙여넣기 UI는 `components/builder/PromptExchange.jsx`를 세 다이얼로그가 공유

### 5-10. 계획 케이스 만들기 (AI)

계획 탭의 `✦ 케이스 만들기` — 사람이 구조·사실·페르소나를 지정하면 AI가 조합별 콘텐츠만 채운다.
사람 소유: 골든 케이스(레이아웃·문체 기준 1개), 상품 카탈로그(가격·URL 등 사실), 페르소나·추가 지시.
LLM 소유: 텍스트 슬롯 문구 + 카탈로그 내 상품 선택 + 추천 이유.

| 기능 | 설명 |
|---|---|
| 조합 전개 | 설문 질문을 축으로 선택해 선택지의 데카르트 곱으로 케이스 골격 생성(조건·`sourceAnswers` 자동). "이미 있는 조합 제외" 지원 |
| 템플릿 슬롯 추출 | 골든 케이스에서 텍스트 슬롯(사실·설정 필드 제외)과 상품 슬롯을 추출. 좌표·컴포넌트 구성·영상 카드는 복제라 LLM이 레이아웃을 못 건드림 |
| 상품 카탈로그 | "브랜드 \| 상품명 \| 가격 \| 정가 \| 특징" 한 줄 형식. LLM은 목록에서 고르기만 하고, 기존 케이스와 상품명이 같으면 이미지·색상 등 표시 정보 승계. 검증기가 카탈로그 밖 상품·허용 외 슬롯을 차단 |
| 배치 왕복 | 조합을 6개씩 나눠 배치별로 프롬프트를 복사하고 응답 JSON을 붙여넣는다. 상단 바에 현재 배치와 전체 진행률이 표시되며, 중간에 `여기까지만 검토`로 그만둬도 이미 검증된 케이스는 적용할 수 있다 |
| 출력 스키마 동봉 | API 응답 스키마 강제가 없으므로 프롬프트에 출력 스키마 전문을 함께 넣고, 돌아온 응답은 허용 슬롯·카탈로그 범위로 다시 검증한다 |
| 검토·적용 | 생성 케이스를 개별 선택해 폴백 앞에 초안 삽입, `generation` 태그(생성 ID·조합·원본) 기록, Undo 지원. 검수는 평가 탭에서 |

- `lib/prompt/planCases.js`(축 전개·슬롯 추출·프롬프트·검증·조립), `components/builder/CaseGenerationDialog.jsx`

## 6. 플레이어 (시나리오 체험)

| 기능 | 설명 |
|---|---|
| 설문→계획 스테퍼 | 상단 플로팅 스테퍼로 단계 이동, 설문 응답에 따라 매칭된 계획 케이스 이름 표시 |
| 기기 폭 반영 | 전역 뷰어 기기 폭의 모바일 프레임 — 좌상단 컨트롤로 즉시 전환 |
| 설문 응답 | 진행률 바, 단일/복수 선택 카드, 프로필 배지 클릭으로 회차별 제외 |
| 설문 요약 반영 | 계획 단계 요약 패널에 프로필(제외 반영) + 각 질문의 답(미응답 = "아무거나") |
| 계획 케이스 매칭 | AND/OR 조합 조건을 우선순위대로 평가해 첫 일치 케이스 선택, 미일치 시 기본 계획 실행 |
| 쓰레드 기록 | 체험 시작·단계 이동·담기·완료가 쓰레드 히스토리에 자동 기록 |
| 이어보기 분기 복원 | 쓰레드에 설문 답·프로필 제외·선택 계획 케이스를 기록해 계획 단계 이어보기 시 같은 분기 복원 |
| 숨김 제외 / ↺ 처음부터 / 담기·완료 | 빌더에서 숨긴 컴포넌트 제외, 초기화, 결제 CTA → 완료 → 홈 |
| 이 시나리오 편집 | 우상단 버튼으로 빌더 직행 |

## 7. 공유 링크 모드

- 빌더의 🔗 버튼으로 복사한 `#s=<인코딩>` URL을 열면 **저장 없이 즉시 플레이어 실행**
- "이 시나리오 편집" 클릭 시 내 스튜디오로 가져와(새 ID, 작성 중) 편집 시작

## 8. 컴포넌트 카탈로그

탐색 6종 + 설문 3종 + 계획 8종 + 공통 8종. 팔레트는 **콘텐츠 / 레이아웃** 두 그룹으로 구분. 탐색 아이템은 시나리오 기기 폭과 무관하게 저장되므로, 캔버스보다 넓으면 진입/기기 전환 시 폭에 맞게 자동 보정.

**컨테이너(레이아웃) 컴포넌트** — 가로/세로 스크롤 패널·그리드·캐러셀은 다른 컴포넌트를 안에 배치할 수 있다:
팔레트 카드나 캔버스 컴포넌트를 컨테이너 위로 드래그하면 하이라이트 후 **포인터 위치의 슬롯**에 들어감.
컨테이너를 선택한 뒤 팔레트 컴포넌트를 클릭하면 컨테이너 끝에, 자식을 선택한 뒤 클릭하면 바로 다음 슬롯에 삽입.
**안의 자식도 캔버스 컴포넌트처럼 직접 조작** — 클릭 선택, 더블클릭 인라인 편집/인스펙터,
우하단 리사이즈 핸들·인스펙터 슬라이더로 크기 조절(자식 고유 크기 유지, 슬롯이 강제 축소하지 않음),
드래그 시 컨테이너 안에서는 원래 구조를 유지한 채 **삽입 캐럿+포인터 고스트로 위치를 미리보기**하고 놓을 때 한 번만 순서를 변경.
컨테이너의 제목·패딩·슬롯 사이 여백은 내부로 판정하며, 부모 전체 경계를 명확히 벗어나면 자동으로 꺼내짐.
드래그로 컨테이너에 넣을 때는 대상 레이아웃 이름과 삽입 위치 가이드 라인 표시. 기본은 비어 있고(드롭존 표시, 실행 화면에선 숨김)
텍스트 카드 목록(`제목|설명|이미지URL`)은 선택 사항. 레이어 패널에 자식이 ↳ 들여쓰기로 표시되고
⠿ 드래그로 부모/슬롯 순서를 정밀 변경할 수 있으며, ↑↓·꺼내기(⤴)·잠금/숨김도 지원. 인스펙터에도 "레이아웃에서 꺼내기" 제공.
컨테이너나 자식을 선택하면 **편집 전용 내부 전체 보기**가 오른쪽 인스펙터 상단에 고정되어 자식 번호·유형·요약·크기와 관리 액션을 제공하고, 6개 이상은 검색할 수 있다. 이 목록은 캔버스 높이와 충돌 계산을 점유하지 않으며, 캔버스 안 자식에도 같은 번호·유형 배지를 표시해 목록과 실제 위치를 즉시 대응시킨다.
컨테이너 삭제·복제·복사(⌘C/V)는 자식 포함, 레이아웃끼리 중첩은 불가.

| 단계 | 컴포넌트 | 주요 플레이스홀더 |
|---|---|---|
| 탐색 | 💬 인사말 배너 | 인사 문구 |
| 탐색 | 🔍 탐색 검색창 | 플레이스홀더, 여러 줄 토글(끄면 말줄임) — 실행 시 시나리오 매칭 |
| 탐색 | ✦ 발행 칩 목록 | 발행된 시나리오 칩 자동 표시 (자리만 배치) |
| 탐색 | 🏷️ 키워드 칩 목록 | 해시태그 칩 (클릭 시 검색어 입력) |
| 탐색 | 🖼️/🃏 스토리 카드 (대형/소형) | 키커/제목/설명/이미지 |
| 설문 | 📋 설문 헤더 | 키커/제목/설명 |
| 설문 | ❓ 설문 질문 | 질문, 선택지(`메인\|서브` 쉼표 구분), 복수 선택, 한 줄 최대 1~6개, 카드/알약/정사각/원형, 가로 스크롤 토글 |
| 설문 | 🪪 프로필 요약 패널 | 원본 saved-profile 스타일 — 안내 문구, 숨길 항목(배지 클릭 동기화) |
| 계획 | 🧾 설문 요약 패널 | 원본 clean-survey-lock 스타일 — 프로필+응답 자동 요약 |
| 계획 | 🧭 계획 타이틀 | 키커/제목 |
| 계획 | 🪜 계획 단계 카드 | 번호 원(●) + 제목 + 설명 + 체크포인트 |
| 계획 | 🛍️ 추천 상품 카드 | Match n% 배지, Gmarket/외부몰 태그, 상세보기+담기(외부몰 담기불가) — 세로↔가로 적응형 |
| 계획 | 🎬 외부 영상 카드 | 출처/제목/채널/길이/영상 URL/썸네일(▶) — YouTube URL은 썸네일 자동 추출, 실행 시 새 탭 이동 |
| 계획 | 📰 외부 게시글 카드 | 출처/제목/본문 미리보기/작성자/게시글 URL — 실행 시 새 탭 이동 |
| 계획 | ✅ 체크리스트 카드 | 제목/항목 |
| 계획 | 💳 결제 CTA 바 | 선택 요약/금액/버튼 문구 → 완료 트리거 |
| 공통 | 📝 텍스트 블록 | 키커/제목/본문 |
| 공통 | 📌 안내 카드 | 제목/내용 |
| 공통 | ↔️ 가로 스크롤 패널 | 제목 + 카드 목록(`제목\|설명\|이미지URL` 쉼표 구분) + 카드 너비 + **스크롤바 상시 표시 토글** |
| 공통 | ↕️ 세로 스크롤 패널 | 고정 높이(px) 안에서 카드 세로 스크롤 — 같은 카드 목록 형식 + 스크롤바 토글 |
| 공통 | 🔲 **그리드 패널** | N열(1~4) 카드 그리드 — 같은 카드 목록 형식 |
| 공통 | 🎠 싱글 스크롤 캐러셀 | 한 장씩 스냅되며 넘기는 가로 카드(16:9 썸네일) + 장수 힌트 — **마우스 드래그 스크롤 + 좌우 화살표 버튼 토글** |
| 공통 | 📊 **테이블** | 헤더(\| 구분) + 행(줄바꿈 구분, 셀 안 쉼표 허용) 표 — 좁으면 가로 스크롤 |
| 공통 | 🌄 이미지 카드 | 이미지 URL/캡션 |

컴포넌트 공통: 자유 배치(x/y), 너비/높이, 잠금, 숨김. 추가는 `src/lib/registry.jsx`에 정의 하나로 끝.

**텍스트 서식/키워드**: 모든 사용자 노출 텍스트는 `kText()`로 렌더 —
`[[키워드]]`(점선 밑줄 → 설명 모달), `{{b,s24,c#hex,fserif|텍스트}}`(부분 서식) 마크업이 캔버스·플레이어에서 동일하게 보인다.

## 9. 데이터 & 저장

| 항목 | 내용 |
|---|---|
| 저장 위치 | localStorage — **`ddak-accounts-v1`** `{accounts:[{id, profile, explore, scenarios, threads}], activeId}` + 전역 `ddak-keywords-v1`, `ddak-viewer-device-v1` |
| 마이그레이션 | 구 단일 키(`ddak-scenarios-v1` 등)는 최초 실행 때 첫 계정으로 이관 (원본 보존) |
| 자동 저장 | 모든 변경 즉시 저장 (새로고침 안전) |
| 시나리오 모델 | `{ id, title, chip, color, device, status, versions[], stages: { survey[], plan:[] }, planCases[] }` (`stages.plan`은 구데이터 입력 호환용) |
| 계획 케이스 모델 | `{ id, name, conditionMode:'all'\|'any', conditions[], isFallback, items[], evaluation }` |
| 평가 모델 | `evaluation = { selected, slot:'A'\|'B'\|'C', components:{ [itemId]:{score:null\|0..5,feedback,resolved,updatedAt} }, updatedAt }` |
| 계획 조건 모델 | `{ id, questionId, operator, values[] }` — 설문 질문 ID와 선택지 값을 참조 |
| 컴포넌트 모델 | `{ id, type, x, y, w, h(null=자동), locked, hidden, props }` |
| 쓰레드 모델 | `{ id, scenarioId, title, chip, color, stage, stageLabel, planCaseId/name, answers, excludedProfile, cart[], status, startedAt, updatedAt }` |
| 공유·이관 수단 | ① URL 해시 링크 (시나리오 1개, 즉시 체험) ② 시나리오 JSON (현재 프로필에 병합) ③ 전체 데이터 JSON (모든 로컬 데이터 교체 복원) |
| 한계 | 발행은 각 사용자 브라우저 로컬 한정 (정적 호스팅, 서버 없음) |
