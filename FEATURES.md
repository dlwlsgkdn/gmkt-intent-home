# DDAK Scenario Studio — 기능 명세

지마켓 뷰티 AI 쇼핑 컨셉의 **시나리오 목업 제작·발행·체험 도구**.
"공통 탐색(홈) → 칩 클릭 → 설문 → 계획" 플로우를 노코드로 편집하고, 발행하면 홈에 칩으로 노출되어 실제처럼 체험할 수 있다.

- 라이브: `https://dlwlsgkdn.github.io/gmkt-intent-home/docs/`
- 데이터는 전부 브라우저 localStorage (서버 없음). 공유는 JSON 파일 또는 URL 링크로.

---

## 1. 홈 (공통 탐색 페이지)

원본 `gmarket-advanced-clean-home` 프레임을 그대로 유지한 화면. 모든 시나리오의 시작점.

| 기능 | 설명 |
|---|---|
| 발행 칩 노출 | 발행된 시나리오만 검색창 아래 `✦#칩라벨` 칩으로 표시 |
| 칩 클릭 = 실행 | 칩 클릭이 곧 "탐색 완료" — 플레이어가 설문 단계부터 시작 |
| 칩 드래그 순서 변경 | 칩을 끌어 노출 순서 조정 (6px 이하 움직임은 클릭으로 처리) |
| 칩 색상 | 시나리오별 지정 색상으로 글자·테두리·배경 틴트 표시 |
| 검색 실행 | 검색어로 시나리오 매칭 (공백/언더스코어/대소문자 무시) → 실행 |
| 플로팅 바 | 하단: 홈 / 마이(목업) / 시나리오 목록 · 우상단: 시나리오 스튜디오 진입 |
| 모바일 프레임 | 홈·설문·계획 실행 화면은 모바일 폭 프레임으로 고정 — **좌상단 컨트롤로 기기 폭 선택**(스튜디오와 동일 프리셋, 전역 저장 `ddak-viewer-device-v1`, 기본 iPhone 15 390px) |

## 2. 시나리오 드로어 (스튜디오 입구)

우상단 "시나리오 스튜디오" 버튼 또는 하단 목록 버튼으로 열림.

| 기능 | 설명 |
|---|---|
| 탐색 페이지 편집 진입 | 모든 시나리오가 공유하는 공통 홈 편집기로 이동 |
| 템플릿 갤러리 | 새로 만들기: **빈 시나리오 / 뷰티 브리프 / 선물 추천 플로우** (설문·계획 프리필) |
| JSON 내보내기/가져오기 | 전체 시나리오를 파일로 백업·공유, 가져오면 새 ID로 병합 |
| 시나리오 목록 | 상태 배지(발행됨/작성 중), 칩 미리보기(색상), **시험·편집·복제·삭제** |
| 시나리오 복제 | 내부 컴포넌트 ID까지 재발급된 완전한 사본 (작성 중 상태) |

## 3. 탐색 페이지 편집기 (공통)

시나리오와 무관하게 전역 1개. 왼쪽 라이브 미리보기 + 오른쪽 편집 필드, 자동 저장.

| 기능 | 설명 |
|---|---|
| 홈 콘텐츠 편집 | 인사말 문구, 검색창 플레이스홀더, 웹진 스토리 3종(키커/제목/설명/이미지) |
| 사용자 프로필 관리 | 고정 설문 정보(이름 + 라벨·값 항목) 추가/수정/삭제 — 프로필 요약 패널에 사용 |
| 기본값 초기화 | 한 번에 기본 구성으로 복원 |

## 4. 빌더 (시나리오 편집기)

### 4-1. 단계 탭
- **🧭 탐색(공통)** — 탐색 편집기로 이동, 뒤로가기로 빌더 복귀
- **1 설문 / 2 계획** — 시나리오가 소유하는 두 단계, 탭에 컴포넌트 수 표시
- 탭 헤더는 스크롤해도 상단에 고정(sticky)

### 4-2. 상단 바
| 기능 | 설명 |
|---|---|
| 제목·칩 라벨 편집 | 인라인 입력 (칩 공백은 자동 `_` 변환) |
| 칩 색상 피커 | 6색 프리셋 (세이지/로즈/블루/앰버/플럼/슬레이트) |
| Undo / Redo | ⌘Z / ⇧⌘Z, 60단계, 연속 변경 500ms 병합, 기기 전환도 복원 |
| 공유 링크 복사 🔗 | 시나리오 전체를 URL 해시에 담은 링크 — 받는 사람은 링크만 열면 즉시 체험 |
| 기기 프리셋 | 데스크톱 720 / 갤럭시 360 / iPhone SE 375 / iPhone 15 390 / Pro Max 430 / 태블릿 768 — 전환 시 컴포넌트 비례 스케일 + 겹침 보정 |
| 자동 정렬 3모드 | 1단 세로 / 2단 그리드(마소너리) / 위로 컴팩트(빈 공간 제거) |
| 시험해보기 | 현재 상태로 플레이어 실행 |
| 버전 N | 발행 시점 스냅샷(최근 10개) 목록 → 클릭 복원 (복원 전 상태는 ⌘Z로 복구) |
| 발행 / 재발행 / 발행 취소 | 빈 단계 경고, 빈 칩 라벨은 제목으로 자동 생성 |
| 자동 저장 표시 | "자동 저장됨 · 시각" 상시 노출 |

### 4-3. 팔레트 (왼쪽)
| 기능 | 설명 |
|---|---|
| 컴포넌트 탭 | 단계별 컴포넌트 카드 + **이름/설명 검색**, 클릭으로 추가(맨 아래 배치) |
| 레이어 탭 | 배치 순서 목록: 선택(⇧ 다중), **🔒 잠금, 🚫 실행 시 숨김**, ↑↓ 순서 변경, ✕ 삭제, 내용 미리보기 |
| 단축키 안내 | 치트시트 상시 표시 |

### 4-4. 캔버스 (가운데)
| 기능 | 설명 |
|---|---|
| 자유 드래그 배치 | 기기 폭 프레임 안에서 자유 이동 |
| 실시간 겹침 회피 | 드래그 중 겹치는 컴포넌트가 애니메이션으로 밀려남 (드롭 후 커밋) |
| 스마트 스냅 가이드 | 캔버스 가장자리·중앙, 다른 컴포넌트 모서리·표준 간격(14px)에 자석 스냅 + 빨간 가이드라인 |
| 리사이즈 핸들 | 우하단 모서리 드래그로 너비·높이 조절 |
| 다중 선택 | ⇧+클릭 토글, ⌘A 전체 — **그룹 드래그 이동**, 방향키 그룹 이동 |
| 잠금/숨김 시각화 | 잠금: 드래그·리사이즈 불가 + 밀리지 않는 고정 장애물 / 숨김: 흐리게 표시 |
| 더블클릭 | 인스펙터 첫 입력란 포커스(전체 선택) — 즉시 문구 편집 |
| 캔버스 내 편집 | 프로필 요약 패널의 배지를 캔버스에서 직접 클릭해 노출 토글 |

### 4-5. 인스펙터 (오른쪽)
| 기능 | 설명 |
|---|---|
| 속성 편집 | 컴포넌트별 필드 (텍스트/멀티라인/토글) 실시간 반영 |
| 선택 서식 툴바 | **문구를 드래그 선택하면 플로팅 툴바** — 볼드/폰트(명조·모노)/크기(px)/색(6색), **밑줄(키워드 설명 모달 연결)**, 서식 지우기. 선택 구간에만 적용(`{{서식\|텍스트}}` 마크업), 같은 구간 재선택 시 서식 병합 |
| 키워드 자동 등록 | 툴바 '밑줄' 적용 시 사전에 없는 단어는 자동 등록 (설명은 탐색 편집기에서 작성) |
| 크기 | 너비/높이 슬라이더, "자동 높이로 되돌리기" |
| 상태 토글 | 잠금 / 실행 시 숨김 |
| 복제·삭제 | 단일 컴포넌트 |
| 다중 선택 도구 | **정렬: 왼쪽/가운데/오른쪽/세로 간격 균등** + 모두 복제/모두 삭제 |

### 4-6. 키보드 단축키
| 키 | 동작 |
|---|---|
| ⌘Z / ⇧⌘Z | 실행 취소 / 다시 실행 |
| ⌘A | 현재 단계 전체 선택 |
| ⌘D | 선택 컴포넌트(들) 복제 |
| Delete/Backspace | 선택 컴포넌트(들) 삭제 |
| 방향키 / ⇧+방향키 | 8px / 1px 이동 (다중 선택 지원) |
| Esc | 선택 해제 |
| ⇧+클릭 | 다중 선택 토글 |
| 더블클릭 | 인스펙터 포커스 |

## 5. 플레이어 (시나리오 체험)

| 기능 | 설명 |
|---|---|
| 설문→계획 스테퍼 | 상단 플로팅 스테퍼로 단계 이동, "#칩으로 진입 · 탐색 완료" 표시 |
| 기기 폭 반영 | 전역 뷰어 기기 폭의 모바일 프레임으로 표시 — 좌상단 컨트롤로 즉시 전환 |
| 설문 진행률 | 답한 질문 수 / 전체 질문 수 진행 바 |
| 설문 응답 | 단일/복수 선택 카드, 프로필 패널 배지 클릭으로 회차별 제외 |
| 설문 요약 반영 | 계획 단계의 요약 패널에 프로필(제외 반영) + 각 질문의 답(미응답 = "아무거나") |
| 숨김 제외 | 빌더에서 숨긴 컴포넌트는 렌더되지 않음 |
| ↺ 처음부터 | 응답·제외·담기 초기화 후 1단계로 |
| 담기 / 완료 | 상품 담기 카운터, 결제 CTA → 완료 토스트 → 홈 복귀 |
| 이 시나리오 편집 | 우상단 버튼으로 빌더 직행 |

## 6. 공유 링크 모드

- 빌더의 🔗 버튼으로 복사한 `#s=<인코딩>` URL을 열면 **저장 없이 즉시 플레이어 실행** (수신자 환경에 흔적 없음)
- "이 시나리오 편집" 클릭 시 내 스튜디오로 가져와(새 ID, 작성 중) 편집 시작

## 7. 컴포넌트 카탈로그 (팔레트 12종)

| 단계 | 컴포넌트 | 주요 플레이스홀더 |
|---|---|---|
| 설문 | 📋 설문 헤더 | 키커/제목/설명 |
| 설문 | ❓ 설문 질문 | 질문, 선택지(`메인\|서브` 쉼표 구분), 복수 선택 토글 |
| 설문 | 🪪 프로필 요약 패널 | **원본 saved-profile 스타일** — 안내 문구, 숨길 항목(배지 클릭 동기화) |
| 계획 | 🧾 설문 요약 패널 | **원본 clean-survey-lock 스타일** — 프로필+응답 자동 요약 |
| 계획 | 🧭 계획 타이틀 | 키커/제목 |
| 계획 | 🪜 계획 단계 카드 | **원본 스타일**: 번호 원(●) + 제목 + 설명 + 체크포인트 |
| 계획 | 🛍️ 추천 상품 카드 | **원본 product-card 스타일**: Match n% 배지, Gmarket 로고/외부몰 태그, 상세보기+담기(외부몰은 담기불가) — 기본 세로형, 넓히면 가로형 |
| 계획 | 🎬 외부 영상 카드 | 출처/제목/채널/길이/썸네일(▶ 오버레이) — 세로↔가로 적응형, 클릭 시 이동 목업 |
| 계획 | 📰 외부 게시글 카드 | 출처/제목/본문 미리보기/작성자 — 세로↔가로 적응형 |
| 계획 | ✅ 체크리스트 카드 | 제목/항목 |
| 계획 | 💳 결제 CTA 바 | 선택 요약/금액/버튼 문구 → 완료 트리거 |
| 공통 | 📝 텍스트 블록 | 키커/제목/본문 |
| 공통 | 📌 안내 카드 | 제목/내용 |
| 공통 | 🌄 이미지 카드 | 이미지 URL/캡션 |

컴포넌트 공통: 자유 배치(x/y), 너비/높이, 잠금, 숨김. 추가는 `src/lib/registry.jsx`에 정의 하나로 끝.

**키워드 설명 모달**: 서술 필드(질문/설명/체크포인트/본문 등)에 `[[키워드]]`로 쓰면 점선 밑줄이 생기고, 실행 중 클릭하면 설명 모달(제목/설명/포인트)이 뜬다. 사전은 탐색 페이지 편집기의 "키워드 사전"에서 전역 관리 (`ddak-keywords-v1`, 기본: 프라이머/픽서/세라마이드).

## 8. 데이터 & 저장

| 항목 | 내용 |
|---|---|
| 저장 위치 | localStorage — `ddak-scenarios-v1`(시나리오), `ddak-explore-page-v1`(탐색), `ddak-profile-v1`(프로필) |
| 자동 저장 | 모든 변경 즉시 저장 (새로고침 안전) |
| 시나리오 모델 | `{ id, title, chip, color, device, status, versions[], stages: { survey[], plan[] } }` |
| 컴포넌트 모델 | `{ id, type, x, y, w, h(null=자동), locked, hidden, props }` |
| 공유 수단 | ① URL 해시 링크 (1개, 즉시 체험) ② JSON 파일 (전체 백업/이관) |
| 한계 | 발행은 각 사용자 브라우저 로컬 한정 (정적 호스팅, 서버 없음) |
